7 # feezal — Manual test checklist

A pre-release manual QA pass. Copy this list per release (or tick in place and reset).

Since A17, four automated suites run the bulk of the old checklist:

| Suite | Command | Covers |
|---|---|---|
| Backend unit + integration | `cd server && npm test` | storage, REST API (sites/assets/themes/history/certs/ai/packages), export ZIP, auth, git, discovery parsing, bridge + hub against a real broker |
| Frontend logic units | `cd www && npm test` | topic matching, class parser, folder reconcile, AI output validation, broker-URI fields, … |
| Component (real browsers) | `cd www && npm run test:browser` | **every element package mounted** (smoke harness), 12 elements' full MQTT behaviour, N6 custom inspectors (navbar, layout-app + host protocol), E38 scaling (container queries + ResizeObserver), packages sidebar UI (stubbed fetch), control topics, view switching, theme variables + swatch screenshots — chromium locally, +firefox/webkit in CI |
| E2E (server + broker + Chromium) | `cd www && npm run test:e2e` | editor boot, palette drag, inspector topic editing → live value, move/nudge/undo, rubber-band + Ctrl+click selection, copy/paste/duplicate/delete, context-menu lock, source mode incl. error gating, view + **site** management UI, copy-on-use asset drag (B15), deploy, reload restore, viewer retained/live MQTT (bridge **and** direct `ws://`), broker-restart reconnect, **AI assistant against a stub provider**, export ZIP **opened from `file://` with live MQTT** |

`npm run test:all` at the root chains all four.

**This file covers what automation can't judge:** visual appearance and scaling,
touch, cross-browser *rendering*, real networks/TLS and third-party services
(real npm registry, real AI providers, HA/zigbee2mqtt devices). Don't manually
re-verify what the suites assert — if an automated area regresses, add a test
instead of a checklist item.

**Issues found and fixed by the automated suites** (exceptions removed — the tests now enforce them):
- ~~`feezal-element-paper-tabs` subscription leak~~ — fixed (1.0.3); the smoke harness enforces clean disconnects for every element.
- ~~editor boot `querySelectorAll`-on-null errors~~ — fixed (null-safe `feezal.views` getter); the E2E boot check no longer filters anything.
- ~~keyboard-shortcuts modal documented arrow-nudge backwards~~ — fixed (plain arrows = grid size, Ctrl/Shift = 1 px).
- ~~site-manager rename did nothing via Enter / ✓~~ — fixed (`{host: this}` was missing from the portal `render()`, breaking every method-reference listener in the dropdown); pinned by the site-manager E2E.
- ~~elements with a user-set `z-index` (e.g. a media player) painted above the PIN lock overlay~~ — fixed (0.1.1: the overlay now lives in the browser top layer via the popover API); pinned by the system-pin component tests.

**Legend:** `[ ]` to do · `[x]` pass · `[!]` bug found (note it) · `[-]` n/a.

---

## 0. Setup / environment matrix

Run the critical paths in more than one configuration.

- [ ] Editor loads with **no console warnings/errors** (uncaught Chromium errors are gated by E2E with no exceptions; watch for *warnings* and non-Chromium errors).
- [ ] Browsers: [ ] Firefox  [ ] Safari/WebKit — component tests run there in CI; check *editor UX and rendering* (the E2E layer is Chromium-only).
- [ ] Theme: [ ] light  [ ] dark (toggle in Editor Settings) — check panels, dialogs, inspector.
- [ ] Viewport: [ ] desktop  [ ] narrow/mobile (or device emulation).
- [ ] Connection: [ ] `wss://` with TLS certs (N8) against a real broker. *(`mqtt://` bridge and plain `ws://` direct are automated.)*
- [ ] A broker with some retained topics available (for MQTT binding + auto-discovery tests).

---

## 1. Smoke test (≈2 min critical path)

The "does anything work at all" pass. If any step fails, stop and fix before the rest.
(Site creation, element drag/edit/deploy, viewer MQTT, reload restore and the
file:// export all run automated on every change.)

- [ ] **Resize** a canvas element via the handles; element + guide **snapping** feels right.
- [ ] Change the theme → editor + viewer reflect it visually.

---

## 2. Verify-first — recently built, not yet browser-tested

These shipped this cycle and were validated by build/unit tests **only** — give them a real click-through first.

### Bug-fix batch B40–B47

- [ ] **B40 (retained replay)** — bridged viewer on a `layout-app` dashboard: load view1 (values present), switch to view2 via the drawer, back to view1 → **both views show live values** with no device publish in between. Repeat with a direct-MQTT viewer.
- [ ] **B41 (deep link)** — with the viewer open, edit the address bar to `#/<view>/<drawer-entry>` → the shell switches to that drawer entry without a reload. Also verify a cold load of the same URL, and that picking a drawer entry still updates the hash to the full path (no loops, no double-loading).
- [ ] **B42 (palette filter)** — type a category (`layout`) and a family (`metro`, or `lcars` if installed) into the palette filter → the whole family/category appears; element-name search still works. **E133:** `circle` now matches the device-card category and `material` matches the MD3 widgets.
- [ ] **B43 (shortcuts dialog)** — switch the sidebar to *Assets* (or collapse the sidebar entirely), press the top-bar `?` button → the sidebar reveals the Inspector tab and the Keyboard Shortcuts dialog opens. Same for `Ctrl+I` and the `?` key. Reload → the previously saved sidebar tab is restored (the reveal is not persisted).
- [ ] **B44 (assets dialog dark mode)** — dark editor theme → Assets → new-folder button: dialog panel and input are dark (regression-tested in `test-browser/feezal-sidebar-assets-dialogs.test.js`; a quick visual glance suffices).
- [ ] **U60 (connection-lost overlay)** — with the editor open, **stop the feezal server**: within a moment a **subtle red top banner** ("Connection to server lost — reconnecting…") appears; after the **grace period (~7 s)** of continuous disconnection it escalates to a **blocking modal** (dimmed backdrop, no ✕, clicking outside does nothing) showing elapsed time (and reconnect attempt count if available), a **Retry now** and a **Reload app** button. **Restart the server** → the overlay **auto-clears** (no manual acknowledgement) and a brief green **"Reconnected to server"** toast flashes; editing resumes. A **transient blip** (server bounce that recovers within the grace period) shows only the banner and **never** the blocking modal. Editor-only (the viewer has its own concerns). Dark + light editor theme both legible. *(Grace→modal escalation, auto-clear+toast, and the transient-blip case are browser-tested in `test-browser/feezal-connection-overlay.test.js`.)*
- [ ] **B45 (add-property focus)** — style inspector → "Add CSS property" → type `font-size` ⏎ → focus lands in the new value field; type `14px` ⏎ → applied. Also with an enum property (`text-transform`) → the select gets focus.
- [ ] **B46 (component size)** — give a component instance `width: 100%` via Add CSS property → deploy → reload editor and viewer → the instance is still 100 % wide (children stay at their template px positions). A fresh instance with no explicit size still gets the template's bounding box.
- [ ] **B47 (ring drag on iOS)** — iPhone/iPad (or the installed PWA): drag the circle-light brightness ring in a scrollable view → the page must not scroll/rubber-band during the drag; the CT track still works; scrolling the page by touching OUTSIDE the card still works.

### Feature batch U47 / U49 / E121–E123

- [ ] **U47 (drawer entries)** — layout-app inspector: "+ add" appends an entry with an empty *pick a view…* dropdown and creates **no** view; the dropdown's last item *＋ Create new view…* opens a name dialog (default `pageN`, duplicate names disable Create); Create makes the view, binds the entry and defaults the label; Cancel restores the previous selection. Unbound entries render nothing in the viewer drawer.
- [ ] **U49 (condition property)** — Conditions tab: each row has a *property* field (placeholder `payload`) between subscribe and operator; set `val` against an mqtt-smarthome JSON topic → the condition evaluates the nested value; clearing the field removes the key from the saved row (check via source view); a hand-written `"property"` survives editing other fields.
- [ ] **E121 / B92 (Switch card, standalone)** — palette → Circle → **Switch** (tag `circle-switch`): drop, wire `subscribe`/`publish` → large power button toggles and publishes on tap; no brightness/mode attributes offered; styling follows the circle-light theme tokens (ON reads as the primary colour). **B92 regression:** it is a **plain switch, not a circle-light subclass** — an autodiscovered Homematic/RedMatic switch (arriving as a `light` offered on/off) publishes on tap exactly like glass-switch (both stamp `publish`); a legacy dashboard whose `circle-switch` still carries `subscribe-state`/`publish-state` keeps working (wiring falls back to them).
- [ ] **E130 (circle-switch: discovery + palette)** — the palette shows **"Switch"** in all three families (Circle's card next to glass-switch and metro-switch); a discovered switch (zigbee2mqtt, or an E126 Homematic word-list switch) offers **all three** family cards — picking Circle stamps `subscribe`/`publish`/`payload-on`/`payload-off`/label + availability, the same contract as glass/metro-switch; existing dashboards with `circle-switch` tags load unchanged. *(Discovery descriptor + palette identity + the B92 publish path are unit-tested.)*
- [ ] **E122 (on/off lights)** — circle-light: mode *On/Off only (switch)* → the ring disappears, a large centre power button toggles; the custom inspector's Topics tab shows only State; brightness/CT/colour attrs hidden. glass-light: mode `on_off` → the details popup is a pure toggle regardless of configured topics. metro-light: mode `on_off` → the tile back shows only ON/OFF.
- [ ] **E123 (cover accent)** — circle-cover: window frame, dividers and position slider render in the theme's primary colour; a **fully closed** cover fills the panel with that accent (slats stay visible); a newly inserted cover defaults to 180×220 matching circle-light; existing covers keep their saved size/colour overrides.
- [ ] **E127 (dimmer settling)** — material/glass/metro-light on a Homematic dimmer (separate mode): set brightness to 0 while at 100 % → the ring/slider **holds at 0** while the dimmer ramps (intermediate 0.95/0.5/… reports must not make it jump); an interrupted ramp reconciles to the reported value after `settle-timeout` (default 5 s); toggle-on via `payload-on 1.005` settles via WORKING/timeout (never shows 1.005). With `subscribe-working` wired: brightness changes made from ANOTHER client don't jitter (first intermediate report is buffered `report-delay-ms` ≈ 100 ms and suppressed when WORKING=true follows); WORKING=false applies the final level. RedMatic dual mode (`subscribe-settled` = …/LEVEL_NOTWORKING): the slider/ring moves **only** on settled values while the % readout (centre text / state line / front tile) follows the live LEVEL during the ramp. Discovery: a dimmer whose broker carries WORKING and/or LEVEL_NOTWORKING gets those attributes auto-wired (assign from the discovery sidebar and check the stamped attributes); a dimmer without them gets none. Inspector: all three families show the "Settling (Homematic ramps)" topics and the timeout/delay config fields.
- [ ] **Brightness % is a clean number (B79)** — on a **Homematic dimmer** (separate mode, `LEVEL` 0…1, so `brightness-max: 1`) set the light to a level whose scaling is inexact in binary — 55 %, 29 % and 7 % are the easy ones — and read the number on **every** light card: `circle-light` ring centre, `metro-light` tile line, `eink-light` value block, and **both** places `glass-light` shows it (the card face AND the brightness pill inside the details popup — the popup was the one that showed `55.00000000000001 %`). All five must read a whole number. Then check a **z2m json light** (`brightness` 0…254) the same way. Finally confirm nothing was quantised away: a device with a **sub-integer range** (e.g. `brightness-max: 0.5`) still reports fractional percentages rather than snapping to whole percent, and toggling off/on restores the previous level exactly (the remembered level feeds back through the same scaling). *(The scaling itself — noise removed, sub-integer precision and the percent→raw→percent round trip preserved — is unit-tested in `test/feezal-light-brightness-rounding.test.js`; the per-card rendering is the manual part.)*
- [ ] **E128 (blind settling + travel direction)** — the same machinery as E127, now inside `@feezal/feezal-controller-cover`, so **circle / glass / metro / eink cover** all get it. On a native Homematic blind (separate mode, LEVEL 0…1): drag the position slider to a new value while the blind is elsewhere → the control **holds at the target** for the whole travel; the intermediate LEVEL reports (0.12 / 0.31 / 0.55 …) must never make it jump, and it releases when the target is reported. `settle-timeout` defaults to **60 s here, not 5** — blinds travel far longer than a dimmer ramps; an interrupted travel reconciles to the reported position after it. **STOP mid-travel** → the slider immediately shows where the blind actually halted (the hold is abandoned, not held to the timeout). With `subscribe-working` wired, a change made from ANOTHER client doesn't jitter (same `report-delay-ms` ≈ 100 ms buffer as E127) and WORKING=false applies the final position. RedMatic dual mode (`subscribe-settled` = …/LEVEL_NOTWORKING): the slider follows settled values only. **Venetian slats**: the tilt slider settles on its own target the same way (the channel's WORKING covers both movements; LEVEL_NOTWORKING carries the position only, so tilt keeps following its live topic) — worth a real-hardware pass on a venetian actuator to confirm the slat reports ramp as assumed. **Travel-direction indicator**: with `subscribe-direction` wired (Homematic DIRECTION: NONE / UP / DOWN / UNDEFINED) a small pulsing ▲/▼ appears top-left while the motor runs and clears on NONE — on **eink** it is a static ▲/▼ glyph instead (the family forbids animation) and it participates in the redraw signature. Both the enum NAME and its numeric device value (1 = up, 2 = down) are accepted; anything else reads as idle. **Discovery**: a blind whose broker carries WORKING / LEVEL_NOTWORKING / DIRECTION gets exactly those attributes auto-wired (assign from the discovery sidebar and check the stamped attributes); a blind without them gets none. **z2m covers are untouched** — the json payload mode never routes through the settler. *(Hold-at-target, WORKING gating, settled takeover, STOP cancel, timeout reconcile, range scaling, tilt settling, the direction indicator on all four families and the observed-only recognizer wiring are tested: `test-browser/feezal-movement.test.js`, `server/test/native-discovery.test.js`.)*
- [ ] **E154 (lock movement)** — a motor lock takes a second or two to turn, and **circle / glass / metro lock** used to snap between locked ⇄ unlocked with nothing in between. Now: press **Lock / Unlock / Open** → the card immediately shows a transitional **locking… / unlocking… / opening…** state with the lock disc/icon pulsing, and resolves when the device confirms. With **`subscribe-direction`** wired (Homematic Keymatic `DIRECTION` — NONE / UP / DOWN / UNDEFINED; Keymatic has no WORKING datapoint, DIRECTION plays that role) the datapoint is the authority: turning the key **physically** or from another app also shows the transitional state, and it ends when DIRECTION returns to NONE — not when STATE arrives. Without a movement topic the expected STATE ends it, and `movement-timeout` (default 20 s) always resolves it, so a card can never be stuck on "locking…". A **jam** drops the transitional state at once (the motor failed). Which way a Keymatic turns to lock is mounting-specific — if the card names the wrong direction for externally-started movement, swap **`payload-direction-lock`** / **`payload-direction-unlock`** (one attribute); a device that publishes a plain boolean "motor running" flag instead shows a neutral **moving…**. **Discovery**: a Keymatic whose broker carries DIRECTION gets it auto-wired; one without it gets nothing (observed-only). A lock with no movement signal behaves exactly as before apart from the brief post-command hold. *(Command hold, signal-vs-state authority, external movement, jam, timeout, the swapped mapping, the boolean flag and the observed-only recognizer wiring are tested: `test-browser/feezal-movement.test.js`, `server/test/native-discovery.test.js`.)*
- [ ] **E160 (device-class value icons)** — assign a multi-sensor device (ESPHome/z2m BME680: temperature, humidity, pressure, CO2, VOC, PM2.5) from the ⚡ picker to **circle-value** / **glass-value** cards: each stamps a **fitting icon** from its `device_class` — humidity → water drop, pressure → compress, CO2 → co2, PM2.5 → blur, VOC → air — **not** a thermometer on every card. An unknown `device_class` (e.g. IAQ) falls back to the neutral **sensors** icon; an entity with no `device_class` leaves the icon untouched. **glass-value's default icon is now `sensors`, not `thermostat`** — a *manually*-placed glass-value (no discovery, no icon set) shows the neutral glyph rather than a misleading thermometer (existing dashboards that relied on the thermostat default now show `sensors` — set the icon explicitly to restore it). *(The `NUMERIC_SENSOR_ICONS` table, the `device_class`→icon stamp, the `_default` fallback and the no-class no-op are unit-tested: `test/feezal-numeric-sensor-icons.test.js`.)*
- [ ] **B87 (viewer trailing slash)** — open a **named** site's viewer directly by hand-typing `/viewer/<site>` (no trailing slash): the address bar settles at **`/viewer/<site>/#/<view>`** (a 301 redirect adds the slash before the hash — no more `/viewer/App#/Menu`). A query string survives (`/viewer/App?sha=abc1234` → `/viewer/App/?sha=abc1234`); an already-slashed URL and the default site (`/viewer/`) are unaffected. *(The redirect, query preservation, no-loop and default-site cases are tested: `server/test/viewer-user-packages.test.js`.)*
- [ ] **E161 (HA discovery: friendly names / icons / areas)** — with an **ESPHome** device connected (which sends abbreviated `dev:{ids,name,mdl,sa}` + `ic:` icons): **(1) friendly names** — the ⚡ picker rows read **"Lichterkette Ida relay"** (device friendly name + entity), not the raw MQTT topic; a multi-attribute z2m device still shows one distinguishable row per attribute under the device name ("Sensor Cellar temperature" / "… humidity"). **(2) icons** — assign an entity whose config carries an `icon` like `mdi:lightbulb`/`mdi:blur`/`mdi:gauge` to a **circle-value**/**glass-value** card: it stamps the mapped Material Symbol (lightbulb / blur_on / speed), which **overrides** the E160 `device_class` icon; an `mdi:*` name feezal has **no alias for** falls back to the `device_class` icon (never a blank glyph), and an entity with neither leaves the element default. **(3) areas** — a device that declares `suggested_area` (`sa`) exposes it on `/api/discovery/device-groups` (`group.area`) for the future Generate-by-area (U58); confirm via the API that the area is captured (not yet surfaced in the UI). *(Unit-tested: `www/test/mdi-to-symbols.test.js` — every alias is an installed Symbol; `www/test/feezal-discovery-stamp.test.js` — friendly-name label + mdiIcon hit/miss→fallback; `server/test/discovery.test.js` — area capture.)*

### Feature batch U44 / U51 / E117 / E118 / E120

- [ ] **U44 (× clear)** — attribute inspector: any populated text/number/topic/colour field shows an ×; clicking it **removes the attribute** (source view: attribute gone, not `=""`), the default reappears as placeholder, one undo step. Selects show × only while explicitly set; clearing returns to the default option. Typing then deleting all text (no ×) still writes an explicit empty attribute. Topic inputs in custom inspectors (cover, light) also carry the ×. **B51:** the × glyph is actually VISIBLE (light and dark editor mode, incl. inside `feezal-topic-input`) — Shoelace system icons (select carets, checkbox check, dialog close ×) all render, and the browser console shows **no** `Refused to connect … data:` CSP violations (the CSP grants `data:` on connect-src). *(Header token guarded in `server/test/privacy.test.js`; the rendered clear-icon SVG in `test-browser/feezal-inspector-clear-icon.test.js`.)*
- [ ] **Default-true boolean persistence** — any attribute that is a boolean defaulting to **ON** (lottie `loop`/`autoplay`; `show-position`/`show-value`/`show-close`/`close-on-backdrop`/`muted`/`drawer-persistent`/`show-effect`/… across the circle/glass/metro/eink/material/paper/panel/tui families): uncheck it in the inspector, **deploy**, reload the viewer → it stays **OFF** (previously it reverted to ON, because the "off" state serialised as an *absent* attribute = back to the default). Source view: an OFF default-true boolean is written as an explicit `attr="false"` (not removed), an ON one as `attr="true"`; the inspector checkbox shows the correct **default-checked** state when the attribute is absent. Default-FALSE booleans are unchanged (present when on, removed when off). Spot-check a lottie (`loop` off → plays once and stops), a cover (`show-position` off), and a dialog (`show-close` off) end-to-end. *(Mechanism: the shared `feezalBoolean` converter reads/reflects the literal "false", paired with the inspector's default-aware boolean write + display — unit-tested in `feezal-sidebar-inspector-attributes.test.js`; lottie loop/autoplay round-trip in `feezal-element-basic-lottie.test.js`.)*
- [ ] **U48 (optional connection toasts)** — Site Settings → Viewer → Viewer presence: a new **"Show connection toasts"** switch appears only while presence is on; default on = unchanged behaviour. Turn it off → deploy → open the viewer: no `Connected as "…"` pop-up on load (and no rename pop-up), but presence still publishes retained status and per-client commands still work; the sticky *"already online in another browser"* collision warning STILL shows. Source view: `presence-toasts="off"` on `<feezal-site>` when off, attribute absent when on. Turning presence itself off hides the sub-switch.
- [ ] **U46 (assistant easter egg)** — Editor Settings → Help: an off-by-default **"Assistant in the keyboard-shortcuts popup"** switch (persists in localStorage). With it on, the paperclip appears in **both** shortcut popups: **canvas mode** (`?` / Ctrl+I with the design canvas focused → "Keyboard Shortcuts" dialog) **and source mode** (toggle `<>` source view, then `?` / Ctrl+I → "Source Editor Shortcuts" dialog). In each, an original self-drawn paperclip + speech bubble appears **below** the shortcut table (never covering it), gently bobbing; with `prefers-reduced-motion` the bob is off. Off (default) → both popups are unchanged. Works in light and dark editor mode (the canvas-mode popup themes via piped `--feezal-clippy-*` vars). Purely cosmetic — no MS asset, bundled locally (A25).
- [ ] **U51 (per-view themes)** — view inspector: `theme` dropdown lists installed themes; pick one → the canvas view restyles instantly; deploy → viewer renders that view in its theme, other views in the site theme; switching views flips cleanly without flicker; a layout-app embedding a themed view shows the theme inside the shell; MQTT `<subscribe>/theme` with a name suppresses ALL per-view themes (user choice wins), payload `default` restores baked theme + view themes; a per-view **user** theme (CSS in dataDir/themes) loads in the viewer and is inlined in a static export.
- [ ] **U57 (richer theme swatch)** — every theme option in the picker (`<feezal-theme-select>`, both mounts — site theme sidebar AND the per-view inspector) previews **seven** roles at once, not three dots: a two-tone background **chip** (left `--primary-background-color`, right `--secondary-background-color`) with everything sitting **inside** it — three larger dots (`--primary-color`, `--primary-text-color`, `--secondary-text-color`), then a thin `--divider-color` rule, then a smaller `--accent-color` dot. Verify in light AND dark and across several themes: the chip shows the real surface contrast, the text/accent dots sit against those surfaces, and the accent dot actually distinguishes themes at a glance. The closed trigger shows the same compound swatch for the current theme; the built-in default and a not-yet-sampled theme fall back to neutral values (no empty slots). *(Seven-role sampling, the compound render, legacy-array coercion and placeholder fill are browser-tested: `test-browser/feezal-theme-select.test.js`.)*
- [ ] **U53 (styled view theme selector)** — the view inspector's `theme` control is the **same styled picker as the site theme selector** (`<feezal-theme-select>` — one component, two mounts): shortened names (`dark-mint`, not `feezal-theme-dark-mint`) + the U57 compound colour swatch per option; the themes sidebar looks unchanged (it's the donor — incl. user themes with ✏ and their × delete). Selection still round-trips: picking a theme sets the attribute (full class name), the leading "Site theme (default)" entry and the trigger's × clear both REMOVE it (B50 contract), undo works (one step via the custom-inspector pipeline). *(Component behaviour, both mounts' contracts and the B50 clear semantics are browser-tested: `test-browser/feezal-theme-select.test.js`.)*
- [ ] **B50 (per-view theme clear + embedded rendering)** — view inspector `theme` picker: the first entry is **"Site theme (default)"**; picking it (or the × clear) **removes** the `theme` attribute (not an empty string) and the canvas view returns to the site theme immediately; while no theme is set the picker displays "Site theme (default)". Viewer with layout-app: a drawer entry pointing at a view with its own `theme` renders that view **in its theme inside the shell** (the shell/other views stay in the site theme) — layout-app mirrors the theme's CSS rules into its shadow root (`#embedded-theme-css`); switching between a themed and an unthemed entry adds/empties that style element; works for bundled AND user themes (user-theme `<link>` loading after the embed re-syncs on load). *(Dropdown emptyOption threading + clear are unit-tested; the shadow-root CSS mirroring is browser-tested in `test-browser/feezal-elements-layout-app.test.js`.)*
- [ ] **E117 (publish-local, buttons)** — material/glass/carbon/paper button, material-icon-button, material-fab: enable `publish-local` → clicking reaches same-page subscribers (e.g. a dialog subscribed to the topic opens) but nothing appears on the broker (verify with an MQTT client); disabled → publishes to the broker as before; works with the connection down.
- [ ] **E117 (publish-local, navigation tranche — completes the rollout)** — `material-navbar` (view pick publish; note the `publish` attribute now actually works from saved markup — it was never a declared property before), `paper-tabs` (legacy publishLocal converged on the shared descriptor wording), `tui-menu` (entry pick), `metro-button` (tap publish), `material-chip` (tap): same recipe as the buttons row — enable `publish-local`, trigger, same-page subscribers receive, broker stays silent; off → broker as before. Sliders/checkboxes/selects and device cards deliberately do NOT carry the attribute (scope: navigation + dialog-opening only). `basic-navigation` publishes nothing — no attribute there. *(Option threading + shared descriptor are browser-tested for all four Lit elements: `test-browser/feezal-publish-local-navigation.test.js`.)*
- [ ] **E118 (click-through)** — basic-number, basic-icon-value, basic-datetime over a button: with `click-through` the tap reaches the button; without it the element swallows the tap; in the editor the element stays selectable/draggable either way.
- [ ] **E120 (hm cover Up/Down)** — delete a discovered Homematic cover, re-apply discovery → `publish-up`/`publish-down` are set to the `…/set/<channel>/LEVEL` topic with payloads `1`/`0`; Up fully opens, Down fully closes, Stop still works, position slider unchanged. Same via glass-cover and metro-cover.

### Feature batch B48 / U42 / U43 / U52 / E126

- [ ] **B48 (edit-mode selection)** — enter component edit mode, leave with Done, re-enter: click selects, Ctrl+click multi-selects, right-click shows the context menu, rubber-band works — identical to a normal view, in EVERY edit session (the bug hit from the second session on). *(e2e-covered: `test-e2e/component-edit-selection.test.js`.)*
- [ ] **U52 (edit-mode UX)** — double-click a component instance on the canvas → edit mode opens (same as the context-menu entry); banner → *Attribute mapping…* → the param table shows `${param}` rows pre-filled; rename/add/clear a mapping → Apply → pseudo-view shows the placeholders; **Done** persists params + placeholders and re-stamps instances, **Cancel** (before Done) discards the mapping too; undo after Done reverts everything in one step.
- [ ] **U42 (resize grip)** — select an element, hover it → a small corner triangle in the selection colour appears bottom-right; not shown on unselected hover, locked elements, component instances or flow-view tiles; resizing itself is unchanged.
- [ ] **U43 (apply connection)** — Site Settings → Connection: the *Apply connection settings* button is disabled/quiet on load; change host/port/credentials → it turns primary with a hint; click → deploys (same as Deploy), button goes quiet; the global Deploy also quiets it.
- [ ] **E134 (Circle design language / sensor state disc)** — drop a Circle **Sensor** card with a hazard/generic type (`water-leak`, `smoke`, `generic`, …) → it renders the **circle state disc** in the card's circle position (width-sized, top-anchored like the light ring): centred type icon + state text inside a ring, muted while clear, accent ring/fill while active, **error-coloured** while an alarm class is triggered; the disc scales with the card width (cqi units); the four motion types keep their dedicated SVG visuals. The Circle **Contact** card (E138 redesign) uses the SAME disc — open/tilted/closed shown via the type icon + state word, primary colour while open, tilt colour while tilted, muted while closed; verify it stays a true circle (never an ellipse) on a tall card. Battery badge top-left, unavailability top-right, label bottom (canon chrome placement). The canon itself is documented in `docs/element-spec.md` §5.2b — new device cards must start from it. Existing dashboards keep their saved sizes. *(Disc rendering + alarm colouring are browser-tested; the side-by-side geometry parity with light/climate is a visual check.)*
- [ ] **E132 (generalized boolean-sensor family)** — palette: the boolean cards said **"Sensor"**, the numeric cards **"Number"** *(superseded by the E138 renames — Motion/Sensor/Value split, see the E138 row and `docs/BREAKING-CHANGES.md`)*. The `type` select now covers motion/presence/radar/zone + `water-leak`/`smoke`/`gas`/`co`/`vibration`/`tamper`/`generic`, each with per-type default icons and state texts (empty text attrs = type default; a dropped water-leak card reads "Dry"/"Leak!" unconfigured); **alarm classes** (water-leak/smoke/gas/co/tamper) render their ACTIVE state in the error colour on all three cards (material hazard view, glass state colour, metro tile background). Discovery: a z2m `binary_sensor` with `device_class: moisture|smoke|gas|carbon_monoxide|vibration|tamper` lands with the right type (shared map, `_default` now `generic`); Homematic devices ride E131. **Battery (E124 subset):** all three cards carry `subscribe-battery-low`/`message-property-battery-low`/`payload-battery-low`/`battery-low-threshold`; a discovered z2m battery device auto-stamps them (boolean `battery_low` sibling preferred — incl. its bracket-form value_template — else the percentage sibling compared against the threshold); a low battery shows the shared **tiny low-battery symbol (battery outline + one short charge bar) bottom-right while the state keeps updating** (never a blackout). *(Type table/map/battery contract, alarm colouring, badges and the applier auto-stamp incl. the declaration guard are unit/browser-tested.)*
- [ ] **E131 (hm motion/sensor discovery)** — a Homematic motion detector (BidCoS `MOTION_DETECTOR` confirmed; HmIP `MOTIONDETECTOR_TRANSCEIVER` is best-knowledge naming — **verify against a real HmIP-SMI/SMO's `hm` metadata**, a wrong name just means the device doesn't appear) shows up as a discovered `binary_sensor` with `device_class: motion` and offers the occupancy/sensor cards of all three families; wiring: `MOTION` state topic (`payload.val`, payloads 1/0 with boolean coercion), `:0 UNREACH` availability, and — **only when the device was seen publishing `LOWBAT`/`LOW_BAT` on `:0`** — a dedicated `battery_low_normalized` record (never folded into availability). Water (`WATER_DETECTION_TRANSMITTER` → moisture) and BidCoS smoke (`SMOKE_DETECTOR` STATE → smoke) ride the same class table — **both channel types are best-knowledge, verify on real hardware**; HmIP smoke (SWSD alarm-status enum) is deliberately not wired yet. `ILLUMINATION` and other extras never confuse the match (datapoint whitelist). *(Recognizer promotion, both battery generation names, presence-check, hazard classes and the no-metadata/contact-STATE non-promotion are unit-tested.)*
- [ ] **E126 (hm switch discovery)** — Homematic switch channels named with a plug word (`steckdose`, `standby`, `plug`, `socket`, `outlet`, `schalter`) appear as discovered **switches** (STATE r/w, `true`/`false`); channels named with a light word (`licht`, `light`, `lampe`, `lamp`, `leuchte`, `beleuchtung`, `bulb`, `spot`) appear as discovered **lights** in on/off mode (no brightness ring — power button only); light wins when both match; unnamed channels (model+serial segments) and HmIP virtual-receiver triple followers do NOT appear; availability follows `:0 UNREACH`.

### A25 — zero third-party traffic / offline

- [ ] **Offline smoke** — open the editor and a viewer with the network tab filtered to *third-party* (or on a machine with no internet, broker on LAN): **zero** requests leave the origin; every icon renders as a glyph (not ligature text like "settings"); Roboto renders (not the system serif); Shoelace controls (select carets, clear ×) show their icons; a map element shows marker pins (tiles need the OSM exception or a local tile server).
- [ ] **CSP enforces** — paste `<link href="https://fonts.googleapis.com/css?family=Lobster">` into a view via the source editor → the browser console shows a CSP violation and nothing is fetched.
- [ ] **Static export offline** — export a site, unzip, open `index.html` from `file://` with the network disabled → fonts and icons render (fonts/ dir in the ZIP); a map's markers render.
- [ ] **Packages** — opening the Packages sidebar fires NO request to `registry.npmjs.org` (network tab); the *Check for updates* button fires exactly one and update badges appear; package search still works.
- [ ] **Dev boot** — starting the server does NOT contact `raw.githubusercontent.com`; with `FEEZAL_FETCH_ICON_CODEPOINTS=1` it refreshes `material-design-icons.js` once.
- [ ] *(CI)* `server/test/privacy.test.js` greps sources + build output for forbidden hosts and pins the vendored fonts.

### Release notes grouping (A22) — verify on the next tag

- [ ] Push the next release tag → the generated GitHub release body groups commits into **Features → Fixes → Docs → Chore** sections (Chore includes test/ci/refactor/build/style/perf; the `chore(release)` commit itself is absent); commits without a conventional prefix appear in a trailing **Other** section; empty sections are omitted; the Full-Changelog compare link and the Docker update instructions are unchanged.

### Mobile app project (A9 Tier 2a) — real build on a dev machine
The export dialog, scaffold/README content and ZIP download are automated —
the actual toolchain run is not:

- [ ] Unzip an exported project → `npm install` → `npm run android` → Android Studio opens; Run installs on a USB-connected phone; dashboard connects to the broker.
- [ ] (Mac) `npm run ios` → Xcode signing with a free Apple ID → runs on an iPhone.
- [ ] `npm run assets` generates native icons/splash from `resources/icon.png`.

### Server-side APK build (A9 Tier 2b) — real Docker build
Capability gating, the job routes/SSE and the container pipeline are automated
(mocked engine + a daemon-gated integration test); the pipeline has produced a
verified APK once — re-check after changing the build image or Capacitor pin:

- [ ] With `FEEZAL_DOCKER_BUILDS=1` on an x86_64 host: "Build APK on server" appears in the export dialog; the log streams live; Download yields an `app-debug.apk` that installs via `adb install` and shows the dashboard with the site icon.
- [ ] Cancel mid-build → container gone (`docker ps`), dialog shows "cancelled", a new build can start.
- [ ] Without the env flag (or on arm64): the button is absent, `POST …/build-apk` → 403. *(automated too — quick sanity click)*

### Server restart / self-update (A13) — needs a containerized feezal
Capability logic, watchtower container spec and route gating are automated —
the live path needs a real feezal container with the socket mounted:

- [ ] Run feezal as a container with `/var/run/docker.sock` mounted and `FEEZAL_DOCKER_SELFUPDATE=1` → Editor Settings shows the Server section; **Restart** brings the editor back automatically (auto-reload).
- [ ] **Update…** pulls the image via a one-shot watchtower, recreates the container with identical config, editor comes back on the new container.
- [ ] Bare metal with `FEEZAL_ALLOW_RESTART=1` under a supervisor (systemd/pm2): Restart exits the process and the supervisor brings it back.

### Per-site CSP (A28)
- [ ] Site Settings → **Security**: three content rows (Images & cameras, Embedded pages, Network connections) + an **Advanced** disclosure (Scripts/Styles/Fonts with the loosening warning). Each row: 3-state select (Open / feezal only / Selected hosts) + origin chips with an add-input; invalid origins (paths, spaces) are rejected inline; the broker origin shows as a locked 🔒 chip on Network connections (direct ws/wss only). Defaults (untouched tab) keep today's behaviour — the deployed viewer header equals the A25 baseline + `report-uri`.
- [ ] Tighten *Images & cameras* to Selected hosts without the camera's host → deploy → the viewer blocks the image, the browser console shows the violation, and **Security → Recent violations** (Refresh) lists `blocked: <host> (img-src)` with an **Allow** button; click Allow → deploy → the image loads. "Found in your dashboard" chips offer hosts already used in element attributes.
- [ ] Header invariants (check response headers on `/viewer/<site>`): `script-src` always keeps `'unsafe-inline' 'unsafe-eval'`, `connect-src` always includes `'self' data:` + the broker origin, `font-src` keeps `data:`, `img/media-src` keep `data: blob:` — regardless of tightening. The **editor** header is unchanged (fixed A25 baseline, no report-uri).
*(Builder modes/invariants/broker auto-include/origin validation, the report ring buffer incl. cap + per-site isolation, and both endpoints are unit-tested: `server/test/csp.test.js`.)*

### Hidden-view subscription pausing (N37)
- [ ] Site Settings → Viewer → **Bandwidth**: enable *Pause hidden views' subscriptions* (grace default 30 s) → deploy. In the **viewer** with an MQTT client watching `$SYS`/broker logs (or the feezal server log): after the grace period, topics used only by hidden views are unsubscribed; switching to such a view **re-subscribes immediately** and retained values repaint instantly (cache replay — no blank flash); switching away and back **within** the grace period never churns subscriptions.
- [ ] Per-view override: a view with `pause-subscriptions="never"` keeps receiving while hidden (non-retained data survives; a layout-app/dialog clone of it stamped later starts with the last-seen retained-origin values — warm cache); `always` pauses the view even when the site default is off.
- [ ] The **editor** is unaffected — all views stay subscribed there regardless of the settings.
- [ ] Availability badges (N31) pause/resume together with the element's data subscriptions.

**Lazy view subscriptions (N40)** — a separate site setting with the same shape as Pause:
- [ ] Site Settings → Viewer → **Bandwidth**: enable *Lazy view subscriptions* → deploy. In the **viewer** (MQTT client / server log): a view subscribes its topics **only when first shown** — an initially-hidden view holds **no** subscriptions at load, and a view you never open never subscribes. Revealing it subscribes (retained values repaint from the cache); the **active** view at load subscribes eagerly.
- [ ] **Composes with Pause:** with both on, a view is fully on-demand — nothing until first shown (lazy), dropped again after the grace on a later hide (pause). With **only Lazy** on, a view stays subscribed (warm) once first shown; with **only Pause** on, it is the plain N37 behaviour.
- [ ] Per-view override: `lazy-subscriptions="never"` keeps a view eagerly subscribed from load even when the site default is on (non-retained data); `always` defers even when the site default is off. `inherit` follows the site setting.
- [ ] The **editor** is unaffected — all views stay subscribed there.
- [ ] **Layout-app sub-views stay warm / honour the grace:** on a `layout-app` dashboard, open drawer entry A (its topics subscribe), then switch to B and back to A → **A's subscriptions are NOT dropped immediately** when you leave it (the viewer/broker shows **no unsubscribe on a drawer switch**). The layout-app keeps each **visited** sub-view's clone mounted (hidden) so it stays warm; a sub-view you never open is never cloned/subscribed (natural lazy). With **pause-hidden OFF** it stays warm indefinitely. With **pause-hidden ON**, a sub-view you leave is torn down (topics released) **only after the pause grace period** (`pause-grace-seconds`, default 30s) — returning within the grace reuses the same warm clone with no re-subscribe; a per-view `pause-subscriptions="never"/"always"` overrides the site default. *(Keep-alive + grace-teardown is browser-tested in `test-browser/feezal-elements-layout-app.test.js`.)*
*(Controller + element pause/resume machinery, grace, `never`/`always`, the stamped-into-paused precondition, and the N40 lazy immediate-pause / reveal / compose-with-pause cases are browser-tested: `test-browser/feezal-visibility.test.js`.)*

### PWA (A9 Tier 1) — real-device installs
The toggle, manifest/service-worker routes, icon pipeline (crop, maskable,
uploads via both entry points) and export bundle are automated — what's left
is the part only real hardware can prove:

- [ ] Android/Chrome: open the viewer URL → "Add to Home Screen" appears; installed app launches standalone with the custom icon (maskable shape looks right on the launcher).
- [ ] iOS/Safari: share-menu install; icon + standalone launch.
- [ ] Offline: installed app opens with cached shell after airplane mode; live values return when the network does.
- [ ] **B52 (updates reach clients without a hard refresh):** update feezal — or fake it: rebuild `www/dist` (`touch`ing `dist/viewer-bundle.js` changes the SW build id too) and restart the server — with an open browser viewer tab AND an installed iOS/Android PWA. A **plain reload** recovers both: no `assets/feezal-elements-<hash>.js` 404, no bricked PWA (the sw.js bytes change via the versioned cache name `feezal-pwa-<site>-v<version>-<distId>` → SW update cycle → activate purges the old cache). Check response headers: hashed `/assets/*-<hash>.*` → `Cache-Control: public, max-age=31536000, immutable`; stable names (`/viewer-bundle.js`, `/editor/`, the generated viewer HTML, `/editor/feezal-elements.js`, `manifest.webmanifest`, `sw.js`, `/themes/*`, `/assets/<site>/*`) → `no-cache` (revalidation is a 304). In the SW: stable names are network-first (cache = offline fallback), hashed assets cache-first. A re-export after a site change yields a changed `sw.js` (content-keyed cache name) so hosted exports update installed PWAs on plain reload too. *(SW strategy markers, distBuildId fingerprinting, versioned cache name + no-cache on sw.js/manifest are unit-tested in `server/test/pwa.test.js`.)*

### Elements (E44–E47)
- [ ] **Responsive Layout** (`layout-responsive`): add breakpoint rules (min/max width, orientation) each → a view; in the viewer, resizing the window **swaps** the mounted view at the thresholds; only the active view's elements are subscribed (no double subscriptions); portrait/landscape rule wins when ordered first.
- [ ] **App Layout** (`layout-app`): top bar + drawer render; picking a drawer entry **swaps the embedded content view**; shrinking the element past `breakpoint` collapses the drawer to an **overlay + hamburger** (+ scrim closes it); title (static and `subscribe-title`); action buttons publish; **hide-header** removes the top bar and (in overlay mode) shows a floating hamburger that still opens the drawer; entry/action **icon fields autocomplete** with the tile picker (same UX as the generic icon control, incl. set chips); **entry-style** (inspector → Drawer → Entry style): default `pill` = rounded chips with side inset, `list` = flat rows without radius where hover/active highlight the **full drawer width** edge-to-edge — switch live and check both hover and the active entry in both modes. *(The N6 inspector add/reorder/remove + create-view, the icon-input component and its items wiring are automated.)*
- [ ] **App Layout — content inset (U50)**: the Style inspector exposes **`--feezal-app-content-padding`** (default `0`). Set it to `16px` → the embedded view gets breathing room on all four sides, away from the app bar and the drawer; the view's **own background still paints under the inset** (no unpainted gutter between drawer and view — that is why this is padding, not margin). Accepts a **full CSS shorthand**, so `8px 16px` and `0 16px 24px` give per-side insets. **The thing to actually check:** with an inset set, the content area must **not** gain permanent scrollbars, and the embedded view must **shrink** to fit rather than being pushed out of view — resize the element and switch drawer entries with a padding applied. Clearing the knob returns to flush-against-the-chrome. *(The knob descriptor plus the real layout — no overflow, and the embedded area narrowing by exactly the inset — are browser-tested in `test-browser/feezal-elements-layout-app.test.js`.)*
- [ ] **App Layout — header mode + active-page label (N39)**: the inspector's **Header** select offers `always` / `small-only` / `never`. `always` = today (bar shown at every width). `small-only` → resize past `breakpoint`: **wide hides the bar entirely**, **narrow shows it** with the hamburger; wide still has the persistent drawer so nav stays reachable. `never` = the old `hide-header` (no bar; floating hamburger in overlay mode) — and an existing dashboard with **`hide-header` set still behaves as `never`** (deprecated alias). **Active-page label** (`show-active-label`, default on): the current drawer entry's label shows in the bar **right of the title**, or **right of the hamburger when the title is empty**; navigating swaps it live; an entry with no label falls back to its view name; turn `show-active-label` off to hide it. *(Header modes, the alias, and the active label incl. the view-name fallback are browser-tested in `test-browser/feezal-elements-layout-app.test.js`.)*
- [ ] **App Layout — rail model (B84)**: the inspector's **Rail** select (Drawer section) offers `off` / `slim` / `edge` / `auto`. `off` = full drawer (today). `slim` = icon rail above the breakpoint; `edge` = thin edge. `auto` shows a **Rail breakpoint** field — between the overlay breakpoint and it the rail is slim, at/above it the drawer is full (resize across `1024` to see it flip). The deprecated **`slim` / `autohide`** attributes still work (old dashboards render a slim / edge rail) and picking a Rail value clears them so they can't conflict. Narrow (< breakpoint) is still the overlay + hamburger regardless of rail.
- [ ] **App Layout — rail expansion overlays the content (U64)**: with a **slim** (or edge) rail, **Rail expand** = `overlay` (default) → hovering the rail (or entering it by keyboard) expands it to labels **drawn OVER the content — the embedded view does not shift**. `push` = the old behaviour (content reflows aside). `never` = the rail never expands. A **mouse click / tap must NOT pin the rail open** (only hover + keyboard focus expand it — `:has(:focus-visible)`). **Rail menu button** (touch): turn it on → a hamburger appears at the **top of the rail**; tap → the full drawer opens as an **overlay** (scrim, Esc, close-on-select), and it is present even with **Header = never** (no app bar to host a button). *(Rail-state derivation across the three zones + deprecated aliases, the overlay-vs-push layout (drawer position + content margin), and the rail-menu overlay incl. header=never are browser-tested in `test-browser/feezal-elements-layout-app.test.js`.)*
- [ ] **App Layout — slim-rail expanded drawer is opaque + icons don't jump**: on a theme whose `--divider-color` (the default drawer colour) is **semi-transparent**, use a **persistent** drawer with a **slim** rail and hover to expand it → the whole expanded panel is **opaque** (the embedded view no longer shows through the part that overhangs the content — previously only the icon-rail portion had a background). On expand, the entry **icon stays put** (it used to jump ~6 px to the right because the hidden label's flex gap was still counted) and there is **no vertical shift**; drawer nav icons are a consistent **24 px**. *(Opaque-backing composite + stable icon x/y on expand + 24 px glyph are browser-tested in `test-browser/layout-app-rail-drawer.test.js`.)*
- [ ] **App Layout — one drawer geometry in every mode (B90)**: the entry rows must sit at the **same x in every drawer mode**, so nothing moves when the drawer changes shape. Walk all six with a **slim rail** configured: full drawer (wide / `rail: off`), `rail: auto` above **and** below the rail breakpoint, the **narrow hamburger overlay**, the **rail menu button** overlay, and the rail **hover-expanded** — the icon and its label must not shift horizontally or vertically at any transition, and the row highlight must keep the same side gutter throughout. Repeat with **Entry style = list**: there the rows are **flush to the drawer edge in every mode** (before, the expanded rail alone drew an 8 px gutter and the icon walked 16 → 19.5 → 24 px). **Touch check that needs a real tap, not a mouse:** with `rail-menu-button` on, **tap** the rail hamburger — the overlay must show **labels**, left-aligned like the full drawer (it used to keep the collapsed look with the icons centred mid-panel whenever the pointer was not over the drawer, which on touch is always). The slim rail is **56 px** wide in `list` and **64 px** in `pill` — that is deliberate: the rail width follows the entry inset so the resting icon is aligned with the expanded state *and* centred in the rail. New knob **`--feezal-app-drawer-entry-inset`** (Styles): set it to `0` on a `pill` dashboard → the highlight reaches the drawer edge in **all** modes, still with no jump; clear it to get the default back. *(Positional guard — icon x/y measured in every mode for both entry styles, plus the inset knob and the touch overlay — in `test-browser/layout-app-drawer-geometry.test.js`. Asserting stylesheet text would have missed all three causes.)*
- [ ] **Custom-inspector attribute coverage (B88)**: the `layout-app` inspector now exposes **every** declared attribute — spot-check that Header, Show-active-label, Initial view, Rail, Rail breakpoint (when auto), Rail expand and Rail menu button all have controls, and `material-navbar` gained a **Show item icons** switch. A guard test (`test/feezal-inspector-attribute-coverage.test.js`) fails CI if a future attribute on a custom-inspector element (layout-app / navbar / device-health) has no control — with a per-element allow-list for deliberately-hidden ones (e.g. the deprecated element-level `subscribe`/`publish`). *(Static coverage scan is automated; the manual check is that the controls actually work.)*

- [ ] **App Layout — improvements (N36)**: **drawer-mode live switch** — toggle **drawer-persistent** (or change **breakpoint**) in the inspector on a wide element → the overlay + **hamburger** appear/disappear immediately, no resize or hard reload needed (previously stuck until refresh); the floating fab hamburger shows in hide-header overlay mode. **Themable chrome** — the Style inspector now exposes `--feezal-app-bar-bg/-color`, `--feezal-app-drawer-bg/-color`, separate **drawer icon** and **label** colours, `--feezal-app-active-indicator/-color` and drawer width; set them and check bar, drawer, entry icon vs label, and the active entry recolour, on a light AND dark theme. **Slim rail** — the inspector's **Drawer** section has **Slim rail** and **Autohide** switches: with *Slim rail* on, the persistent drawer is an icon-only rail that expands to icon+label on hover or when keyboard/**D-pad** focus enters it; *Autohide* collapses it to a thin edge until hover/focus. Both toggle live. **Keyboard / smart-TV** — Tab into the drawer, Arrow keys move between entries (wrap), Home/End jump, Enter/Space activate, Esc closes an overlay; focus ring visible. **Embedded background** — give a drawer-entry view its own background (Solid/Image/Gradient) → it now fills the shell content area when embedded (was uniform before). **Theming** — the chrome now defaults to the canonical feezal theme vars (no MD3 tokens): drawer bg = `--divider-color`, drawer label = `--primary-text-color`, bar = `--primary-color`; check it tracks light/dark + custom themes. **Initial wide drawer** — load a dashboard with a persistent App Layout in a **wide** viewer → the drawer is visible immediately, without needing to resize the window (initial ResizeObserver race fixed). **Narrow overlay bg + transparency** — shrink below the breakpoint so the drawer is an overlay; its colour is `--feezal-app-drawer-overlay-bg` and its **transparency** is the plain-number `--feezal-app-drawer-overlay-opacity` (Style inspector) — 100 = opaque, 60 = 60 % opaque / 40 % see-through, 0 = fully transparent; text/icons stay opaque. (color-mix; on a browser without it the overlay stays opaque.) *(Reactivity/burger, the chrome style descriptors incl. overlay-bg + canonical-var defaults, slim/autohide reflection, arrow-key nav and the embedded-background copy are browser-tested; slim-rail visuals, the wide-init fix and TV feel want a manual pass.)*
- [ ] **App Layout — site view contract (N30)** (viewer): put a `layout-app` with 2+ drawer entries on a site view (e.g. `main`) with the site **publish** topic set. Picking a drawer entry now (a) updates the **URL hash** to `#/main/<entryView>` and (b) publishes `main/<entryView>` on `<publish>/view` (not just the deprecated element-level topic). **Deep-link:** opening `…/#/main/page2` restores the shell on `main` with `page2` already active (reload keeps it). **Inbound:** an MQTT `<subscribe>/view` = `page2` (bare) swaps the drawer entry *inside* the shell without leaving `main`; `main/page2` (nested) switches to `main` and activates `page2`; a name that is **not** a drawer entry still switches the top-level site view as before. A plain site with no layout-app is unchanged (bare `#/view`, bare published name). *(hash parsing, nested/ bare inbound routing, deep-link-once-registered and the no-router-unchanged path are unit-tested.)*
- [ ] **Navbar** (`material-navbar`): tapping navigates; **active item follows** the current view from tab bar / swipe / MQTT; badges update from MQTT; orientation/labels/align render right; empty → auto-fills all views; **item-width (E81)**: a CSS length gives every destination that fixed size, `equal` shares the bar evenly, empty keeps content sizing (vertical orientation: applies to the height). *(The N6 inspector item editing + item-width sizing are automated.)*
- [ ] **PIN Lock** (`system-pin`): navigating away and back re-locks (unless remembered). *(Placeholder-in-editor, keypad overlay incl. covering high-z-index siblings, wrong/correct PIN, clear, and `remember` are automated.)*

### AI assistant — agent mode, needs a real provider
Non-agent chat (streaming, proposal card, Accept, model list) is automated
against a stub provider — this block is about **agent/tool mode + real providers**.

- [ ] Configure a real provider (OpenAI-compatible, Anthropic, Ollama — for Ollama set **`num_ctx` ≥ 8192`**); connection test + model list work for each type.
- [ ] Ask a change with **agent mode ON** → tool activity streams (searching topics / reading payload / searching devices), then a confirmation card.
- [ ] Ask "add switches for all `<room>` lights" → it resolves via discovery/topics, wires `subscribe`/`publish` (`…/set`), correct `payload-on/off`, and a real element tag; **Accept** applies it.
- [ ] Ask for a **new view** ("make a new page for …") → "Create new view" card → applies + navigates.
- [ ] Assistant messages render as **Markdown** (code blocks, lists, links open in a new tab).
- [ ] Empty/failed agent turn shows a clear error (no phantom "(proposed a change)").

### Package Manager — real npm, needs network
The whole sidebar UI (search, install, update badge, remove, rejection errors,
reload prompt) is automated against stubbed endpoints — what's left is the real thing:

- [ ] **Type filter tab bar**: All · Elements · Themes · Icons · Sets render as a tab bar (same styling/alignment as the Inspector and Site Settings tab bars); switching tabs filters the Installed list and clears stale search results.
- [ ] Install a real published `feezal-element-*` from npm; after reload the element is in the palette; **Update** and **Remove** round-trip.
- [ ] **Update installs the badge's version, even right after a publish**: with an older version installed and the `→ x.y.z` badge showing, click **Update** → after the reload the Installed row shows exactly the badge's version (the client sends that version explicitly and the server installs with `--prefer-online`, so npm's 5-minute packument cache can't reinstall the old one). The server log shows `install: npm install <pkg>@<version>` and `install: resolved <pkg>@<version>`.
- [ ] **Installed packages in viewer + export (N27)**: with an installed element placed on the canvas and deployed → the **live viewer** renders it working (the page source shows a `<script type="module" src="/user-elements/…">` tag per used package; unused installed packages are not injected). A **static export** of the same site renders it from `file://` too (the bundle is inlined into index.html) and the export dialog's size report shows an `(installed)` bucket. Same for an installed **theme** set as the active theme. *(Selection, injection and inlining are unit-tested; this is the real-browser check.)*
- [ ] **Element sets (N29 Phase A)**: search for `eink` (or filter **Sets**) → `@feezal/feezal-elements-eink` appears with the `set` chip; **Install** expands it — after reload all member `feezal-element-eink-*` elements are in the palette; the Installed list shows the set row with its members indented beneath; **Remove** on the set removes the marker AND its members (a member also installed individually or owned by another set survives); **Remove** on a single member leaves the set and siblings; **Update** on the set reinstalls all members at latest. *(Type/keyword plumbing, bundle expansion write layout, set-aware remove and the grouping UI are unit-tested; this is the real-registry check.)*
- [ ] **Multi-element families (N29 Phase B)**: install a published `feezal-elements-*` package carrying `feezal: {type: "elements", elements: [...]}` (one bundle, many tags — none published yet; when the first family ships, use it) → ONE row in the Installed list (Sets filter, `set` chip, no indented members); after reload **every** tag from its manifest appears in the palette and drops onto the canvas; source-mode tag completion offers the family's tags; **Remove** deletes the whole family in one step; a static export of a site using two of its elements includes the family bundle exactly once (both elements render). *(Discovery/registry/tag-map/install-path are unit-tested; this is the real-registry + export check.)*

---

## 3. Editor — core

(Selection — rubber-band, Ctrl+click toggle, view-select on empty canvas —
copy/paste offset, Ctrl+D, Delete, context-menu lock/unlock incl. drag-blocking,
single-element move, arrow nudge, single undo, source-mode error gating + apply,
and palette drag-to-canvas are all automated.)

- [ ] **Top bar — standalone View button + MQTT dot:** to the **left of the Deploy split-button** there is now a standalone **View** button (same grey/blue styling as Deploy) that opens the live viewer in a new tab — the **View entry is gone from the Deploy ▾ menu** (which now holds Save / Export / Mobile app…). **U93: the MQTT dot is no longer in the top bar** — it is a small badge on the **Site Settings** icon in the right sidebar tab bar: **green** when the broker bridge is connected, **red** when configured-but-not-connected, **yellow (pulsing)** while a deploy applies the connection, **grey** when no broker is configured. The state reads in that tab's **tooltip** (`Site Settings — MQTT: …`), and clicking it opens Site Settings → Connection. *(The state mapping is unit-tested in `test-browser/feezal-app-editor-topbar.test.js`.)*
- [ ] **Top bar layout (U93)**: left to right the centre cluster reads **palette toggle · site switcher · copy/paste/cut/delete/undo/redo · Generate · Source · Deploy ▾ · View**. **Generate** and **Source** show their icon AND a text label (they stay flat toolbar buttons, not solid pills like Deploy/View). Source still highlights while active and still shows the syntax-error badge beside it; Generate still hides in source mode. Switching sites from the (now left-hand) site switcher works as before.
- [ ] **Inactive viewers drop out of the Clients list (B108)**: open a viewer, check it appears under **Site Settings → Clients**, then **kill it ungracefully** (close the tab / pull the network — not a clean reload). Within ~3 minutes it is **hidden** and the panel shows `1 inactive viewer … hidden · Show · Forget it`. **Show** reveals it greyed out with a grey dot; **Forget** clears its retained status on the broker, so it does not come back after an editor reload (check with an MQTT client that `<publish>/clients/<id>/status` is gone). A viewer left **running and untouched** must stay listed indefinitely — it heartbeats every 60s even when nobody is looking at it, so parking on one view for hours must NOT make it disappear. Also verify a viewer with a **wrong system clock** stays listed: staleness is measured from when the editor received the update, not from any timestamp the viewer sent.
- [ ] **Cross-view selection clears the old one (B109)**: select an element on **view 1**, then in the **Layers** tree click an element on **view 2** → the editor switches view and selects it, and the view-1 element is **no longer marked** in the tree (exactly one selected row site-wide). Same when you then click empty canvas on view 2: nothing anywhere stays selected.
- [ ] **Drawer scrollbar (U94)**: give a `layout-app` enough drawer entries to overflow (or shrink the element) → the drawer scrolls with a **thin** bar whose thumb has **rounded ends** and is **clearly visible against the drawer background** — check on a dark theme such as midnight-blue, which is where the thumb used to disappear, and on a light theme. The **content area** scrolls the same way. The thumb colour is adjustable in the Style inspector as **`--feezal-app-scrollbar-color`** (defaults to the theme's secondary text colour). *Worth checking on Chrome/Windows specifically — that is where the heavy native bar was reported, and headless test browsers use overlay scrollbars.*
- [ ] **Rubber-band survives view switches (B35)**: switch between views **at least 3×** (include an empty view and one **created mid-session**) — rubber-band select keeps working on *every* view after *every* switch (rectangle draws AND elements actually get selected), the console stays free of `NotFoundError … removeChild`, and elements on a freshly created view are selectable/draggable immediately. While at it, watch B32/B33/B36 (stuck snap lines, unselectable elements, dead snapping): they were plausibly downstream of the same aborted view-switch handler. *(Idempotent stop, selectable re-registration on revisit, mid-session view creation and overlay disposal are browser-tested with the real DragSelect.)*
- [ ] **Snap lines clear after palette drops (B32)**: drag a new element from the palette and drop it so it snap-aligns with an existing element (guide lines visible during the drag) → on release, ALL guide lines disappear (the final snapped placement used to redraw one v + one h line after the hide). Also check the cancel gesture (drag back over the palette). *(The snap-aligned drop is e2e-tested.)*
- [ ] **Resize** via handles; **snapping** to other elements + grid (both axes); snap guide lines show/hide correctly.
- [ ] **Grid:** toggle visibility; change size + colour (both axes use the colour); grid aligns to the view origin, not the toolbar.
- [ ] **Undo / redo** breadth: multi-step sequences, redo (Ctrl+Y), across add/delete/source-apply.
- [ ] **Undo keeps the selection:** select an element, change an attribute/style/position, Ctrl+Z → the same element stays selected and the inspector shows the restored values; repeated Ctrl+Z keeps tracking it. Undoing the element's own creation (or a structural change that removed it) falls back to the view selection — never silently selects a different element type. *(Capture/re-match semantics are unit-tested.)*
- [ ] **Cut** (Ctrl+X) and paste; group IDs remapped (if grouping used).
- [ ] **Clipboard keeps light-DOM children (B31)**: copy/paste, cut+paste and duplicate (Ctrl+D) a `basic-template` with template content → the pasted element renders the same template (not empty); same for a dialog with a template body and a layout element with children. *(Deep-clone behaviour is unit-tested.)*
- [ ] **Context menu:** copy-to-view and move-to-view submenus. *(Lock/unlock is automated.)*
- [ ] **Context menu never renders off-screen:** right-click **near the bottom (or right) edge** of the window with a tall menu (an element selected, so the menu has all its entries) → the **top-level menu is shifted up/left** to stay fully on-screen (it never spills past the bottom). Then, with **many** views (well past a screenful — the count is unlimited), hover **Copy to view…** / **Move to view…** (and **Switch family…**): the submenu also opens **fully on-screen** — it **caps its height to the viewport and scrolls internally** instead of growing past the bottom, **flips to the left** of the parent item when it would run off the **right** edge, and **shifts up** when the item sits near the **bottom**. Every view in the list is reachable by scrolling. *(Both the top-level clamp and the submenu positioning are browser-tested in `test-browser/feezal-inspector-ctx-submenu.test.js`.)*
- [ ] **Align & distribute (U83)** — select **2+ elements** on an absolutely-positioned view → right-click → **Align & distribute…**. Align left/centre-h/right and top/centre-v/bottom move the selection to its own bounding box (the outermost element stays put); with **3+** selected the submenu also offers **Distribute horizontally/vertically** (equal gaps, the two outermost anchored); **Match width / height / size** resize every element to the **first selected** one. Each operation is **one Ctrl+Z**. Geometry comes from the RENDERED box, so an element sized by a class or size preset lines up too. **Locked** elements are skipped and the toast says how many; an operation that would change nothing says "Already aligned" instead of dirtying the site. The submenu is **absent** for a single element and on a **flow** view (the container lays those out). *(The geometry engine is unit-tested in `test/feezal-canvas-align.test.js`; the menu → real elements → single-undo path in `test-e2e/align-layers.test.js`.)*
- [ ] **Layers panel (U87)** — its own **sidebar tab** (the layers icon, right after the Inspector): a tree of the WHOLE site — every view as a collapsible node with its element count, **only the currently open view expanded** (so a 20-view site stays readable). Expansion NEVER collapses by itself: switching views opens the new one and leaves the previous one open, and selecting an element opens the view holding it — only a click on a header collapses. The open set is **persisted**, so the tree looks the same after a reload (check with several views open); elements of views the editor has not initialised this session are listed too. Elements are listed **in DOM order** — the same order the canvas reads in, deliberately NOT the usual layers-panel top-most-first, since flow/grid views lay their children out in DOM order and reversing ran the tree backwards against what you are looking at — with palette icon, label (label → name → tag), topic hint and a lock toggle. **Click** selects on the canvas and scrolls it into view **without swapping the sidebar away from Layers** (nor does selecting on the CANVAS: selection never moves the sidebar) — clicking an element in ANOTHER view **switches to that view first**; **Ctrl/Cmd+click** toggles, **Shift+click** takes the range. The canvas selection is mirrored back into the list (it reads the canvas's own selected-class, so it can never drift). **Drag a row** onto another to restack — within one view only, never across views — one undo step; dragging **down** drops the element after the row it lands on, dragging **up** drops it before, so it always takes the target row's position. The **lock** button locks/unlocks without selecting the row. **Fuzzy filter** (the search box): matches element type, label, subscribe and publish topics — "temp" surfaces every temperature card **across all views** (views with hits auto-expand; views without are hidden), "ktemp" still finds "Kitchen temperature" (subsequence), a topic fragment like `home/kitchen` works, and a miss says so. Clearing the box restores the default expansion. The tree tracks the DOM live: drop an element from the palette, delete one, paste, add a whole view, or run Generate → the tree updates without leaving the panel — **and the tree is populated the first time you open the tab**, without needing a keystroke in the filter (the panel mounts before the site markup arrives over the socket, so it waits for it). **Dark mode**: the panel, its sticky view headers and the context menu all follow the editor's dark tokens. **Icon column**: every row's label starts at the same x — an element whose package declares no palette icon shows a placeholder glyph rather than collapsing the column; hovering the icon (or the label) shows the element TYPE, e.g. `glass-contact`. **Right-click** an element row → the canvas actions (Cut/Copy/Duplicate, the four stacking commands, **Move to view… / Copy to view… as hover flyouts** listing every other view, Lock, Delete); a menu opened near the bottom/right edge is clamped fully on-screen, and the flyout flips to the left when it would overflow — right-clicking an unselected row selects it first, and every action is delegated to the inspector, so there is one implementation shared with the canvas menu. **Right-click a view header** → Open / Rename… / Duplicate / Delete view. **Drag an element onto another view's header** → it MOVES there (the header highlights as a drop target); hold **Ctrl** while dragging → it is COPIED instead and the cursor shows the platform's copy affordance (a +). A toast confirms which happened. Dropping onto the element's own view does nothing; dragging onto a sibling ROW still restacks within the view. *(Browser-tested in `test-browser/feezal-sidebar-layers.test.js`, the matcher in `test/feezal-fuzzy.test.js`, the real sidebar tab in `test-e2e/align-layers.test.js`.)*
- [ ] **Element MQTT panel (U88)** — select a **single** element → inspector → **MQTT** tab (absent for a multi-selection or the view). **Subscribes** lists the topics the element ACTUALLY wired (read from its live subscription registry, not from the attributes — so a topic the element failed to wire is visibly missing), each with the last payload, a message count and a timestamp; before the first message it says "waiting for a message…". The panel opens its **own** subscriptions, so it shows live traffic **even with "Prevent MQTT element manipulation" ON** — the exact situation where a card looks dead and you need to know whether data is arriving. **Publishes** lists the element's publish targets with a **test-payload field** (type + Enter or click Publish → goes to the broker, and appears in the tail marked ↑). **Live tail** shows the last 20 messages, ↓ received / ↑ published, newest first. Switching selection clears the tail and re-wires to the new element. Binary/data-URL payloads collapse to `data:…(binary)` instead of flooding the panel. *(Browser-tested in `test-browser/feezal-sidebar-debug.test.js`.)*
- [ ] **Switch family (E115)** — right-click an element → **Switch family…** submenu lists only **installed** families that have a twin for the element's *function* (e.g. a `glass-light` offers Metro/Circle/E-ink…; its own family is never listed; a `basic-image` with no twins shows no submenu). Pick a family → the element becomes that family's twin **in place** (same spot in the stack/grouping) and stays selected; **shared config carries** (topics, payloads, limits, label, `discovery-id`) and **position is kept** — but **width/height reset to the target family's default size** (families differ in proportions; the old size is deliberately not remembered when switching back), while the source family's look/`--feezal-*` overrides reset to the target's defaults. **Numeric readout** pairs across the tag divergence: a `glass-value` offers **E-ink** (→ `eink-value`) and vice-versa. **Multi-select**: select a mix, Switch family → each switches independently; elements with no twin in that family are **skipped** and the count is reported; if any attribute was **dropped** (no counterpart in the target), a transient report names exactly which (never silently lossy). The whole switch is **one Ctrl+Z**. Views and component instances are never offered the action. *(Target discovery, the intersection/`discovery-id`/position carry + size reset, orphan drop + skip reporting, and the numeric-readout family pairing is browser-tested in `test-browser/feezal-switch-family.test.js`.)*
- [ ] Locked state **persists after deploy** (amber outline back after reload).
- [ ] **Oversized/scrolled canvas drags (B8)** — fix shipped 07/2026 after multiple reverted attempts; test thoroughly: **auto-sized view** (100%×100%) with elements below/right of the fold → scroll the canvas, drag an element to the very bottom/right of the *existing* canvas → it lands under the pointer (previously it stopped short by exactly the scroll offset); dragging past the farthest element's edge **stops there — a drag never grows the canvas** (the boundary is snapshotted at drag start; dragging the farthest element itself can move it inward/along the edge but not outward). **Fixed-px view** → elements still clamp exactly at the view edge and the far edge is reachable; no spurious scrollbar at the bottom edge of a not-yet-overflowing canvas. **Snap guide lines**: on a horizontally scrolled canvas, vertical guides appear exactly on the aligned element edges (previously offset by the scroll amount); check with a view margin set, too. Also re-check plain drags, multi-select drags, resize snapping, arrow nudge, and undo after drag — this code path broke in past attempts. *(Restriction geometry incl. extent-snapshot semantics, scrolled snap-line placement, canvas-never-grows, and the old-clamp regression are unit/browser/e2e-tested.)*
- [ ] **Keyboard shortcuts** modal (`?`) lists correct bindings; each works.
- [ ] **Source mode:** format document; editing comfort (highlighting, completion). **U54:** with a non-first view active, entering source mode scrolls to that view's `<feezal-view>` line (near the top of the viewport) with the cursor placed on it — regardless of attribute order in the tag; the first view / a not-found view (fresh unsaved) falls back to the document top silently. **B96:** a **big** site (serialized HTML well over 100 kb) opens source mode **properly indented** (not one giant line) and the server log shows **no** `PayloadTooLargeError` — the `/api/format` POST is accepted (the JSON body-parser limit is 25 mb). *(Reveal targeting incl. attribute order and exact-name matching is unit-tested; the large-body accept in `server/test/api-assets.test.js`.)*
- [ ] **Palette** (left): categories in order; search filter works.
- [ ] **Sidebar tabs** (right): Inspector · Theme · Site Settings · Assets · Packages · Version history · Editor Settings — each opens its panel; no empty/dead tab.
- [ ] **Deploy auto-reload (N32)**: open a viewer (via-server mode) next to the editor → Deploy → the viewer reloads by itself and shows the new dashboard; the editor does NOT reload. With a direct-MQTT viewer and the site's `subscribe` control topic wired → same behaviour via the `<subscribe>/reload` publish (check the broker: the publish is NOT retained). Two sites open → deploying site A never reloads site B's viewers. Site Settings → Viewer → "Reload viewers on deploy" off → deploy reloads nothing (both paths), and the setting survives deploy + editor reload. *(Broadcast scoping, opt-out and the MQTT publish are covered by server hub tests.)*
- [ ] **Site Settings tabs**: Connection · Site · Viewer · Clients. Connection = broker/auth/TLS/client; Site = name/title/topics/PWA/mobile app; Viewer = "Viewer connection mode" (via-server switch), "Viewer presence" and "View playlist"; Clients = the live viewer list (formerly its own sidebar tab — a persisted `sidebar=clients` selection falls back to Site Settings). Settings changed on the Viewer tab persist through deploy exactly as before the split.
- [ ] **Tab bar alignment**: the inner tab bars of Inspector, Site Settings and Editor Settings — and the **Version history header** — share their bottom edge with the view tab bar left of the sidebar (41px, 2px border) — no vertical offset.
- [ ] **View tab hold-to-drag (U55)** — view AND folder tabs are click-safe by default: a quick click — even with a few px of mouse movement between down and up — switches the view (or opens the folder menu) and **never reorders**; holding the button ~300 ms on a tab **arms** dragging (visible lift cue — slight scale + shadow, grab cursor) and the existing drag/drop reorder works from there; releasing without dragging disarms. **Reliability (07/2026 fix):** the tabs are permanently `draggable` and an unarmed `dragstart` is vetoed — Chrome samples draggability at mousedown, so the original flip-the-attribute-after-300 ms approach started a drag only when a re-render won the race. Verify specifically: hold until the lift cue shows, then move → the drag starts **every** time, many attempts in a row. Moving before the hold elapses burns that press (no drag, no lift cue) — release and hold again. Dblclick rename and the context menus are unaffected (the hold timer never swallows the second click). **Self-drop no-op:** press-hold a tab, drag a few px so the drop line sits next to the dragged tab itself, release → the order is **unchanged** (previously `view1,view2,view3` became `view2,view3,view1`); an unresolved drop target aborts instead of moving the tab to the end. Touch: long-press-then-drag is the natural idiom and works the same. *(Self-drop/abort/reorder are unit-tested; jittery-click, arming/disarming, self-drop and genuine reorder are e2e-tested in `test-e2e/editor-flows.test.js`.)*
- [ ] **Editor Settings tabs**: Editor Settings · AI Assistant — editor-level settings (theme, colors, grid/snapping, MQTT guard, capability-gated Server section) in the first tab, the complete AI assistant configuration (provider/endpoint/key/model/limits, Save + Test connection) in the second; both scroll independently.
- [ ] **Sidebar panels scroll** when taller than the viewport: shrink the browser window height, then check Inspector (Attributes/Styles with an element selected), Site Settings (both tabs), Theme and Editor Settings — each shows the same thin scrollbar as the palette and reaches its bottom content; nothing is clipped without a scrollbar.
- [ ] Sidebar + palette **collapse** toggles; widths persist across reload.

### Welcome tour (U37 · U73)

- [ ] **U81 — first-run MQTT connect dialog:** the **Connect your MQTT broker** dialog is shown on editor open **only when there is no broker configured, or the configured broker failed to connect** — decided from the server bridge status, **never** when a broker is already **connected** (nor while it is still connecting; give it a few seconds). Verify: with a working broker configured, opening the editor does **not** pop the dialog; with none configured (or a broker the server can't reach), it does — before the welcome tour. It is the same fields as the Connection sidebar but **larger, in a wide modal** (fields fit on one row, no horizontal scrollbar). Verify:
  - Prefilled **mqtt://localhost:1883** (protocol shows `mqtt://` — **not blank**). The Protocol dropdown offers **exactly four** options — `mqtt://`, `mqtts://`, `ws://`, `wss://` — with **no empty option**.
  - The **Connection status row sits at the top** (right under the intro, before **Broker**). The intro reads **"Feezal"** (capitalized).
  - **Dark mode is fully respected:** input **backgrounds stay dark on hover and focus** (no white flash), the **"Test connection" (and other non-primary) buttons keep a dark hover** (no white flash), input **text is vertically centred**, and the protocol dropdown + menu are dark.
  - Picking **mqtt:// or mqtts://** **disables** the Viewer-connection switch and forces **bridge mode**; **ws://** / **wss://** default to **Direct** (recommended). For **wss://** direct a hint warns **every viewing device must trust the broker's TLS certificate**. An **https://** editor page choosing **ws://** direct warns that browsers block ws:// there.
  - **TLS certificates (mqtts:// / wss:// only):** a **TLS certificates** section appears offering the **same CA + client cert/key upload & paste-PEM** affordances as the Connection sidebar (upload file, paste PEM, remove; badges when present). It is **absent for mqtt:// / ws://**.
  - **Test connection** applies the entered settings to the server bridge **without closing** the dialog; the top status row goes **yellow pulsing + "Trying to connect to `<proto>://<host>:<port>`…"** while connecting, then **green "Connected" + the uri** on success, or a **red dot + the broker error message** on failure.
  - **Save & connect** applies + closes → the welcome tour runs. **Skip for now** closes without saving → the tour still runs. *(Defaults, four-protocols, forced-bridge, status-at-top, TLS-section visibility, test-applies-and-polls-yellow-then-green, save/skip, and the show-only-when-unconfigured-or-failed gating are browser-tested in `test-browser/feezal-connect-dialog.test.js` + `feezal-app-editor-topbar.test.js`.)*
- [ ] **Auto-start on first use**: clear the `feezalTourSeen` localStorage key and load the editor with an **empty** site → the spotlight tour starts by itself (dimmed editor, cutout on the palette, explanation card). A site that already has elements never auto-starts the tour. **(Broker setup is no longer part of the tour** — it lives in the first-run connect dialog above; the tour has no broker/connection-status/deploy-the-connection steps anymore.)
- [ ] **U73 fork**: right after the Terminology page a **fork** card asks *"How would you like to start?"* with two choices (no Next button): **"Place a few elements myself"** (→ the hands-on Explore path below) and **"Autogenerate an app from my devices"** (→ the Autogenerate path). The progress dots re-count to the chosen path.
- [ ] **U73 Autogenerate path**: choosing it **skips** palette/canvas/inspector/theme and the hands-on element steps, going **straight to** a centred **"Connecting and discovering your devices"** waiting card that polls `/api/discovery/devices` (the broker was already set in the first-run dialog); a **"No devices yet? Skip to the editor →"** bail-out link is always present (click → tour ends, you're in the editor). Once ≥1 device is discovered it **opens the Generate dialog at the App tile** and the tour **hides itself** (no card/dim over the modal); finish the wizard (or cancel — cancel lands you in the editor) → on the result the tour resumes with a **finale** card spotlighting **Deploy** and mentioning **▾ → View** + **Export**. Nothing tour-related reaches the viewer/export bundle.
- [ ] **Steps (Explore path, U41 order)**: after choosing *Explore*, the tour opens with a centred **Welcome page** followed by a centred **Terminology page** (Element / View / Site as a highlighted definition list; both pages: no spotlight — the whole editor dims, strong ~78 % dim so the later cutouts clearly pop); every spotlight carries a **glow ring**: a bright arrival flare that settles within ~1 s when a new step appears (replays on every step change), then a gentle continuous breathing — both off under `prefers-reduced-motion` (static ring stays); the cutout still slides smoothly between targets. Next then walks palette → canvas → inspector (sidebar switches to the Inspector tab by itself) → **Theme** (sidebar switches to Theme, picking one works while spotlighted) → **hands-on exercise** (the drop-template step). There are **no broker/connection-status/deploy-the-connection steps** anymore — the broker is configured in the first-run connect dialog. *Every sidebar step's cutout also extends up to the right-sidebar tab switcher (`#menu-right`) so the active tab is visible/switchable.* Back returns; the dots show progress; the cutout animates between targets and follows window resizes.
- [ ] **Interaction blocking**: during the explanation steps (Welcome / Terminology / fork), clicks on the editor do nothing (stray clicks can't derail the tour); the interactive steps (theme and the hands-on steps) leave the editor fully usable under the dimming.
- [ ] **Hands-on progression (event-driven)**: the drop step spotlights the **Template tile itself** in the palette (Basic category auto-expands and scrolls it into view; with a search filter active it falls back to the whole palette); drag it onto the canvas → the tour advances by itself. Then TWO guided config steps: (1) set the `subscribe` topic — the card suggests a topic carrying a temperature reading; advances as soon as the topic is set; (2) set the template content — the card shows the **copyable snippet chip** `${msg.payload}°C` (click copies it, "Copied!" confirmation); advances once the template has content. Final card points at Deploy; **Done** ends the tour. Next also works as manual override on every hands-on step.
- [ ] **Skip & persistence**: *Skip tour* (or Done) closes the tour and persists the seen-flag **server-side** (`<dataDir>/editor.json` via `/api/editor/prefs`) — reloading never auto-starts it again, in any browser. **Deleting the data dir re-triggers the tour** on the next fresh editor load (the server flag is authoritative; the localStorage copy only covers serverless contexts).
- [ ] **Re-launch**: Editor Settings → Help → *Show welcome tour* restarts it anytime (also after it was skipped). *(Step machinery, sidebar switching, click-blocking, hands-on advance and the trigger gating are unit/browser-tested.)*

## 4. Editor — inspector

- [ ] **Attributes:** every attribute type renders the right control (text, number, select, boolean, color, mqttTopic w/ autocomplete, icon w/ autocomplete); help tooltips show.
- [ ] **Monaco editors** (template attributes, system-script, source view, history diff): syntax highlighting renders for HTML *and* javascript; `${…}` completions in templates, `fzl.` completions in scripts. The bundle registers **only** html/css/javascript/typescript (`src/monaco-slim.js`) — an element needing another language must extend that file (and `vite.config.js` if the language has a worker). *(Language registration, tokenization and worker boot are e2e-tested: `test-e2e/monaco-smoke.test.js`.)*
- [ ] **Live-apply while typing (U36):** type into a text/number/topic/style field → the canvas updates ~250 ms after the last keystroke *without* leaving the field; the caret never jumps mid-typing; blur/Enter commits immediately; **one Ctrl+Z undoes the whole typing burst** (not per-character); switching elements mid-typing never applies the value to the new selection. *(Debounce/flush/cancel/one-undo-step semantics are unit-tested.)*
- [ ] **Structured inspector (U39)** (glass-climate is the flagship; metro-climate too): the Attributes tab shows **collapsible sections** (Connection · Setpoint · Display · Availability) instead of one flat ~25-field wall; **Availability** starts collapsed, the rest expanded; clicking a section header toggles it. **Conditional visibility** — with `payload-mode = separate` only the separate-mode topics show; switch to `json` and the field set swaps live to `subscribe`/`publish` (no reload). **Advanced disclosure** — each section's `message-property-*` twins sit behind a collapsed "▸ Advanced" toggle. Editing any field still applies correctly (indices are preserved). An element with **no** section annotations (most elements) renders the old flat list unchanged. *(visibleWhen matching incl. array/AND, grouping + first-appearance order, advanced split, index preservation and default-collapsed sections are unit-tested.)*
- [ ] **Custom inspector section hook (U39 `type:'custom'`)** (E102 WP3 / E106 building blocks): an element whose `feezal.attributes` includes a `{type:'custom', component:'<tag>', section, advanced?, visibleWhen?}` descriptor renders that registered component **inline in the structured inspector** (in its section/position, no label row), with the selected element passed as `.element`. The component drives the same commit path as the N6 whole-element inspectors: emitting `feezal-attribute-changed` (`{name, value}`) writes the attribute onto the element and marks the change (dirty + undo). A component that **stamps several attributes** (a multi-attribute stamping building block) emits one event per attribute → each is a separate undo step (per-attribute, not one combined undo). A custom entry honours `visibleWhen`/`advanced` like any other; under **multi-select** it is hidden (the component is authored around a single element, matching N6). Elements without a `type:'custom'` descriptor are unaffected. *(item build, section placement, visibleWhen gating, multi-select drop, `.element` wiring and change routing are unit-tested.)*
- [ ] **Multi-select** shows the intersection; "— varies —" for mixed; editing applies to all.
- [ ] **Styles** tab: position/size, custom CSS props with autocomplete + CSS-var colour resolution in the picker; per-property mixed handling.
- [ ] **Alpha-capable colour pickers (U66)**: every colour swatch in the editor is now an `sl-color-picker` with an **opacity slider** — the Styles tab, the attribute inspector (both the plain colour attribute and rows inside the list editor, e.g. computer-stats `rings`), and the Background editor (solid colour + every gradient stop). Checks: (1) open a colour knob's swatch, drag **alpha to ~50%** → the value serialises as **8-digit hex** (`#rrggbb80`), the canvas shows the transparency, and re-opening the picker shows the same alpha (round-trip, not reset to opaque or black); full alpha collapses back to 6-digit hex. (2) **theme-var alpha (the honest rule)**: set a colour knob to `var(--primary-color)`, then drag ONLY the alpha slider -> the value becomes `color-mix(in srgb, var(--primary-color) 50%, transparent)` — switch the theme and the translucent colour **re-tints** (it did not silently freeze to a literal); drag alpha back to 100% → the plain `var()` returns; pick a genuinely different colour → a literal is written (that is a colour choice, not an alpha tweak). (3) **the swatch never lies**: type `rgba(0,128,255,.5)` or an 8-digit hex into the text field → the swatch previews it truthfully (it used to show black); type a `var(--…)` → the swatch resolves it against the element; an unresolvable value shows the **checkerboard** with an empty trigger, never black. (4) **gradient stops**: give a stop alpha via the picker, and give a `var()` stop alpha → the `color-mix` stop form **round-trips** through re-selecting the view (the stop parser is paren-depth-aware now). (5) the picker panel opens fully inside the narrow sidebar (hoist) and the trigger fits the dense rows. *(Alpha round-trips, the color-mix rule incl. fallback chains, honest var()/rgba swatches, the empty-not-black gate and the nested-paren stop parser are unit/browser-tested: `test/feezal-sidebar-inspector-attributes.test.js`, `test-browser/u66-color-alpha.test.js`.)*
- [ ] **Site-wide colour ranges (U65)**: Themes sidebar now has a **Ranges** tab — the manager for named value→colour mappings stored in `<feezal-site color-ranges>` (site HTML, so export/offline work). **Manager**: create a range (name is slugified — spaces become dashes), type **bands** (from + colour rows, the existing gauge shape), **gradient** (stops + blend space oklch/srgb) or **enum** (value + colour rows); every card shows a **swatch strip** preview and a usage count ("N in use"); edits apply live; **rename rewrites every reference** (element style pairs *and* gauge `ranges` attributes — check both survive a rename); **every** delete asks first in a **styled sl-dialog** (same look as the assets-sidebar confirms, correct in editor dark mode - never the browser's native confirm) - a used range warns with the usage count, an unused one asks plainly; Cancel keeps the range, Delete strands any bindings, which just keep their last colour. **Colour rows** (Styles tab): every colour **custom-property** row (`--…`) shows a **Static · Subscribe · Range** mode switch — plain CSS properties (`background`, `color`) stay Static-only (they cannot carry the paired declarations). **Static** = the unchanged colour + theme-var picker. **Subscribe** = topic (autocompleting) + message-property, both starting **empty** — the payload IS the colour: publish `#e53935`, an `rgb()`, a colour name or `var(--error-color)` to the topic and the element recolours live (theme vars still resolve at use time). **Range** = range dropdown (first entry **"＋ Create new colour range…"** jumps to the manager with the create form open; each option shows its swatch strip) + topic/property **pre-filled from the element's subscribe / message-property** but editable — e.g. tint a border from an *outdoor* sensor while the dial shows the room; clearing the topic falls back to the element's own primary value. Publish values across band edges → the colour steps (bands), blends smoothly (gradient — a `color-mix(in oklch, …)` value), or maps states (enum, with the default catching unknowns); a value below every band **keeps the previous colour** (no flash to black). **Serialization**: source view shows the paired properties (`--x-source-topic`, `--x-source-property`, `--x-range`) next to the colour var — the resolved colour is written INTO the var itself, so a static export shows the last colour immediately and goes live when MQTT connects. Editor canvas: with "Prevent MQTT element manipulation" ON (default) nothing subscribes in the editor — the binding only runs in the viewer. **Gauges**: set a gauge's `ranges` attribute to a range **name** — arc/ring fill follows the range (any type), needle zones render for **bands** ranges; inline JSON keeps working unchanged. *(Resolver semantics (bands/gradient/enum, oklch blending, safe degradation), quoting of ugly topics, the paired-property runtime incl. editor gating and primary-value fallback, manager CRUD + rename-rewrite, the inspector modes incl. the create sentinel, gauge named ranges, and an outerHTML export round-trip are unit/browser-tested: `test/feezal-color-ranges.test.js`, `test-browser/u65-color-ranges.test.js`.)*
- [ ] **Theme-var discipline / base theme**: the built-in **default theme is now real CSS variables** (`:root` block from `www/src/feezal-base-theme.js`) instead of per-element hex fallbacks - open a dashboard with **no theme** selected in editor AND viewer → the default look is unchanged (blue #0284c7 accents, white surfaces, dark text); switch to any `feezal-theme-*` → everything still retints (theme classes override the :root base by inheritance). Element colour defaults are BARE canonical vars now - no `--sl-*`, no literal fallbacks - so a regression here shows as MISSING colour (transparent/black), not as a wrong shade. Static export: default-theme export renders with the same palette offline. *(Ratchet: `test/theme-var-discipline.test.js` fails CI on --sl-color-* or any canonical var with a fallback; the base block completeness is asserted too.)*
- [ ] **Site locale — numbers & language defaults (N38 + A27)**: Site Settings → Site → **Locale** shows locales **by example** (`1.234,56 — Deutsch`) with Automatic (browser language) first, a Custom BCP-47 input, and a live preview line. Pick **Deutsch** → value cards re-render live: `basic-number`, glass/metro value cards, every family **gauge**, `material-gauge`/`material-tank` and the climate readouts (all three families) show `21,5` instead of `21.5`; state words flip to German — glass/eink/metro/circle/panel switch/light/contact/wled render **Ein/Aus**, **Offen/Geschlossen/Gekippt**, countdown **Fertig** — with NOTHING configured per element (case matches each family: metro lower-case `offen`, panel engraved `EIN`). **Explicit values win**: type a label or a `decimal-separator` → it stays whatever the locale is; clear it → the locale default returns. **Grouping** is per-element opt-in (`grouping` switch): on → `1.234,5`, off → `1234,5`. `basic-datetime` with no `locale` attribute follows the site locale. **The saved HTML is language-independent**: switch locale back and forth, save, diff — no label/text attributes appear (locale defaults are never materialised; pre-existing baked `text-on="On"` / `decimal-separator="."` reflection junk is healed away on load). Automatic mode: a German-language browser shows German with zero config; an export with no set locale follows the viewing device, a set locale is pinned (serializes with the site). *(Chain resolution, formatter caching, grouping, the dict opt-in, authored-wins incl. the junk-healing heuristic, live locale switch and HTML purity are unit/browser-tested: `test/feezal-locale.test.js`, `test-browser/n38-a27-locale.test.js`.)*
- [ ] **Classes** selector applies/removes `feezal-class-*`; conflicting inline styles cleared with a note.
- [ ] **Custom (N6) inspectors** for the remaining ones: layout-flex, layout-responsive, light, climate, cover, alarm-panel, humidifier, media-player, vacuum, repeater, energy-flow. *(navbar + layout-app + the host protocol are automated.)*
- [ ] **Background editor / style groups (N34)**: select a **view** → the Styles tab shows a **Background** widget (mode buttons Solid / Image / Gradient) instead of a raw `background` row, and `width`/`height` stay ordinary rows. **Solid** (default): value input + resolving colour swatch, exactly like the element colour-var rows — the input accepts hex, named colours and `var(--…)` (swatch resolves it against the view; unresolvable → checkerboard); placeholder/default is `var(--primary-background-color)`; **clearing the input removes `background-color` and the view falls back to the theme default** (switching from a solid colour back to empty must NOT leave the old colour behind); picking from the swatch writes the hex into the field. **Image**: URL field (or use the asset manager's *Set as background*, N33 — the editor reflects the stamped values on re-select), size select (cover/contain/auto/custom + free input), repeat select, 3×3 position anchor grid + custom field — each control updates the canvas immediately; clearing the URL clears the whole image family. **Gradient**: linear (angle °) / radial (circle/ellipse), ordered colour stops (add / ↑↓ reorder / ✕ remove, min 2), live preview bar; the serialised `linear-gradient(…)`/`radial-gradient(…)` lands in `background-image`. **Themed stops (U59)**: each stop is a **value input + resolving swatch + a "var…" theme-var menu** (same model as the Solid input) — type or pick `var(--primary-color)` (etc. from the canonical set) and the swatch resolves it against the view (unresolvable → checkerboard); the gradient then **re-tints when you switch the view/site theme** (a hex stop stays fixed). Re-selecting the view round-trips a `var()` stop back verbatim (source mode shows `var(--…)` inside the gradient, not a frozen hex); mixing `var()` and hex stops works. Re-selecting the view round-trips every mode back into the editor's fields; only longhands are written (check source mode — no `background` shorthand; a legacy inline shorthand is migrated to longhands on first edit). The covered `background-*` longhands never appear as extra custom rows and "Add CSS property" refuses them; undo after an edit reverts the whole multi-property change in one step. *(Mechanism — group items, covers, suppression, mount, multi-property apply incl. null-removal, add-prop refusal — and the editor's mode detection, empty-=-default semantics, var() pass-through, swatch resolution, shorthand fallback, gradient round-trip and stop editing are unit-tested.)*
- [ ] **List editor (U35)** (radio/select `options`, alarm-panel/humidifier `modes`, fan `preset-modes`, computer-stats `rings`, paper-dropdown `items` (value/name), paper-tabs `items` (bare names)): the attribute renders as rows instead of raw JSON — add / edit / ✕ delete / **drag-handle reorder** all update the element live; the `rings` editor shows a colour swatch and numeric `max`; fan presets / tab names are single-column bare-string lists; paper-dropdown's Polymer-escaped (`&quot;`) attribute parses fine; a legacy slash-separated paper-tabs value opens in the raw fallback (still renders in the viewer); hand-edit the attribute to invalid JSON in source mode → the inspector falls back to a raw text input without destroying the value. *(Parse/serialize/reorder round-trips and the paper formats are unit/browser-tested.)*

### Conditions (E50)

*(Engine semantics — operators, AND-combined visibility, later-row-wins, pristine revert, editor no-op, runtime re-edit — are unit-tested; this is the UI + live-broker pass.)*

- [ ] Select a single element → a **Conditions** tab appears beside Attributes/Styles (not for the view, multi-selection, or component instances); the tab label shows the row count (e.g. "Conditions · 2").
- [ ] Add a row: topic input offers **MQTT autocomplete** but accepts free text (incl. `${param}` — no autocomplete for placeholders); operator/value/action selects render; action-specific fields switch correctly (class name / style key-value rows with add-remove / attribute select from the element's declared attributes + custom… fallback / keep-layout checkbox for show-hide).
- [ ] Rows reorder via ▲▼ and delete via ✕; deleting the last row removes the `conditions` attribute (check in source mode).
- [ ] Element with ≥ 1 condition shows the **👁 badge** (top-left) on the canvas; effects are **not** applied in the editor.
- [ ] The `conditions` JSON round-trips through the **Monaco source editor** (edit there → inspector reflects it).
- [ ] **Viewer:** `hide` row hides on matching payload and restores on unmatch (with `keep-layout`: element keeps its space); `class`/`style`/`attribute` rows apply and revert; a `show` row keeps the element visible only while matched (after the first message); retained topics apply on load.
- [ ] Conditions survive save/deploy/reload and the static export.

## 5. Views & navigation

- [ ] **Switching a view between absolute and flow (B80)** — build a view with several elements at distinct positions in **absolute** mode, then flip **Child position → flow** in the view inspector. The tiles must lay out in the flex flow **immediately and correctly — no reload**, and none may sit off its slot (the old symptom was scattered tiles until a deploy + refresh). Drag one to reorder: flow reordering works right after the switch, without a reload. Now flip **back to absolute**: every element returns to **its original pre-flow position**, not stacked in the corner. Repeat the whole cycle with a **deploy in between** (absolute → flow → deploy → reload → absolute) — the positions must still come back, because the offsets are parked on `data-abs-*` in `views.html` rather than deleted. Check the delivered HTML contains **no `data-abs-*`**: open the **viewer** and a **static export** and search the page source. Finally, an element **created in flow** (no stashed position) must land where it was rendered when you switch to absolute — not at 0,0. *(Stash/restore, the no-stash rendered fallback, the save/deploy round trip and the delivery strip are unit-tested in `test/feezal-child-position-switch.test.js`; the live re-init is the manual part.)*

- [ ] Reorder / delete views; view folders — drag into folders, nesting UI.
- [ ] Tab bar reflects views; hidden/foldered views excluded as configured.
- [ ] Viewer: navigate via **Navigation** element, **Navbar**, swipe (if used).
- [ ] Region/embedded views (layout-flex / layout-responsive / layout-app / layout-view) render the referenced view in editor (placeholder) and viewer (live).
- [ ] **Rubber-band selection (A38)**: on an **absolute** view, press on empty canvas and drag → a translucent selection rectangle in the selection colour follows the pointer, and on release every element it touched is selected (the Inspector shows the multi-selection, not just the outlines). **Hold Shift/Ctrl/Cmd while dragging** → the band ADDS to the current selection instead of replacing it. Elements only count when the band actually overlaps them — grazing an edge does not. **Locked** elements are still band-selectable. A press that starts **on an element** must drag the element (interact.js), never draw a band; a **right-press** opens the context menu; a **click** (no drag) on empty canvas selects the view, and a click on an element selects it. Near the canvas edge the view **auto-scrolls** while you keep dragging. Then: **switch views and band again** (works on the new view), **switch back and band again** (works on revisit), and enter/leave **component edit** and band inside it — these were the three lifecycle bugs of the old library (B2/B35/B48) and are the scenarios most worth re-checking by hand. Finally, **save and look at the source**: the markup must contain no selection-rectangle div and no inline `z-index` on canvas elements. *(Geometry unit-tested; the gesture, the lifecycle scenarios and the no-artifacts guarantee are browser-tested in `test-browser/feezal-editor-rubberband.test.js`.)*
- [ ] **Selection never moves the right sidebar**: open **Layers** (or Themes, or Editor settings), then click an element on the canvas → it gets selected but the sidebar **stays on the panel you were in**. Same for a rubber-band multi-selection, Ctrl/Shift-click, and clicking empty canvas (which selects the view). The **only** case that jumps to the Inspector is **adding a new element**: drag one in from the palette, or drop an image from the Assets panel → the sidebar switches to the Inspector with the new element selected, because a brand-new element is unconfigured. **Duplicate, paste and “create component” do NOT jump** — those produce already-configured elements. *(Browser- and e2e-tested.)*
- [ ] **Multi-selection drag does not self-snap (B104)**: select two or more elements that share an edge (e.g. same `left`), drag the group → it moves smoothly; the members must NOT magnet or jitter against each other. A **stationary** element outside the selection still snaps normally during that drag, and with no drag in progress a selection parked elsewhere is still a valid snap target (resize an element towards it → it snaps). *(Browser-tested.)*
- [ ] **Equal-gap guides while dragging (U89)**: place two elements in a row with a visible gap between them (say 16px), then drag a third towards the row → as it nears, **two dotted spans with end ticks and the px value** appear at the dragged element's centre line: one over the existing gap, one over the gap being proposed. With element snapping on, the drop **lands on that rhythm** (a third card 16px past the second). Works to the LEFT of the row too, and vertically for a column. The arrows vanish on drop and when the drag leaves range. A lone neighbour proposes nothing (a gap needs a pair), and elements in another row/column are ignored. An exact **edge** alignment still wins when it is closer than the gap proposal. *(Geometry unit-tested; the drag was driven end-to-end in a real editor.)*
- [ ] **Distribute never overlaps (U91)**: select three or more elements that are **crowded** — their combined width larger than the span between the outermost two — and run **Distribute horizontally**. They must end up **flush edge-to-edge, never overlapping**: the first stays put and the strip grows to the right past where the last one used to be. With **room to spare** the behaviour is unchanged: equal gaps, and both outermost elements stay exactly where they were. Same vertically. One Ctrl+Z undoes the whole operation.
- [ ] **Source formatting (B105)**: open **source mode** (Ctrl+Shift+U) on a site with several views → every closing tag sits on its **own line**, indented; no line break inside a `</tag`, no `>` hugging the next opening tag, no two closing tags glued together. Long elements still show **one attribute per line**. Deploy, reopen source → identical (formatting is a fixed point). **Templates**: put tight markup with no spaces in a `basic-template` (e.g. `<b>bold</b>glued<i>italic</i>`), deploy, reopen → it is preserved character-for-character and the rendered text has gained no spaces. *(Unit-tested in `server/test/format-html.test.js`.)*
- [ ] **Fold-friendly source view (U92 / B107)**: in **source mode**, an element that is too long for one line shows its **identifying attributes on the opening-tag line** — `label` and `subscribe` for elements (both when present), `name` for a view — with the remaining attributes below. **Fold** the element in Monaco (the gutter arrow): the collapsed line still says which element it is. This works even when the attributes were written last in the markup, because serialization puts them first. An element with neither `label` nor `subscribe` keeps a bare tag line. **B107**: the closing tag of a multi-line element sits on its **own line**, indented level with the opening tag — never glued to the last attribute. A short element that fits on one line stays compact. Deploy and reopen source → identical (fixed point). *(Both halves unit-tested; the end-to-end path was driven through the real source view.)*
- [ ] **Notifications on/off + dark mode (U85 / B102)**: in **Editor settings** there is a **Show notifications** switch, **on by default**. With it on, deploying shows the “Deployed” toast at the bottom; in a **dark editor the toast is dark** (it used to render white). Switch it **off** → confirmations stop appearing, but **errors still do**: force a failing deploy and the “Deploy failed” toast with its Retry action must still show, since a silently swallowed failure is how work gets lost. The setting **persists** — reload the editor and the switch and behaviour are as you left them. *(The gate and the dark palette are unit/browser-tested; a ratchet in `test/editor-dark-palette-propagation.test.js` fails CI when a new component reads the editor palette without being listed in the editor's dark block.)*
- [ ] **Flow layout views (U41)**: set a view's **child-position = flow** (view attribute inspector) → its elements tile **left→right, wrapping row by row** (not one vertical column) in editor AND viewer/export identically. **Place**: drop elements from the palette → they insert at the drop point (no top/left; check source mode is clean). **Select**: click an element selects it, click empty space selects the view, Ctrl/Shift-click multi-selects. **Reorder**: drag an element (interact.js, not native HTML5 drag) → the tile lifts and follows the pointer while the **other tiles live-reflow** around a placeholder; drop commits the new DOM order and **one Ctrl+Z undoes it**. The cursor is `move` only over tiles, the drag ghost is just the tile (not the whole canvas), and the view tab bar / click-selection keep working during and after. **Knobs** (view inspector, shown only in flow mode): `flow-gap`, `flow-direction` (row/column), `flow-justify`, `flow-align` change the layout live. **% widths**: set an element `width: 50%` → it takes half the row (auto-resizes with page width); `100%` → full row (mind gap: exact halves need `calc(50% - gap/2)`). **Resize**: hovering a tile's bottom-right edge shows the resize cursor; dragging it resizes the tile (width/height) and the row reflows; a size typed into the Style inspector's width/height is **kept** across a subsequent drag (not wiped). The Style inspector **hides top/left** for flow-view children — both the declared rows and any stray inline `top`/`left` (width/height stay). **Legacy alias**: a view saved with `child-position="static"` loads and renders as flow, and the next save writes `flow`. *(Alias, flow-var mapping, sortable→undo wiring and click-selection are unit/browser-tested; the % preset fraction buttons are deferred.)*
- [ ] **Grid layout views (U90)**: set a view's **child-position = grid** → its elements tile into a cell grid, and — unlike flow — a **double-size element no longer wastes the space beside it**. The case to actually reproduce: a view with one element sized exactly 2× the others (e.g. a `basic-camera` at 520×380 among 245×180 cards) → in **flow** nothing sits beside the camera's lower half; switching to **grid** makes the following cards fill that space and the view gets visibly shorter. Editor, viewer and static export must render **identically**. **No configuration needed**: leave `grid-cell-width`/`grid-cell-height` empty → the cell is derived from the **smallest** element in the view, so the ordinary tiles are 1×1 and the big one spans 2×2. **Knobs** (view inspector, shown only in grid mode): `grid-cell-width`, `grid-cell-height` (explicit cell; overrides the derived one), `grid-gap`, `grid-justify` (start/center/end/space-between — positions the whole grid when the view is wider than a whole number of columns), `grid-dense`. **Quantisation is expected**: a tile is stretched to its cell span, so a 520 px element in a 245 px cell renders 495 px wide — its **authored** width/height is unchanged (check the Style inspector, and switch back to flow: the original size returns). **`grid-dense`**: off by default; turn it on in a view where a wide element wrapped and left a hole → a later element moves **backwards** to fill it. Expect the visual order to stop matching the element order, which makes drag-reorder harder to predict — that is why it is opt-in, not the default. **Reorder**: drag a tile → it lifts at its **own pixel size** (not collapsed to nothing) and follows the pointer while the others reflow around a placeholder **that claims the same span**; drop commits the new order and one Ctrl+Z undoes it. **Resize**: drag a tile's bottom-right edge → it snaps by whole cells; the saved markup must stay clean (source mode: no `grid-column`/`grid-row` inline styles on elements, no leftover `feezal-lift` class). **Place**: palette drops insert at the drop point by DOM order, like flow. The Style inspector **hides top/left** for grid children, as it does for flow. **Round-trip**: grid → absolute → grid returns every element to its original size and position (B80 stash). *(Span arithmetic is unit-tested; the real layout, dense-vs-sparse divergence and teardown are browser-tested in `test-browser/feezal-view-grid.test.js`.)*
- [ ] **Umlaut view names (B30)**: create a view named `Küche` → switching to it works from the view tabs, navigation elements, and a direct URL `…#/Küche` (browser shows `#/K%C3%BCche`) in both editor and viewer; reloading on that hash stays on `Küche` instead of falling back to the first view; the address-bar hash doesn't flicker/rewrite on every switch. *(Hash decode + sync loop are browser-tested.)*

---

## 6. Elements

**Automated baseline:** every element package mounts cleanly (smoke harness);
`button · switch · image · number · gauge · slider · progress · checkbox ·
contact · motion · select · input` have full MQTT behaviour covered; E38
scaling is asserted for the container-query and ResizeObserver mechanisms
(checkbox, slider, select).

**Manual recipe — per element, focus on what's left** (tick in the table below):
1. Configure via the inspector (element-specific attrs render sensible controls).
2. Resize across a reasonable range → internal UI **scales** without clipping (E38) — visual check; the mechanism itself is automated.
3. Deploy → **viewer**: reacts to live MQTT / publishes on interaction *(skip for the automated 12)*.
4. Light + dark theme: colours inherit the theme / `--feezal-*` tokens.
5. Icons (where used) render as glyphs, **not literal text**.

### Basic
- [ ] chart · [ ] countdown · [ ] datetime · [ ] iframe · [ ] image · [ ] json · [ ] navigation · [ ] number · [ ] svg · [ ] table · [ ] template
  - **Countdown/timer (E34)** (basic-countdown): all three `mode`s — seconds-remaining (ticks down locally, clock-skew-immune), target-timestamp (Unix s/ms + ISO auto-detected, counts to it against the client clock), count-up (stopwatch from a start timestamp); `format` auto picks mm:ss / HH:mm:ss / d HH:mm:ss; digits + ring turn to the error colour within `warn-seconds`; the ring becomes a horizontal bar on a wide element; at zero the `done-label` replaces the digits; `publish-on-zero` arms only after a value > 0, fires exactly ONCE on reaching zero, never on a stale retained <= 0 on load, re-arms on a new > 0 (every viewer publishes — automations must tolerate duplicates); editor shows a static 12:34 preview and never ticks/subscribes/publishes. `show-ring` off persists (default-true boolean via feezalBoolean).

### Layout (composite — also test embed & swap)
- [ ] layout-flex · [ ] layout-responsive · [ ] layout-app · [ ] layout-repeater · [ ] layout-view

### System (pseudo-elements — placeholder in editor, behaviour in viewer)
- [ ] system-pin · [ ] system-script · [ ] system-connection-status · [ ] system-splash

### Material packages (palette categories: **Material** widgets + **Circle** device cards)
*(2026-07, E133: palette category rename round two — the device cards (light, climate, cover, contact, wled…) are now **Circle** (their circular-slider design language); the MD3 widgets (button, checkbox, gauge, slider…) are now **Material** (they literally are Material Design 3 controls). Same `feezal-element-material-*` packages; only the palette grouping/order changed. Palette order: Basic, Layout, System, Circle, Glass, Metro, Material, …)*
- [ ] alarm-panel · [ ] badge · [ ] button · [ ] camera · [ ] checkbox · [ ] chip · [ ] climate · [ ] clock · [ ] computer-stats · [ ] contact · [ ] countdown-dialog · [ ] cover · [ ] dialog · [ ] door-lock · [ ] energy-flow · [ ] fab · [ ] fan · [ ] gauge · [ ] humidifier · [ ] icon-button · [ ] input · [ ] light · [ ] map · [ ] media-player · [ ] motion · [ ] navbar · [ ] outlet · [ ] plant · [ ] progress · [ ] radio · [ ] schedule · [ ] select · [ ] sensor · [ ] slider · [ ] switch · [ ] tank · [ ] time-picker · [ ] vacuum · [ ] wled
  - **outlet (E121, palette name "Switch" since E130):** circle-light subclass locked to on_off mode — big power button, no ring; only state/availability/label attributes (generic inspector, no mode select); shares the `--feezal-light-*` theme tokens; carries the family `switch` discovery descriptor (E130 — the original collision concern was moot: multiple matching cards are a user choice, exactly how glass/metro coexist).

### Carbon (E85 — IBM Carbon Design System)
- [ ] button · [ ] switch · [ ] checkbox · [ ] slider · [ ] select · [ ] input

### Glass (E58 frosted cards)

- [ ] button · [ ] value · [ ] gauge · [ ] sensor · [ ] motion · [ ] light · [ ] switch · [ ] climate · [ ] contact · [ ] cover · [ ] fan · [ ] lock · [ ] dialog · [ ] dialog-view · [ ] countdown-dialog · [ ] wled *(E138: occupancy→motion, numeric sensor→value, sensor = the NEW alarm card; E139: gauge = the circle dial)*

### Metro (E55 live tiles)
- [ ] button · [ ] switch · [ ] light · [ ] climate · [ ] value · [ ] gauge · [ ] sensor · [ ] motion · [ ] media · [ ] contact · [ ] cover · [ ] wled *(E152: tile→button)* *(E138: occupancy→motion, numeric sensor→value, sensor = the NEW alarm card)*

### Eink (E57 e-paper 1-bit cards)
- [ ] button · [ ] number · [ ] sensor · [ ] motion · [ ] contact · [ ] climate · [ ] switch · [ ] light · [ ] cover · [ ] fan · [ ] wled · [ ] dialog · [ ] dialog-view · [ ] countdown-dialog *(E138: sensor narrowed to alarms, motion is new)*

### Panel (E56 analog cockpit)
- [ ] led · [ ] switch · [ ] value · [ ] gauge · [ ] knob

### TUI (E59 terminal / retro-CRT)
- [ ] value · [ ] checkbox · [ ] menu · [ ] sparkline · [ ] log · [ ] ascii · [ ] panel · [ ] crt

### Fancy (E139 Lottie-animated device cards)
- [ ] fancy-switch · [ ] fancy-contact · [ ] fancy-sensor — *(released; light/cover/climate/lock are DISABLED pending rework: not registered, must NOT appear in palette, discovery picker or Generate; fancy-contact's `type` is pinned to window — the select offers only window and discovery always stamps window)*

- [ ] **Fancy family (E139)** — animation IS the chrome: flat duotone vector motion (lottie-web, lazy-loaded only when a fancy card is on a view — check the network tab: the ~250 kB chunk must NOT load on a dashboard without fancy elements, and never in the editor). **Editor = static pose**: the canvas shows a duotone SVG of the current state, no animation, no lib. **`prefers-reduced-motion`** freezes to the same poses in the viewer. **Theme recolour**: the animation uses two tones resolved from the element — base = `--secondary-text-color`, active = `--primary-color` (`--error-color` on the alarm sensor); switch the site theme AND a per-view theme → the running animations re-tint (rebuild) to the new palette; the `--feezal-fancy-base/active-color` style knobs override per element. **Directional transitions** — state changes play a clip, never jump-cut: light OFF→ON grows the glow in and ON→OFF plays it back out; lock LOCKED→UNLOCKED swings the shackle open and back. **fancy-light**: breathing glow while on; with a known brightness the glow SCALES with it instead (drag the light to 20/80 % and compare); tap toggles. **fancy-contact**: window/door/garage variants (the `type` attribute swaps the animation). The **window** is a German Dreh-Kipp: **square at rest** (outer Fensterrahmen + inner Flügel as split stroke frames with a visible divider gap, **half-transparent active-tone glass**); the **perspective trapezoid exists only while open/tilted** with the hinge edge nailed to the frame (the sash must never detach), and a **handle on the free edge turns BEFORE the sash moves** — closed = down, open = left, tilted = up; closed→open turns the handle then swings the sash with perspective foreshortening **and a breeze flourish** (three nearly-transparent wavy streamlines flow in through the opening while the sash swings — gone by the time the open pose holds, and NEVER visible at rest), closed→tilted turns the handle up then tilts (Kipp); **closing plays sash-first-handle-last** (derived reverse clip — verify the order visually); the editor pose mirrors frame split, glass, perspective and handle position per state. **fancy-cover**: the blind SEEKS to the reported position (it stands where the device says — publish 0/30/100 % and watch it travel; settling-aware because it renders the controller position); tap upper half = up, lower = down. **fancy-climate**: heat waves rise while heating (valve > 0, or setpoint above actual without a valve topic); state line shows `actual → setpoint` (localized separators, N38); tap left/right steps the setpoint. **fancy-sensor** (alarm slice) — **per-type animation**, and unlike its siblings it is **CSS-driven, not Lottie** (its `animationKey()` is null, so it loads **no** lottie chunk and the duotone SVG pose IS the animation, in the viewer and the editor alike; `prefers-reduced-motion` freezes the accents on). Each **type** has its own triggered motion: **water-leak** a bobbing drop with ripples spreading on a puddle; **smoke** a flickering flame over a detector base; **gas** wisps wafting up from a source; **co** a C≡O molecule with a radiating alarm ring; **vibration** a device that shakes with side waves; **tamper** a padlock whose shackle springs open with a pulse; **generic** a sensor core with radiating rings. At rest each shows its own calm "clear" pose; on trigger the accent (error colour for the alarm classes, `--feezal-fancy-active-color` override wins) animates. **fancy-lock**: jammed = shake loop; tap toggles lock/unlock. **Badges**: availability ⚠, battery, sabotage/fault per the controller contracts. **Override**: `animation-src` (Asset Manager Lottie JSON) + `animation-map` replaces the built-in set; a broken src leaves the static pose in charge. All six are pure views over the shared E137 controllers (wiring identical to the circle/glass/metro siblings — parity-tested). **fancy-switch (E162 proof piece)**: switching ON slides the knob with a visible OVERSHOOT, flashes a radial wipe from the knob and fires a two-burst MULTI-COLOUR confetti explosion (~24 particles, curved flight paths, spins — reference-derived choreography); switching OFF plays its own explicit shrink-down (imploding ring, knob slides home with a squash) — verify it is NOT the on-animation reversed; the confetti keeps its own palette while track/knob follow the theme tones; judge the feel at 60 fps on real hardware; MQTT contract = glass/material switch (subscribe / message-property / **publish** / payload-on / payload-off, state text via text-on/text-off; verify a tap actually publishes) — NOT a light-controller view; use fancy-light for dimmables. *(Generator validity, the two-tone recolour, the segment player incl. reversed clips and seek, editor-no-lib, tilt tristate, position/brightness seek, retint rebuild and the broken-src fallback are unit/browser-tested: `test/fancy-animations.test.js`, `test-browser/feezal-elements-fancy.test.js`; regenerate the animation set with `node scripts/generate-fancy-animations.js`.)*

### Paper (legacy)
- [ ] badge · [ ] button · [ ] card · [ ] checkbox · [ ] dialog · [ ] dialog-view · [ ] dropdown · [ ] listbox · [ ] slider · [ ] switch · [ ] tabs

### Element-specific notes
- [ ] **Carbon family (E85)** (carbon-button/switch/checkbox/slider/select/input): wraps `@carbon/web-components` the way the material family wraps `@material/web` — attribute contracts mirror the material siblings one-to-one (same subscribe/publish/payload-on/off/message-property descriptors), only the presentation is Carbon's sharp-cornered enterprise look. **button**: Carbon kinds (primary/secondary/tertiary/ghost/danger) via `variant`; E79 active-state highlight follows `payload-active`/`-inactive`. **switch**: Carbon toggle — ON track colour comes from `--feezal-switch-track-on` (wired onto Carbon's support-success token, so it's the theme primary, not Carbon's green); `state-labels` shows On/Off text; `--feezal-switch-border-on`/`-off` draw a track border per state (OFF defaults to the primary colour, ON to transparent — Carbon's track has no border of its own, the ring is injected into the toggle's shadow root as an inset outline, so it must not shift the layout). **slider**: Carbon always shows min/max labels (by design, no hide option); B17 derived step applies; unfilled track defaults to the primary colour, `--feezal-slider-knob-color` styles the handle (defaults to the primary text colour). **select/input**: field with a primary-coloured bottom border by default — check the field background/border tokens follow light AND dark themes; `--feezal-select-popup-background-color` colours the dropdown option list (injected option styling — Chromium/Firefox honour it, Safari/mobile native pickers ignore it). Carbon's density is fixed (no E38 container-query scaling in the first cut) — verify the default sizes look right rather than expecting the internals to scale with the element. Fonts fall back from IBM Plex Sans (not bundled) to the system stack. *(MQTT contracts for button incl. E79 and switch are browser-tested; all six mount via the smoke harness; slider step derivation is unit-tested.)*
- [ ] **Glass family (E58)** (glass-button/sensor/light/switch/climate/contact/cover/lock + `feezal-theme-glass`): frosted `backdrop-filter` cards over the theme's gradient wallpaper (set view backgrounds transparent, or use your own wallpaper image — the frost is the theming); check light AND dark (frost variables follow `prefers-color-scheme`); `degrade` on any card swaps the live blur for a semi-opaque solid — verify on a weak tablet that scrolling stays smooth with it on; squircle corners round-trip (Chrome 139+ gets `corner-shape`, others the radius fallback). **button** (renamed from scene): tap publishes, `payload-active` highlights. **no shadow rectangle**: card shadows render rounded and unclipped (host overflow visible) — check corners and while pressing. **sensor**: value/unit/decimals. **light**: tap toggles, **long-press or the ⋯ button opens the Apple-Home-style details popup** (top layer, ~200px wide): big vertical brightness pill (drag anywhere on it), colour-temperature slider beneath, round hue/saturation wheel at the bottom — sections appear per capability, brightness is always offered on dimmables; outside tap closes without toggling the card; full circle-light wiring contract — `payload-mode: json` (z2m single-topic), `on-off-source: brightness` (Homematic LEVEL, payload-on `1.005` restore), `mode` picks the back sliders (brightness / brightness+ct / ct / rgb / hs), colour temp kelvin/mired, discovery maps `supported_color_modes`/mired range — effects/white stay circle-light’s; two-tab Topics/Config inspector. **switch**: material-switch contract, tap toggles, per-state `icon-on`/`icon-off`, availability badge. **contact**: circle-contact contract (open/closed/`payload-tilted`, availability badge, `type` picks the icon); flat attribute form like the material sibling. **cover** (renamed from shutter): circle-cover contract (json/separate, up/stop/down payloads, position, optional tilt slider, invert, availability) with the two-tab inspector; plain tile — tap opens the details popup: vertical position pill (fill = open %, drag to set), up/stop/down buttons, tilt slider when configured. **climate** (renamed from thermostat): circle-climate contract (setpoint/actual/mode subset, json TRV shape + separate topics, min/max/step/unit, modes list, discovery incl. °C/°F unit map); card shows actual + → setpoint + mode; popup: vertical setpoint pill (snap to step, actual shown at the top) with mode buttons beneath. **lock** (circle-lock contract — locked/unlocked/jammed, `payload-lock/-unlock/-open`, jammed=fault, Keymatic `ERROR`, availability, battery): **tap toggles locked ↔ unlocked**; **long-press or the ⋯ button opens a popup** with the explicit **Lock / Unlock / Open** actions (Open only when `payload-open`/`publish-open` is set) — a popup action publishes and closes; the card has **no inline buttons**; read-only lock (no command topic) shows no ⋯ button and doesn't toggle. Auto-discovery wires all three device cards (light/binary_sensor/cover). **Popups (E155)** — the detail popovers (light / cover / climate / fan / wled / lock / meter) are **centred in the viewport**, modal-style, not anchored to their card: open one from a card in each screen corner and it appears in the same centred place every time. Width is **~70 vw, floored at 240 px and capped at 450 px** — check a phone-width window (the pill/wheel/mode chips must still lay out) and a wide desktop (it must not sprawl); height stays `fit-content` under `max-height: 90vh`, so a tall popup scrolls rather than overflowing. The `::backdrop` still dims behind it so it reads as modal. The old above/below/clamp JS is gone, so there is nothing left to re-run on resize — resize the window with a popup open and it stays centred. **switch/contact**: state texts configurable (text-on/off resp. text-open/closed/tilted). **occupancy**: same circle-motion contract as the metro tile (type icons, texts, availability, discovery incl. device_class), card highlights while detected. **All cards**: topics set via the inspector start flowing ON THE LIVE CANVAS immediately (subscription rewire — no reload needed). *(MQTT contracts, E77 derivation, payload matching, json/separate publishes, colour/CT round-trips, the live-canvas rewire, editor guards and inspector registration are unit/browser-tested.)* **E140 (per-state icon colour parity):** the glass state-icon cards now expose the **inactive-state** icon colour as a Style-inspector var (it used to fall back to the unexposed shared frost-muted, so only the ON/active side was settable): **glass-switch/glass-light** `--feezal-glass-off-color`, **glass-button** `--feezal-glass-inactive-color`, **glass-contact** `--feezal-glass-closed-color`, **glass-sensor/glass-motion** `--feezal-glass-clear-color` (glass-lock already had all states). Each defaults to the frost muted colour, so untouched cards look identical; set e.g. `--feezal-glass-off-color: #888` on one switch → only its OFF-state icon recolours, the ON accent unchanged. Circle already exposes every state's icon colour (the reference); metro signals state via the *tile background* (E141), so its icon stays the uniform `--feezal-metro-text`; eink is monochrome (inverts, no icon colour). *(Descriptor presence + muted defaults browser-tested in `test-browser/feezal-glass-icon-colors.test.js`.)*
- [ ] **Metro family (E55)** (metro-button/switch/light/climate/sensor/media/contact/gauge): flat accent tiles (`--feezal-metro-accent`, WP7 look with `feezal-theme-metro`); the `size` attribute (1x1/2x2/4x2/4x4) snaps the element to the 70px+10px mosaic grid — mixed sizes align; tiles with detail controls show the ⋯ affordance and **flip with the 3D Y-rotation** (front tap = base action, ⋯/outside tap flips back, instant under `prefers-reduced-motion`, flip state never published); front-only tiles (contact, plain button tile, gauge) have no ⋯; light/climate always flip (ON/OFF resp. setpoint stepper), extra controls gate on their topics. **switch/light**: whole tile toggles on tap, accent=ON / dark=OFF; light: `mode` (brightness/brightness+ct/ct/rgb/hs) picks the back sliders, `payload-mode: json` covers zigbee2mqtt single-topic lights (autodiscovery sets it via the schema), `on-off-source: brightness` covers HmIP/Homematic dimmers without an on/off datapoint — all identical to circle-light; **climate**: setpoint stepper clamps to min/max, mode chips; **sensor**: back trend polyline + min/max; **media**: front tap = play/pause, back transport + volume; **button** (renamed from `metro-tile` in E152 — see `docs/BREAKING-CHANGES.md`): publish and/or navigate-to-view on tap, live badge top-right; **switch/light** take per-state `icon-on`/`icon-off` (base `icon` as fallback); **occupancy** mirrors the Device occupancy card (circle-motion): payload-active/clear incl. JSON {state} + boolean coercion, type (motion/presence/radar/zone) picks the icon, icon-active/icon-clear + text-active/text-clear per state, availability ! badge, discovery maps device_class; **contact** — `type` visuals (window incl. tilt + mirrored handle, door, generic, garage — leak/fire dropped in E138), `payload-tilted`, availability `!` badge, `icon-open`/`icon-closed` overriding the visual, discovery maps `device_class` → type. (Metro contact keeps its own SVG visuals — it did NOT adopt the Circle disc.) *(Flip machinery, size grid, MQTT contracts incl. editor guards and capability gating are browser-tested: `test-browser/feezal-elements-metro.test.js`.)* **E136 (touch-first backs):** flip any metro tile — the back's controls are Metro flat segments (quiet fill, no borders, invert on hover/active, visible pressed-scale feedback), **nothing interactive below ~36 px**; metro-climate's setpoint stepper is the WP7-volume-style pair of **giant flat +/− halves (≥48 px)** with the value between them, mode chips are full-width segments; sliders (light/media/wled) have 6 px rails and 18×28 thumbs with a 32 px hit area. **Size-adaptive:** at **4x2** the back lays its zones **side-by-side** (climate: stepper left, modes right; assert with a thumb on a wall tablet — stepper and mode change operable at 2x2 AND 4x2); at 2x2 it stacks; **4x4** gets extra breathing room, not stretched 2x2 content. Fronts unchanged; MQTT contracts/attributes/flip machinery untouched. *(Stacked-vs-side-by-side layouts, ≥48/≥36 px hit targets, pressed-state declaration and the 32 px slider hit area are browser-tested.)* **E129 (size tokens):** every metro element's Style inspector lists `--feezal-metro-icon-size` (default 56px — noticeably bigger than the old 42px cap), `--feezal-metro-font-size-label` (13px), `--feezal-metro-font-size-value` (38px), `--feezal-metro-font-size-unit` (13px); setting them per element visibly resizes icon/label/value/unit text on front AND back faces; a theme override (e.g. `.feezal-theme-metro feezal-element-metro-* { --feezal-metro-icon-size: 64px }` — commented example in feezal-theme-metro) applies family-wide and the per-element style still wins over it; check the new larger defaults look right on 1x1/2x2/4x2/4x4 (small tiles stay capped by tile height via cqh). *(Descriptor presence family-wide + token-driven computed sizes are browser-tested.)* **E141 (per-state tile background vars):** every state-driven metro tile now exposes a **dedicated per-state background var** in its Style inspector, so the *primary* state no longer silently reuses the generic `--feezal-metro-accent`: **switch/light** `--feezal-metro-on-color` (+ existing `-off-color`), **contact** `--feezal-metro-closed-color` (+ `-open-color`/`-tilt-color`), **motion/sensor** `--feezal-metro-clear-color` (+ `-active-color`), **lock** `--feezal-metro-locked-color` (+ `-unlocked-color`/`-jammed-color`). Each primary-state var **defaults to `var(--feezal-metro-accent)`**, so an untouched dashboard (and one that sets `--feezal-metro-accent`) looks identical; set e.g. `--feezal-metro-on-color: #2e7d32` on one switch → only its ON tile recolours. *(Descriptor presence + accent defaults browser-tested in `test-browser/feezal-metro-state-colors.test.js`.)* (metro-climate/metro-cover have no per-state tile background today — a mode/position background would be a new visual, tracked separately.)
- [ ] **Glass badge tray (B91)** — every glass card's low-battery, availability and warning (fault/sabotage) badges plus the details (⋯/tune) button now sit in **one shared tray in the upper-right corner**, always in the fixed order **battery → availability → warnings → details** (left to right). Verify across cards that used to differ: a **contact** with low battery + offline + a fault/sabotage all show them clustered top-right in that order (was: warning top-right, battery bottom-right); a **light**/**wled**/**cover** show availability next to the details button top-right (was: availability bottom-right); a **climate** shows battery + availability + details top-right (was: details top-right, battery bottom-left) — no badge in a different corner on any card, none overlapping the details button, and the row does not reflow the icon/value when one appears/clears. Cards with only some indicators show just those, still in order and gap-free. The cover's direction arrow (top-left) and the meter's inline error text are separate and unchanged. *(The shared `glassBadgeTray` order + that each indicator renders into `.glass-badge-tray` (not a per-card corner) are browser-tested in `test-browser/feezal-glass-badge-tray.test.js`; the availability/battery parity still passes in `feezal-controller-parity.test.js`.)*
- [ ] **Boolean-card taxonomy (E138)** (material/glass/metro/eink): four functions per family — **contact** (ContactController), **sensor** = ALARM card (leak/smoke/gas/CO/vibration/tamper/generic; error-colour active default), **motion** (motion/occupancy/presence/radar/zone; accent-colour active default), **value** = numeric readout (glass/metro/material — `circle-value` mirrors `glass-value`'s contract 1:1: `subscribe`/`message-property`/`unit`/`decimals`/`icon`/`label`, big tabular numeral + unit, Circle chrome; eink now uses `eink-value` (E148)). Hard renames per the migration table in `docs/BREAKING-CHANGES.md` — glass/metro `occupancy` → `motion`, numeric `sensor` → `value`, and the `*-sensor` names are REUSED for the new alarm cards. Verify: each card's `type` select offers only its slice; discovery offers the right card per `device_class` (motion/occupancy/presence → Motion, smoke/moisture/gas/co/vibration/tamper → Sensor, door/window/garage → Contact); active colours default from the canonical theme vars (motion accent, alarm error, contact primary) with per-element overrides winning; **circle-sensor AND circle-contact carry the E134 state disc** (width-sized, top-anchored circle — no ellipse when the card is tall; circle-motion is SVG-only now); E124 battery + availability on all boolean cards. Sabotage badges land with E135. *(Slice vocab/routing/colour vars unit+browser-tested; parity covers all 8 boolean adopters; renames covered by the migration table.)*
- [ ] **Dialog placeholder labels (E142)** — dialogs are invisible ~120×40 px canvas pseudo-elements; several look identical. **`*-dialog` and `*-countdown-dialog`** (material/glass/eink/paper) now have an **editor-only `label`** attribute: set it (e.g. "Confirm delete") → the canvas placeholder reads **"Dialog: Confirm delete"** / **"Countdown Dialog: Reboot"**; empty → just the base word. The label is **never** shown in the viewer (open the dialog in the viewer — no trace of the label in the title bar or anywhere). It is **distinct from `title`** (the user-visible heading) — the inspector help says so. **`*-dialog-view`** elements deliberately have **no `label`** — their placeholder shows the **bound view name** instead: pick a view → the placeholder reads **"Dialog: &lt;view name&gt;"** (empty view → just "Dialog"). *(Helper + per-element placeholder/leak behaviour browser-tested in `test-browser/feezal-dialog-label.test.js`.)*
- [ ] **Meter cards (E147)** (`glass-meter` / `metro-meter` / `circle-meter` over the shared `@feezal/feezal-controller-aiedge`) — a purpose-built meter reader for [AI-on-the-edge](https://github.com/jomjol/AI-on-the-edge-device) but generic for any MQTT meter. **JSON mode**: wire **`subscribe-json`** to the device's `…/<seq>/json` topic → **value + unit** (big numeral, rounded to `decimals`), the **rate** line (↗, with `rate-unit`), the **reading age** ("3 min ago", refreshes on its own), and — when the payload's `error` is anything other than "no error" — an **error badge** with the fault text; all pulled from that one topic (field paths `message-property-value`/`-rate`/`-error`/`-timestamp`/`-raw`, defaulting to `payload.value` etc.). **Generic mode**: instead wire **`subscribe-value`** (+ optional **`subscribe-rate`**) for a meter that publishes plain topics. **`subscribe-status`** shows the current processing step / last action ("digitizing…"). **`stale-after`** (seconds): once the last reading is older, the age line turns to the error colour with a ⚠ (the device keeps its last value even when a read fails). **Availability** from the device's `…/connection` topic (payload-available default `connected`) — offline shows the family's unavailable badge. Toggles: `show-rate`/`show-status`/`show-timestamp`/`show-raw`. Discovery (E146) only wires the plain value sensor; this card is author-wired for the richer topics. Metro variant: value+rate on the front, status/age/raw on the flip back. *(Controller JSON/separate parsing, error/stale/timestamp logic + the three views rendering value/rate/error/status are unit+browser-tested: `test/feezal-controller-aiedge.test.js`, `test-browser/feezal-meter-cards.test.js`; the E137 parity test covers all three views.)*
- [ ] **Swipe navigation (E7)** (`system-swipe`) — a pseudo-element: invisible in the viewer, a small "Swipe" placeholder on the canvas (position/size irrelevant). Drop one on a multi-view dashboard, open the **viewer on a touch device / touch-emulation**: a horizontal swipe left → next view, right → previous; **`direction: vertical`** switches to up/down. Past the last view it **wraps** to the first (unless `wrap` off). Below **`threshold`** px, or a clearly **diagonal** gesture (over scrollable content), does nothing — it doesn't fight scrolling. A **mouse drag never navigates** (touch pointers only). `views` restricts/orders the cycle (empty = all views in order). With `animate` on, the incoming view slides/fades in briefly (off under `prefers-reduced-motion`). *(Gesture thresholding, axis, wrap, explicit-order, the touch-vs-mouse gate and the editor placeholder are browser-tested in `test-browser/feezal-system-swipe.test.js`.)*
- [ ] **Device-health board (`basic-device-health`)** — the wall-panel "is everything okay?" list, now **cross-ecosystem** (Homematic, zigbee2mqtt, ESPHome, Tasmota, …). The device list is resolved **at edit time**, not by runtime wildcards:
  - **Custom inspector (the checklist):** select the element → the inspector shows a **checklist of every discovered device** that reports a battery or availability signal (from `/api/discovery/devices`), grouped by source (hm / zigbee2mqtt / …), each row showing which signals it has (battery / availability / fault / sabotage icons). Newly placed board: **all devices watched by default** (the list is stamped immediately). **Uncheck** a device to exclude it; **All / None**; the **↻** button re-scans discovery (a device added later appears only after a re-scan). Title + show-battery / show-unreach / show-ok live at the top of the inspector. Friendly names match what linking a discovered device stamps (`friendlyName`: `licht_hobbyraum` → "Licht Hobbyraum", HM channel `:N` stripped).
  - **Viewer:** it subscribes only to the stamped topics (no discovery/name-derivation at runtime). Trigger signals: **low battery** — z2m `battery_low` / HM `LOWBAT` (**boolean only** — a percentage-only battery sensor is deliberately not listed); **unavailable** — z2m/ESPHome/Tasmota availability offline, or HM `UNREACH`; **Homematic fault** (`FAULT_REPORTING` ≠ 0 → decoded "Communication error" etc.) and **sabotage** (`ERROR == 7` / HmIP `SABOTAGE`) as before, warning-tier / alarm-grade. Rows sort by severity (sabotage → fault → battery → unreach); an issue **clears** when its signal returns to OK; toggles hide battery/unreach; with nothing wrong and `show-ok` on, "All devices OK". Icons render as glyphs (feezal-icon), not ligature text. *(The decoder is unit-tested in `test/feezal-hm-fault.test.js`; the pure list builder `buildHealthDevices`, the unavailability helper, and the list-driven viewer in `test-browser/feezal-device-health.test.js`.)*
- [ ] **Sabotage / fault badges (E135, part 2)** — the **contact / motion / (alarm) sensor** cards (circle/glass/metro/eink) now show maintenance badges. **Sabotage** (case-opened/tamper) is **alarm-grade** (error-colour shield): on a Homematic window/door contact whose case is opened it appears (classic HM encodes it as `ERROR == 7`; HmIP as a `SABOTAGE` bool). **Fault** shows a warning-tier triangle with the decoded text in a tooltip when `subscribe-error` is wired. Both are **auto-wired by discovery**: a Homematic contact/motion sensor seen publishing `ERROR` (or `:0 SABOTAGE`) gets `subscribe-sabotage` (+ encoding) auto-stamped, presence-checked like the E124 battery record — verify by picking such a device in the ⚡ picker and confirming the attributes stamp, then opening the case → the alarm badge shows and clears when reset. `circle-lock` already shows the Keymatic `ERROR` fault (E144). **Climate stuck-valve fault:** a Homematic TRV (`HM-CC-RT-DN`) reporting a `FAULT_REPORTING` ≠ 0 shows a **warning fault badge** on the `*-climate` cards with the decoded text ("Communication error" / "Valve mounting error" — the enum is [OpenCCU-Base](https://github.com/homematicip/OpenCCU-Base)-verified on the CLIMATECONTROL_RT_TRANSCEIVER channel) **while the setpoint stays fully operable**; auto-wired by discovery. *(Recognizer emission (contact + motion sabotage both encodings, TRV FAULT_REPORTING), the auto-stamp, and the badge render — sabotage alarm + decoded contact/climate fault text — are tested: `server/test/native-discovery-e135.test.js`, `test-browser/feezal-sabotage-badge.test.js`, `test/feezal-discovery-stamp.test.js`.)* *(HmIP-eTRV named-flag valve faults remain verify-gated — see the E135 archive.)*
- [ ] **evcc integration (E109)** — [evcc](https://evcc.io/) (EV-charging / energy manager) publishes a flat, retained MQTT tree with **no** HA discovery, so a **native recognizer** (server, structure-detected on `<root>/site/grid/power` + `<root>/site/battery/soc`; the root prefix is user-configurable) synthesizes: one **energy-flow site entity** and one **loadpoint entity per loadpoint**, with availability from the `<root>/status` LWT. **Discovery**: with evcc on the broker, the ⚡ picker / Generate wizard offer the entities — select a `material-energy-flow` element and pick the site entity (wires solar/grid/home/battery + the EV node, **inverting battery sign** since evcc battery is +=discharge); select a `*-loadpoint` card and pick a loadpoint. **Energy-flow (E109 EV node)**: `material-energy-flow` now has a **Car node** — wire `subscribe-charge` and the car node appears with a House→Car flow; `invert-battery` flips the battery sign; the battery/car nodes auto-show when their topic is set. **Loadpoint cards** (`glass-loadpoint` / `metro-loadpoint` / `circle-loadpoint` over `@feezal/feezal-controller-evcc-loadpoint`): **mode buttons** Off / Solar / Min+Solar / Fast publish to `…/mode/set` (tap → evcc switches mode); **charge power** (kW), **vehicle SoC** bar toward the **limit** marker, **session energy**, **connected/charging** status. A **heat-pump loadpoint** (`heating` — auto-set by discovery from `chargerFeature/heating`) shows a **reduced °C view**: current temperature (from `vehicleSoc`) + target °C (from `limitSoc`) + power + mode, no car SoC/limit controls. Verify a mixed setup (a wallbox **and** a heat pump) shows both correctly. **Metro variant (E153):** the front is the readout only — status, big charge power, vehicle SoC → limit. The four charge-mode buttons live on the **flip back as a 2×2 grid** (touch-friendly targets, the active mode inverted), with the session / vehicle / current detail rows below them. Verify: the front carries **no** mode buttons; ⋯ flips; each of the four back buttons publishes to the mode-set topic and marks itself active when the mode topic echoes back; at 4x2 the grid and the detail list sit side by side. evcc has always worked with manual wiring; discovery + these cards make it one-click. *(Native recognizer, the loadpoint controller and all three cards + the energy-flow EV node/invert are unit+browser-tested: `server/test/native-discovery-evcc.test.js`, `test/feezal-controller-evcc-loadpoint.test.js`, `test-browser/feezal-loadpoint-cards.test.js`; the E137 parity test covers the three loadpoint cards.)*
- [ ] **Badge-render parity (B67)** — every device element that *accepts* a badge attribute actually *draws* the badge (the fix closed gaps where the attribute was auto-stamped by discovery but never rendered). Spot-check: **circle-climate** low battery → the tiny battery symbol shows **top-left** (above the button row, not overlapping); **metro-climate / metro-switch / metro-light** with `subscribe-availability` wired → a `!` badge appears top-right while the availability topic reports offline and clears when it reports online. **`*-value` availability (opt-in)** — `circle-value`, `glass-value`, `metro-value` show an availability badge **only once `subscribe-availability` is configured** (per family: circle SVG top-right, glass `⚠` top-right, metro `!`); the last value stays visible underneath; with no availability topic set, no badge ever appears. *(An anti-regression browser test in `test-browser/feezal-controller-parity.test.js` drives real MQTT messages and asserts each element that declares `subscribe-battery-low`/`subscribe-availability` renders the badge — so a future element can't declare-but-not-render silently.)*
- [ ] **Circle design-language pass (E139)** (material sensor/contact/motion/value/lock/cover): every Circle device card now presents one width-sized, top-anchored **circle** (ring or disc) concentric with the light/climate ring, with control/label rows stacked below. Verify:
  - **Alignment**: put a Circle Light, Climate, Sensor, Contact, Motion, Value and Lock in a row at the same height → all their circles share the same centre and top edge (the sensor/contact discs no longer sit higher than the rings). Discs are ~90% of the ring diameter (a hair inset), and stay true circles — never an ellipse — on tall cards.
  - **Motion**: the PIR / presence / mmWave-radar / zone artwork now sits *inside* a disc with a state word below it ("Motion"/"Clear", or `text-active`/`text-clear`); the disc frame tints to the accent colour while motion is detected.
  - **Lock**: the padlock sits inside a disc that tints by state (unlocked → accent, jammed → error, locked → neutral); the Lock/Unlock buttons and state word stay below the disc.
  - **Value**: always a disc now. `mode=readout` (default) shows the numeral — with the optional `icon` above — inside a ring; `mode=fill` turns it into a liquid gauge that fills from the bottom between `min`/`max`, with `ranges` colour bands (JSON `[{"from":18,"color":"#4caf50"},…]` — each band colours its value span, revealed up to the waterline) and `ticks` horizontal graduation lines (0 = none). Editor shows the 21.5 sample; an unconfigured fill sits at that level. `--feezal-value-fill-color`/`-track-color` restyle the fill and ring.
  - **Cover**: a new `visual` attribute (Config → Display → "Circle visual") selects **ring** (a position arc like the light/climate cards — default), **fill** (a disc filling from the bottom), or **blind** (the slatted window clipped to a circle). The **ring** visual carries a **drag knob on the arc** and behaves exactly like the light brightness ring / climate setpoint arc — drag the knob (or anywhere on the ring) to move the cover, position updates live and publishes on release; `--feezal-cover-track-width`/`--feezal-cover-knob-size` restyle it (same unitless scale as the light/climate knob). The **fill** and **blind** visuals use a vertical drag (drag up = open) instead. All three keep the up/stop/down buttons, position %, optional tilt slider and label below; `invert` and `slat-count` still apply (`slat-count` only affects the blind visual). Editor never publishes/drags. Existing saved covers default to the ring visual (their look changes; switch to "blind" to keep the slatted window).
  - **Ring-colour consistency**: the neutral disc ring on **every** Circle card (sensor/contact/motion/value/lock) is now the same colour — `currentColor` at 25% (anchored to the card's text colour). Verify lock and value no longer show a differently-tinted ring than their neighbours on a dark AND light theme.
  - **Value numeral size**: the readout numeral default shrank (`--feezal-value-font-size` now `18cqi`) so a value like `22.0` fits the disc without truncation; it's exposed as the `--feezal-value-font-size` Style-inspector prop (cqi or px) — bump it and the numeral grows.
  - **Switch on/off colours (E139, `material-switch`)** — the toggle now reads **muted when OFF** (transparent track + grey thumb) and **active/accent when ON** (accent-filled track), i.e. the active colour marks the ON state, not OFF. A **thin active-colour ring stays around the track in BOTH states** (drawn by feezal, not MD3 — MD3 only outlines the unselected track) as the "this is clickable" affordance. Verify on **midnight-blue** (and a light theme): OFF is muted with a blue ring, ON is a solid blue track with the ring still present; `--feezal-switch-ring-color`/`-ring-width` restyle the ring, `--feezal-switch-track-on`/`-track-off` the fills. (`--feezal-switch-outline-off` still works as a back-compat alias for the ring colour.)
  - **Circle Gauge (E139, `circle-gauge`)** — a NEW Circle-family gauge (palette **Circle → Gauge**, distinct from the plain **Material → Gauge** `material-gauge`). Drop one, wire `subscribe` to a numeric topic, set `min`/`max`. **`look`** selects three styles: **arc** (270° fill arc), **ring** (full 360° progress ring), **needle** (analogue dial with a pointing needle). **`ticks`** (major divisions) + **`minor-ticks`** (between majors) + **`tick-labels`** (numeric scale labels) draw the scale. **`ranges`** (JSON `[{"from":0,"color":"#4caf50"},{"from":90,"color":"#e53935"}]`) — in arc/ring the fill takes the band the value sits in; in needle the scale is drawn as coloured zones. `unit`/`decimals`/`show-value` control the centre readout; `label` the caption. All colours + the track/value sizes are `--feezal-dial-*` Style props. Editor shows a mid-scale sample when unwired. *(Element mounts via the smoke suite; visuals are a manual check.)*
  *(Disc geometry, the value fill/ranges/ticks and the cover visual switch are visual checks; the MQTT wiring is unchanged and stays covered by the shared controller suites.)*
- [ ] **Gauge parity (E151, `glass-gauge` + `metro-gauge`)** — the Glass and Metro families now have the dial card the Circle / Panel / Material families already had, over the shared `@feezal/feezal-gauge` package (`circle-gauge` was refactored onto it, so all three derive the identical geometry — same value must land on the same needle angle in every family). Drop each, wire `subscribe` to a numeric topic and set `min`/`max`: `look` (**arc** 270° fill / **ring** 360° progress / **needle** analogue dial), `ticks` + `minor-ticks` + `tick-labels`, `ranges` colour bands, `unit`/`decimals`/`show-value`, and the `--feezal-dial-*` Style props. **glass-gauge**: the dial sits inside the frost card — frost tint, `degrade` solid fallback, squircle corners, `size` presets (2x2 / 2x1), label under the dial, and the `⚠` availability badge top-right once `subscribe-availability` is wired; the fill defaults to `--feezal-glass-accent`. **metro-gauge**: a flat accent tile on the `size` mosaic grid — the dial takes the tile palette (fill/needle/value in `--feezal-metro-text`, a quiet wash of it as the track) so it reads on any accent colour; front-only (no ⋯ flip); the `!` availability badge top-right. Check both against a **dark AND light theme** and with the family theme active (`feezal-theme-glass` / `feezal-theme-metro`), and that a `ranges` band still overrides the family fill colour. Both are `component: 'sensor'` — verify the ⚡ picker and the Generate wizard offer them for a numeric `sensor` entity, and that switching family between `circle-gauge`/`glass-gauge`/`metro-gauge` carries the config. Editor shows a mid-scale sample when unwired. *(Needle-angle mapping, arc/ring fill length, colour bands, tick geometry, clamping, the live-canvas rewire and the cross-family angle parity are browser-tested: `test-browser/feezal-gauge-cards.test.js`; visuals are a manual check.)*
- [ ] **Multivalue cards (E165)** (glass/metro/circle/eink-multivalue, all views over `@feezal/feezal-controller-multivalue`): one card showing SEVERAL values of a multi-datapoint device. **Config:** the `values` list editor — per value `property` (dot-path), `label`, `unit`, `decimals`, `role` (stack primary/secondary), `row`+`col` (grid), optional per-value `topic` (empty = the element's `subscribe`, the z2m one-JSON-payload case). **stack layout:** the `role: primary` value renders big like the family's `*-value` card with the smaller secondaries ABOVE it (temp big, humidity small); no role marked → first entry is primary. **grid layout:** values pivot into a table by row/col — column headers across the top, row headers left (with the unit as suffix when a whole row shares one); check all four families render it, incl. the CIRCLE disc (deliberately full parity — cqi-scaled to fit the round card). **Template generator:** set `grid-rows: power, voltage, current` + `grid-cols: a, b, c` + `grid-pattern: payload.{row}_{col}` on a card with an EMPTY values list → the list fills once (9 entries, layout flips to grid) and stays hand-editable afterwards (edits are never re-clobbered; clear the list to re-expand). **Wiring:** N values on one topic open exactly ONE MQTT subscription; a per-value topic opens its own; live config edits rewire on the canvas. **eink:** republishing unchanged values must not touch the DOM (redraw signature = the display strings). Unconfigured cards preview a sample on the editor canvas only. **Discovery — ⚡ device-fill:** select a multivalue card → the picker lists **devices** (E161 groups with ≥2 numeric sensors, "Name — N values"), not single sensors; one pick fills the whole values list (labels from entity names, units, `message-property` from value_templates, one shared state_topic collapsed to the element `subscribe`) — a 3-phase meter (`power_a/b/c` × `voltage_a/b/c`) lands as a **grid**, a temp+humidity sensor as a **stack with temperature primary**. **Discovery — Generate auto-merge:** a device with ≥2 numeric sensors generates as **ONE multivalue card by default** (Devices flow AND App wizard); its rows in the device list / review carry a clickable **combined** badge — click → **split** (that device generates individual value cards again; click again to re-combine). Families without a multivalue element (material/basic/…) are never merged. Dupe-guard: the merged card carries `mv:<deviceId>` plus the member ids in `discovery-ids`, so re-running Generate skips it in both merged AND split form. **Availability**: one device = one availability — a merged/device-filled card auto-stamps the device's availability record (LWT badge appears when the device goes offline; a device without a record stamps nothing). **Per-role colour ranges**: primary and secondary value colours are separate style knobs — glass `--feezal-glass-value-color`/`--feezal-glass-secondary-color`, metro `--feezal-metro-value-color`/`--feezal-metro-secondary-color`, circle `--feezal-value-text-color`/`--feezal-value-secondary-color` — assign each its OWN named colour range in the style inspector (e.g. temperature bands on the primary, humidity bands on the secondary) and verify they tint independently; grid cells follow the primary var; **eink deliberately has NO colour knobs** (1-bit family). *(Stack/grid rendering, topic dedupe, per-value override, template expansion incl. the no-reclobber and viewer-never-writes guards, and the editor sample are browser-tested in `test-browser/feezal-multivalue.test.js`; merge groups, grid derivation, primary preference and the fill stamping in `test/feezal-discovery-stamp.test.js`; the attribute contract in `feezal-controller-parity.test.js`.)*
- [ ] **Eink family (E57)** (eink-button/value/sensor/contact/climate/switch/light/cover/fan/wled/dialog/dialog-view/countdown-dialog + `feezal-theme-eink` + shared `@feezal/feezal-eink`): pure 1-bit black-on-white cards for e-paper wall panels and repurposed tablet kiosks — verify **no greys carry information**, borders/rules are ≥2px, numerals are oversized, and there is **zero animation/transition anywhere** (state changes swap content instantly; check with a slow-motion screen recording or the browser's animation inspector — nothing should tween, incl. dialogs). **Theme:** `feezal-theme-eink` sets a white canvas, black mono type and a global no-animation rule; the elements enforce their own contrast so they stay 1-bit under any theme; inverted panels: flip `--feezal-eink-fg`/`--feezal-eink-bg` in a site style. **Redraw discipline (the family's core engineering):** every card implements `renderSignature()` on the shared `EinkBase` — republishing an UNCHANGED value must not touch the DOM (e-ink partial refresh ghosts). Verify: wire eink-value to a topic, publish the same value repeatedly with `decimals` set → the DOM mutates only when the ROUNDED display string changes (watch with DevTools "paint flashing" or a MutationObserver snippet); config/inspector edits always re-render. **Contracts mirror glass:** value = glass-value wiring (subscribe/message-property/unit/decimals); sensor/contact/climate consume the E137 controllers (same attributes as every other family, incl. the E124 battery quartet — the shared tiny low-battery symbol renders bottom-right but **inherits the e-ink ink colour** (stays 1-bit monochrome, not the warning amber), `!` unavailability top-right); button/switch/light/cover/fan/wled/dialog/dialog-view/countdown-dialog mirror their glass siblings' attribute names (light: any discovered color mode degrades to brightness — no color UI on 1-bit; cover: tilt attrs declared but no tilt UI; wled: presets/brightness only, no effects/palette UI). **Active states invert** (black card, white text) — sensor active, contact open/tilted, switch/light/wled ON. Discovery: a z2m light / native hm climate / contact offered to the eink cards stamps identically to the glass siblings. Dialogs render as solid white panels with thick black borders (no blur). *(Per-element wiring rides the shared controller/browser suites; the eink smoke coverage comes from `element-smoke.test.js` mounting all packages.)*
- [ ] **TUI family (E59)** (tui-value/checkbox/menu/sparkline/log/ascii/panel/crt): monospace phosphor look shared via `--feezal-tui-color/bg/glow/font` (green default; retheme once for all); pair with the `feezal-theme-tui` theme for the full console page. **value**: dot leaders stretch with the element width, cursor block blinks ~2 s after each message (steady under `prefers-reduced-motion`); **menu**: digit hotkeys work with the menu focused, entries flash on activation; **sparkline**: block characters follow min/max or auto-scale; **log**: MQTT wildcards (`+`/`#`) feed it, buffer scrolls to the newest line; **ascii**: unsupported characters render blank (digits, `:.%-°+`, small letter set); **panel**: the box-drawing frame closes cleanly at any element size / font-size and the embedded view renders live in the viewer; **crt**: purely decorative overlay — click-through in viewer, selectable in editor, flicker stays off under `prefers-reduced-motion` and is off by default. *(MQTT binding, publish contracts incl. editor guards, hotkeys, block/segment rendering, wildcard log, frame geometry and CRT layers are browser-tested: `test-browser/feezal-elements-tui.test.js`.)*
- [ ] **Panel family (E56)** (panel-led/switch/value/gauge/knob): transparent host background — the instruments carry their own faces (style the element `background` if a console plate is wanted); `--feezal-panel-bezel/text` (+ `--feezal-panel-face` on gauge dial / value window) retheme the family; check on a light theme. **knob**: drag-to-turn feels right on touch (finger angle tracks, no page scroll), detents snap, wheel + arrows step, editor canvas drag never turns the knob; `start-angle`/`sweep-angle` reshape the throw, `ticks` configurable. **switch**: lever visible, points at the active position and snaps with overshoot; `direction` places OFF (down/up/left/right — horizontal layout for left/right), `label-on`/`label-off` engrave the markers, `guard` cover must be opened first and auto-closes after 4 s; **gauge**: needle springs (overshoot + settle) instead of jumping, zones render as coloured bands, `start-angle`/`sweep-angle`/`ticks`/`minor-ticks`/`tick-labels` reshape the dial, `show-value` + `value-prefix`/`value-suffix` + `--feezal-panel-gauge-value-size/color` control the readout; **value** (`panel-value` — a seven-segment LED readout, renamed from `panel-7seg`): value right-aligned with ghost segments, decimal point, minus, `Err`-style letters; the `88.8` **editor placeholder shows even once a subscribe topic is set** (a configured display isn't blank on the canvas); autodiscovery works — the ⚡ picker offers `sensor` entities and the Generate wizard resolves a `sensor` to it (function `value`); `--feezal-panel-value-color` sets the lit-segment colour; **led**: `states` list maps payload→colour with blink/blink-fast. *(State binding, publish contracts incl. editor guards, guard cover, detents/wheel/keys drag math, dial geometry/tick/readout options, segment/zone geometry, the lever-rect regression and the needle spring are browser-tested: `test-browser/feezal-elements-panel.test.js`.)*
- [ ] **WLED (E103)** (circle-wled / glass-wled / metro-wled, identical contract): set `topic` to the device base topic → state flows from `<topic>/g` (0–255 → %, 0 = off) and `<topic>/c` (colour swatch); all commands go to `<topic>/api` as `/json/state` JSON (on/off, `bri`, `seg.col`, `seg.fx`, `seg.pal`; `transition` attribute in seconds appended as 0.1 s units); effect/palette pickers use the bundled WLED 0.14 name lists (ids beyond the lists show numerically); availability auto-derives `<topic>/status` when `subscribe-availability` is empty — an explicit value wins; per family: material card with power/brightness/colour/pickers, glass tap-toggle + details popup, metro flip tile. Editor never publishes; topic changes rewire live. **Feature pass (2026-07):** circle-wled's brightness is a circular ring slider with the exact circle-light geometry (centre tap toggles, track drag publishes `bri`; `--feezal-wled-track-width`/`--feezal-wled-knob-size` style vars); all three have effect **speed/intensity** sliders (0–100 % → `seg.sx`/`seg.ix`, display defaults 50 % — opt-in read-back via `subscribe-speed`/`-intensity`), `show-effect` (off hides effect + both sliders) and `show-palette` toggles, immediate colour publish while dragging the picker (~100 ms debounce, plus the close event), and an optional preset selector (`show-presets` + `presets` `{id,name}` list, numeric input fallback) publishing `{"ps":id}`. **B38:** the effect/palette/preset dropdowns must be readable in light AND dark themes — check the closed control *and* the open option list on each family (selects carry `color-scheme` + solid option backgrounds from the family's tokens). *(Contract, auto-derive, guards, list fallback, ring geometry/vars, sliders, toggles, debounce, presets and the B38 select theming are browser-tested.)*
- [ ] **Glass wide-tile row layout (E105)** (glass button/switch/contact/sensor/light/cover/occupancy/climate/fan): resize any card to at least twice as wide as tall → the layout flips to icon LEFT with state/label stacked right of it (Apple-Home wide-tile style), live while resizing; back under 2:1 → the vertical stack returns; overlay chrome (⋯/tune, ⚠ badge) keeps its corners; popups unaffected; check a very flat card (e.g. 320×80) has no overflow. Family-wide 2:1 breakpoint — mixed dashboards flip consistently. *(Grid/flex switching is browser-tested for three cards.)*
- [ ] **Glass typography = fixed px vars (E106)**: glass cards no longer auto-scale type with container-query units — fonts/icons are **fixed px** exposed as `--feezal-glass-icon-size` / `--feezal-glass-font-size-state` / `-label` / `-value` / `-unit` (Style inspector). Check a **2×1 tile (172×64)** reads correctly (type is no longer oversized as it was with `cqmax`), and a **2×2 (172×128)** looks right; set a var (e.g. bump `--feezal-glass-font-size-state`) → all cards using it follow. **Size-preset gap fix (E106)**: **glass-light** and **glass-cover** now show the **size** (Auto / 2×2 / 2×1) select in their custom inspectors (it was declared but unreachable before). *(Glass unit + browser suites green; visual sizing wants a manual pass.)*
- [ ] **Glass dialogs (E101)** (glass-dialog, glass-dialog-view, glass-countdown-dialog): attribute-identical mirrors of the material trio in frosted-glass chrome — verify open/close via MQTT payloads, OK/Cancel publishes, B25 header (✕/show-close/hide-header incl. live change while open), B24 sizing (min-height keeps an open dialog tall), dialog-view embeds the selected view live (MQTT bindings work inside; self-embed guarded), countdown ring auto-confirms at zero with amber/red warn thresholds; `degrade` swaps the blur for a solid panel (incl. the body portal); editor shows placeholders and never publishes. *(All contracts incl. portal sync are browser-tested.)*
- [ ] **Metro cover (E104)** (metro-cover): circle-cover contract 1:1 (json/separate, B26 min/max + slat ranges, dedicated up/stop/down topics) in Metro chrome — front face shows the position % with a flat fill (no slats, `--feezal-metro-cover-fill`); tap flips to the back: position slider, Up/Stop/Down, tilt slider only when tilt topics configured; `size` mosaic snapping; availability `!` badge via the N31 base; editor never flips/publishes. *(Contract incl. scaling, precedence, json inference and guards is browser-tested.)*
- [ ] **Glass fan (E100)** (glass-fan): mirrors circle-fan's contract (separate topics; subscribe/payload-on/off, speed with `speed-range-min/max` scaling, presets via `preset-modes` + `publish-preset`). Fan icon spins **in place** while on (speed-proportional, still under `prefers-reduced-motion`) — **B93:** the default icon is the vendored `toys` pinwheel (the old `mode_fan` was not a vendored glyph, so it rendered a broken fragment that orbited instead of spinning; the same fix applies to circle-fan / eink-fan palette icons); state line "On • 60 %"; tap toggles; long-press/tune opens the glass popup with the vertical speed pill (publishes on release) and preset chips; availability badge bottom-right via the N31 base; editor never publishes; inspector-set topics rewire live. *(All of this is browser-tested.)*
- [ ] **Base-class availability (N31)** (all glass/material/metro device cards): single-topic `subscribe-availability` behaves exactly as before (badge on `offline`, gone on `online`, JSON `{"state":…}` payloads unwrapped); NEW multi-topic form — set `subscribe-availability` to a JSON array (e.g. `[{"topic":"zigbee2mqtt/bridge/state","property":"payload.state"},{"topic":"zigbee2mqtt/dev/availability","property":"payload.state"}]`) → with `availability-mode` `all` (default) the badge appears when ANY topic reports offline, with `any` only when all do; changing the topics via the inspector rewires live; the `unavailable` host attribute appears on the element in the VIEWER only (check the editor never serialises it into views.html). **Discovery**: dropping a zigbee2mqtt-discovered device onto any element auto-wires the availability array incl. per-entry `payload.state` paths — no element-specific mapping involved. *(Base machinery incl. modes/rewire/editor-gating and the server-side normalization are unit/browser-tested.)*
- [ ] **Light state labels (E99)** (glass-light, circle-light): set `label-on`/`label-off` on glass-light (e.g. "Ein"/"Aus") → the card's state line shows them, brightness keeps appending ("Ein • 60 %"); circle-light's `label-off` replaces the centre "off" text. Defaults unchanged (On/Off resp. off); distinct from `payload-on/off` (MQTT) and `label` (card title). *(Glass label rendering is browser-tested.)*
- [ ] **Climate per-entry mode descriptors + boost (E102)** (circle-climate, **glass-climate**, **metro-climate** — same contract in each family's mode UI: material chips, glass popup buttons, metro flip-face chips): a plain `modes` entry still publishes its value on `publish-mode`. Add a per-entry **`publish`+`payload`** → clicking that mode writes `payload` to that topic (string, or a **JSON object** published as-is for a putParamset topic like `hm/paramset/WTH:1/VALUES` → `{"CONTROL_MODE":1,"SET_POINT_TEMPERATURE":4.5}`); **`$setpoint`** in a payload resolves to the last real setpoint received (so Homematic `MANU_MODE=$setpoint` sends the remembered temp, and "off" sends 4.5). A **`"momentary":true`** entry (boost) renders as a push button: first tap activates (publishes it), second tap deactivates and runs `"off":{"publish","payload"}` (HmIP `BOOST_MODE=false`) or `"off":"restore"` (BidCoS — re-publishes the pre-boost mode). **Match-setpoint-max display rule:** an entry may carry a numeric `"match-setpoint-max"` (e.g. `4.5`) — a display/selection rule only (never a write rule). When several entries share the same mode read-back value (e.g. `Manu {value:1}` + `Off {value:1,"match-setpoint-max":4.5}`), the entry with `match-setpoint-max` shows **active** only while the **current setpoint read-back** is `<=` that number (B53: the current value, NOT the remembered pre-Off setpoint), and it wins over the plain entry (more specific); above it the plain entry (`Manu`) shows active. Only one chip/button highlights. Entries without `match-setpoint-max` behave exactly as before. Verify against a real Homematic TRV/wall thermostat (both generations) if you have one. *(Per-entry write, $setpoint, object/putParamset payload, the boost toggle + both off-strategies, and the match-setpoint-max active-chip selection are browser-tested.)*
- [ ] **Climate valve scaling (E102)** (circle-climate, **glass-climate**, **metro-climate** — WP1 brings the valve to all three, identical `subscribe-valve` / `message-property-valve` / `valve-min` / `valve-max` attribute contract, purely additive): with `subscribe-valve` set and the defaults (valve-min 0 / valve-max 100), a Homematic BidCoS `VALVE_STATE` of `73` shows a 73 % valve indicator unchanged; set **valve-max = 1** (Valve section) for an HmIP `LEVEL` device → an incoming `0.5` shows 50 %, `1` shows 100 %; out-of-range values clamp to 0–100 %; the same scaling applies when the valve is read from a JSON payload (`json-map` `valve` key → default `position`; **metro** has no JSON mode, separate topic only). **material** shows a subtle inner valve arc + a "Valve" bar; **glass** shows a subtle valve mark on the setpoint pill plus a numeric "Valve x %" line in the details popup; **metro** shows a flat bar + "Valve x %" readout on the flip-face (back). **Purely additive** — nothing valve-related renders while `subscribe-valve` is unset (`_valve` stays null); existing cards are unchanged. *(Separate + JSON scaling, clamping, the zero-span guard, hidden-when-unset and per-family rendering are browser-tested for all three.)*

- [ ] **Native climate + WLED self-discovery (E108)** (circle-climate, **glass-climate**, **metro-climate**; circle-wled, **glass-wled**, **metro-wled**): the E102 WP3 client-side profile-stamping picker is **removed** — device wiring now comes from the server's native self-discovery, consumed through the SAME generic ⚡ device picker / **Auto-configure** banner that HA-discovered lights already use. The server synthesises discovery entities from native MQTT topics and merges them into `/api/discovery/devices`; Homematic thermostats appear as `component:'climate'` entities and WLED strips as `component:'wled'`. **Climate** — select a `*-climate` element → the ⚡ picker (above the inspector) lists matching Homematic entities; picking one, or typing a matched topic and clicking the **Auto-configure** banner, stamps the wiring attributes (`subscribe`/`subscribe-mode`/`subscribe-valve`/`publish-*`/`min`/`max`/`step`/`unit`/`modes`/`label`), plus the native-only keys **`message-property=payload.val`** (MQTT-Smarthome JSON-Extended value path) and the valve range (`valve-min`/`valve-max`: BidCoS 0–100, HmIP 0–1). A rich `modes` array round-trips (JSON-stringified into the attribute). Every stamp is undoable and every attribute is plain/hand-editable afterwards. **WLED** — select a `*-wled` element → the ⚡ picker lists discovered WLED devices; picking one stamps `topic` (device base) + `label`, and availability wires automatically (`subscribe-availability`/`payload-available`/`payload-unavailable`) from the entity's normalized availability record. **HA/z2m parity** — the added native keys are HA-absent, so HA/zigbee2mqtt climate entities simply skip them (additive, backwards compatible). **Homematic availability** auto-wires from the device's `:0` maintenance `UNREACH` datapoint (`subscribe-availability` → `hm/status/<device>:0/UNREACH` with `payload-available=false`/`payload-unavailable=true`, since `UNREACH=true` means unreachable). **Grouping:** one entry per physical device (`hm: <deviceName>`), resolved to the control channel via the CCU `channelType` metadata — a room with a wall thermostat + TRV + `HmIP-HEATING` group shows all three (distinct devices). **Contact** — select a `*-contact` element (material/glass/metro) → the ⚡ picker lists Homematic window/door contacts. The server promotes them as `component:'binary_sensor'` from `hm/status/<seg>/STATE`, gated on the CCU `channelType` so only real contacts appear (a `SHUTTER_CONTACT` or `ROTARY_HANDLE_TRANSCEIVER`, never a switch's generic `STATE`; a `STATE` with no `hm` metadata is skipped). Picking one stamps `subscribe`/`type=window`/`message-property=payload.val` plus the payload semantics: **SHUTTER_CONTACT** → `payload-open=true`/`payload-closed=false` (boolean STATE, no tilt); **ROTARY_HANDLE_TRANSCEIVER** → `payload-closed=0`/`payload-tilted=1`/`payload-open=2` (tristate STATE — the `0`/`1`/`2` mapping is an **assumed** CCU convention, adjust if a real handle differs; the rotary handle renders the tilted icon on `1`). **Homematic contact availability** wires from the device's `:0` maintenance channel `UNREACH` datapoint only (`payload-available=false`/`payload-unavailable=true`); battery is **no longer folded into availability** — a device seen publishing `LOWBAT`/`LOW_BAT` on `:0` gets a dedicated `battery_low_normalized` record that stamps the E124 battery attributes instead (see the E137/E124 contact row below). Verify against a real Homematic contact/rotary handle if you have one (device_class `window` + the tristate values are the assumptions to confirm). **Cover** — select a `*-cover` element (material/glass/metro) → the ⚡ picker lists Homematic blind/shutter actuators. The server promotes them as `component:'cover'` from `hm/status/<seg>/LEVEL`, gated on the CCU `channelType`: a **BidCoS `BLIND`** channel → one cover (any channel index); an **HmIP `BLIND_VIRTUAL_RECEIVER`** actuator exposes its outputs as virtual-receiver channels grouped in consecutive triples (the user's HmIP convention: 1 output = 3 channels) — the server sorts them by `channelIndex`, chunks into 3s and wires each cover to the **first (lowest-index) channel of each triple** (a 12-channel device → 4 covers keyed `…:g0/:g1/:g2/:g3` with leaders at index 1/4/7/10; a stable per-group `discovery_id` keeps the retained-message burst from leaving stale duplicates as channels arrive). A `LEVEL` whose `channelType` is not a cover type (TRV valve, dimmer) or has no `hm` metadata is skipped. Picking one stamps the **separate-mode** wiring — `payload-mode=separate`, `subscribe-position`/`publish-position` = `hm/status|set/<seg>/LEVEL`, `publish-stop` = `hm/set/<seg>/STOP`, **`min=0`/`max=1`** (Homematic reports LEVEL 0.0–1.0 → max 1 scales it to 0–100 %, no server scaling), and `message-property`/`message-property-position` = `payload.val` — plus `:0` `UNREACH`-only availability (covers carry no LOWBAT). Verify against a real Homematic blind (both generations) if you have one; the group-of-3 leader rule and the STOP datapoint name are the assumptions to confirm. **Light** — select a `*-light` element (material/glass/metro) → the ⚡ picker lists Homematic **dimmers**. The server promotes them as `component:'light'` from `hm/status/<seg>/LEVEL`, gated on the CCU `channelType`: a **BidCoS `DIMMER`** channel → one light (the DIMMER channel number varies per device, so it's keyed off the channelType, not the index); an **HmIP `DIMMER_VIRTUAL_RECEIVER`** actuator groups its virtual-receiver channels in consecutive triples exactly like the blind actuator — first-of-3 leaders, stable `…:g0/:g1` ids (a 6-channel device → 2 lights, leaders at index 1/4). A `LEVEL` whose `channelType` is not a light type (blind, TRV valve) or has no `hm` metadata is skipped. Picking one stamps the **separate-mode** wiring — `payload-mode=separate`, `subscribe-brightness`/`publish-brightness` = `hm/status|set/<seg>/LEVEL`, **`brightness-min=0`/`brightness-max=1`** (Homematic LEVEL 0.0–1.0 → max 1 scales feezal's 0–100 % to 0…1, no server scaling), **`on-off-source=brightness`** (dimmers have no on/off datapoint — on/off derives from the level), `payload-off=0`, `payload-on=1.005` (OLD_LEVEL restore convention), `mode=brightness`, and `message-property`/`message-property-brightness`/`message-property-state` = `payload.val` — plus `:0` `UNREACH`-only availability (dimmers carry no LOWBAT; metro-light has no availability attributes so the record is stamped but ignored). Verify against a real Homematic dimmer (both generations) if you have one; the group-of-3 leader rule and the `payload-on=1.005` OLD_LEVEL / `on-off-source=brightness` derivation are the assumptions to confirm. *(The client discovery maps ↔ server contract, incl. `message-property`/`valve-*`/`modes` on climate, `topic`+availability on WLED, `payload-tilted`/`type`/`message-property`+UNREACH/LOWBAT availability on contact, the cover separate-mode LEVEL topics + `min/max`=0/1 + the 12→4 group-of-3 promotion, and the light separate-mode LEVEL brightness topics + `brightness-max`=1 + `on-off-source`=brightness + the 6→2 group-of-3 promotion, are unit-tested in `test/feezal-sidebar-inspector-attributes.test.js` and `server/test/native-discovery.test.js`.)*
- [ ] **Climate boost countdown badge (E102 WP2)** (circle-climate, **glass-climate**, **metro-climate** — same contract in all three; boost/mode section attributes `boost-duration` (minutes, default 5), `subscribe-boost-remaining` + `message-property-boost-remaining`, `boost-remaining-unit` (`minutes`|`seconds`, default `minutes`), identical names + help texts across families): configure a `"momentary":true` boost mode entry. **Client timer path** — with **no** `subscribe-boost-remaining` topic wired, tapping the boost chip active starts a client-side countdown from `boost-duration` (minutes → seconds) that ticks down each second and renders an **`mm:ss` badge on the active chip** (material chip label, glass popup-button badge, metro flip-face chip badge). **Device topic path** — wire `subscribe-boost-remaining` and the badge shows the device-reported remaining time live instead (converted to seconds via `boost-remaining-unit`: BidCoS `BOOST_STATE` is minutes; a `seconds` value is used as-is); no client timer runs. The badge **clears** when the boost deactivates — either a mode read-back that no longer matches boost, or a second tap (tap-off). **Reload caveat:** a page reload while boost is active *without* the device topic restarts the client timer from full `boost-duration` — accepted degradation. **Real-device verification is a pre-preset gate, not a code gate** — confirm `BOOST_STATE` (BidCoS RT-DN) / `BOOST_TIME` (HmIP) names and units before the profile presets wire them; the client-timer path ships regardless. **Purely additive** — with no momentary entry / no boost active, nothing changes. *(Client countdown decrement + mm:ss badge, device-value override with unit conversion, deactivation clearing, and interval cleanup on disconnect are browser-tested for all three.)*
- [ ] **Climate shared controller + battery (E137/E124)** (circle-climate, glass-climate, metro-climate): all three cards share `@feezal/feezal-controller-climate` — one attribute fragment (both payload modes, setpoint/actual/mode/valve/boost + the E124 battery quartet), one discovery-map fragment, one E102/B53–B58 mode machinery. Family quirks are constructor flags, not forks: metro `{json:false, actualFromSubscribe:true}` (no json attributes in its inspector; `subscribe` stays the current-temperature topic, and it gains `subscribe-actual`/`message-property-actual` for map parity), material `{humidity:true}`. **Battery:** a TRV discovered with a battery record (z2m sibling, or hm `LOWBAT`/`LOW_BAT` seen on `:0`) auto-stamps the quartet; a low battery shows the shared **tiny low-battery symbol (a battery outline with one short charge bar, in the warning colour) bottom-right** while everything keeps working — never a blackout; **glass-climate keeps it bottom-left** (its ⚠ unavailability badge owns bottom-right there); mains-powered wall thermostats get no badge (presence-checked stamping). **Display change:** string `modes` entries (`["off","heat"]`) now render capitalized labels (`Heat`, `Fan only`) in ALL three families (previously glass/metro showed them lowercase). Live topic edits on the canvas re-wire in all three (previously glass only). *(The E102/B53–B58 suites now run against the controller in all three families; the E137 parity test derives each family's required attributes from the controller's consumed-set declaration.)*
- [ ] **Light shared controller (E137 part 4)** (circle-light, glass-light, metro-light, eink-light; B92: circle-switch is no longer part of this — it is now a standalone plain switch): all four light cards share `@feezal/feezal-controller-light` — one attribute fragment (both payload modes, E77 on/off-from-brightness incl. the 1.005 OLD_LEVEL restore + remembered last level, brightness-min/max scaling, colour temperature kelvin/mired, RGB/HS, effects, white/warm/cold channels) and one discovery map. **E127 settling is folded behind the controller's command surface** — elements no longer instantiate `SettlingController`; the settling attributes (`subscribe-working`/`subscribe-settled`/`settle-timeout`/`report-delay-ms`) are part of the shared fragment. Colour machinery (`pctToRaw`, kelvin/RGB/HSV/xy conversions) lives once in `@feezal/feezal-element/feezal-color.js`. Regression focus: a Homematic dimmer (separate mode, `on-off-source=brightness`, LEVEL 0–1) toggles/dims identically on all four cards with no ring/pill/tile jumping during ramps; a z2m json light (state/brightness/color_temp/color/effect in one payload) drives every card; live-canvas topic edits re-wire. *(The whole existing light test stock now runs against the controller; parity is derived from `LIGHT_CONSUMED_ATTRIBUTES`.)*
- [ ] **Cover shared controller (E137 part 5)** (circle-cover, glass-cover, metro-cover, eink-cover): all four cover cards share `@feezal/feezal-controller-cover` — one attribute fragment (json default + separate mode, B26 position/tilt device-range scaling incl. Homematic LEVEL 0…1 via max=1, dedicated `publish-up/-stop/-down` topics winning over `publish-command`, up/stop/down payloads, slat/tilt) and one discovery map. `invert`/`show-position`/`slat-count` stay per-family display attributes. **Change:** the up/stop/down/position/tilt commands are now editor-guarded in every family (previously the material buttons published even in the editor). **E128 (Homematic cover settling + DIRECTION) has landed inside this controller** — see its own row in §2; the settling + direction attributes are part of the shared fragment, so all four families carry them. Regression focus: a z2m cover (json, state-inferred position) and a native Homematic blind (separate, LEVEL 0…1, dedicated STOP topic) drive all four cards identically; live-canvas topic edits re-wire. *(Existing cover test stock runs against the controller; parity derived from `COVER_CONSUMED_ATTRIBUTES` for all four adopters.)*
- [ ] **WLED shared controller (E137 part 6 — completes the v1 controller set)** (circle-wled, glass-wled, metro-wled, eink-wled): all four WLED cards share `@feezal/feezal-controller-wled` — one attribute fragment (topic/transition/speed+intensity read-backs/presets), one discovery map, one `<topic>/g`+`<topic>/c` wiring + `<topic>/api` JSON command surface, and the `<topic>/status` availability auto-derivation. The bundled WLED effect/palette name tables now live ONCE in the controller package (the per-family `wled-lists.js` copies are gone). Regression focus: a WLED device drives all four cards identically (toggle, brightness, colour where rendered, effect/palette, preset recall incl. the numeric fallback); availability auto-derives and an explicit `subscribe-availability` still wins; live-canvas topic edits re-wire. *(The E103 suite runs against the controller; parity derived from `WLED_CONSUMED_ATTRIBUTES` for all four adopters.)*
- [ ] **Homematic climate fix wave (B53–B58)** (circle-climate, **glass-climate**, **metro-climate** — same behaviour in all three; verify against a real Homematic system, both generations if available):
  - **Off state (B53):** set a thermostat to Off (from feezal OR the wall dial — the device reads back Manu + setpoint 4.5) → the card shows **"Off"** as the active mode with **no target temperature** (material: mode label in the arc centre instead of "→ 4.5 °C", parked fill arc; glass: state line/pill show "Off"; metro: front line + back stepper readout show "Off"). Crucially this also works when the thermostat previously ran at a real setpoint (e.g. 21°). Raising the setpoint (dial or drag/step) flips the display back to Manu + target. Non-Homematic climates (no `match-setpoint-max` entries) are pixel-identical to before.
  - **Boost detection (B54):** new `subscribe-boost-state` + `message-property-boost-state` attributes (all three families; hm discovery stamps them to `hm/status/<ch>/BOOST_MODE` / `payload.val` for BOTH generations). Activate boost from feezal → the chip stays active for the full duration (no flip back to Manu when HmIP `SET_POINT_MODE` keeps reporting 1); activate boost **at the wall dial** → feezal shows it and the countdown starts; reload mid-boost → boost shows active; `BOOST_MODE=false` read-back (or tap-off) returns the display to the real mode.
  - **Auto button (B55):** a discovery-wired Homematic thermostat shows **Auto** (mode value `0`) on all three cards; tapping Auto publishes, and an Auto read-back (0) marks the chip active. Same on glass's state line (B57).
  - **Valve datapoint (B56):** re-discover a TRV that exposes `VALVE` (newer HmIP) → the valve % is wired to `VALVE` (not the `VALVE_STATE` adaptation enum) and `valve-min`/`valve-max` are stamped from the datapoint's real min/max metadata; BidCoS `VALVE_STATE` (percent) and HmIP `LEVEL` (0–1) devices re-discover as before.
  - **Mode label (B57, glass):** the front state line shows `→ 21,5 • Manu` / `• Auto` / `• Off` (labels, incl. mode 0) — never the raw number; an unknown mode value still shows raw.
  - **HmIP Manu (B58):** click Manu on an HmIP device/heating group → the putParamset payload carries `SET_POINT_TEMPERATURE` as a JSON **number** (wire: `{"CONTROL_MODE":1,"SET_POINT_TEMPERATURE":17}`, no quotes) and the thermostat actually switches; Off keeps working; BidCoS `MANU_MODE=$setpoint` (string payload) unchanged; a hand-wired embedded `"temp=$setpoint"` still substitutes textually. *(All six are unit/browser-tested: `test-browser/feezal-element-{material,glass,metro}-climate.test.js`, `server/test/native-discovery.test.js`.)*
- [ ] **Circle-slider parity (B29/B37)** (circle-climate, circle-light): place both cards side by side with identical width/height → the setpoint arc and the brightness ring have the same centre, radius, track width and knob size (climate's track is no longer narrower), **and the same vertical position** — both circles are width-sized and anchored at the top of the card (B37: climate no longer centres its arc in the leftover height); rows below the arc (mode chips, valve, humidity, label) stack underneath and clip on too-short cards, exactly like the light card's controls. Setpoint dragging still works across the whole arc. Style inspector on both: `--feezal-*-track-width` (default 7) and `--feezal-*-knob-size` (default 10) are unitless %-of-viewBox numbers — setting e.g. 10/14 visibly thickens track/knob, and the same numbers on both elements look identical.
- [ ] **Topic autocomplete in custom inspectors (B28)**: every MQTT-topic field in every custom (N6) inspector offers the same dropdown autocompletion as the generic attribute form (shared `feezal-topic-input` from `@feezal/feezal-element`) — type a prefix with retained topics on the broker → suggestions appear (debounced), arrow keys + Enter pick, an intermediate `topic/` completion descends a level and stays open, Escape/Tab dismisses, clicking a final completion commits the attribute. Spot-check across families: glass-light/cover, metro-light, circle-climate/cover/light, material-dialog(+view), material-navbar per-item badge topic, system-notification, basic-qrcode, basic-svg binding rows, layout-app (subscribe-title + per-action publish). layout-flex/-responsive and system-script have no topic fields. Non-topic fields (payloads, labels, message-property) stay plain inputs.
- [ ] **Contact shared controller + battery (E137/E124)** (circle-contact, glass-contact, metro-contact): all three cards share `@feezal/feezal-controller-contact` — identical attribute contract (subscribe/payload-open/-closed/-tilted/message-property + the E124 battery quartet `subscribe-battery-low`/`message-property-battery-low`/`payload-battery-low`/`battery-low-threshold`) and identical open/closed/tilted matching (shared `payloadMatch`, incl. numeric Homematic `0`/`1`/`2`). A low battery shows the shared **tiny low-battery symbol (battery outline + one short charge bar, warning colour) bottom-right while the contact state keeps updating** — never a blackout (the `!` unavailability badge keeps top-right in each family; glass/circle/metro contacts all use bottom-right). Discovery stamping: a z2m contact with a `battery_low` or percentage sibling auto-stamps the battery attributes exactly like the sensor cards (E132); a Homematic contact stamps them only when the device was seen publishing `LOWBAT` (BidCoS) / `LOW_BAT` (HmIP) on `:0`, and availability is now `UNREACH`-only. *(Controller contract + fragments, tristate matching, badges, the recognizer presence-check and the UNREACH-only availability are unit/browser-tested.)*
- [ ] **Contact tristate + live rewire (B27)** (circle-contact): with `payload-closed`/`payload-open`/`payload-tilted` = `0`/`1`/`2` (Homematic), payload `2` shows the tilt state on the disc (tilt colour + `Tilted`), `1` open (primary colour + `Open`), `0` closed (muted + `Closed`); changing `subscribe` or the payloads via the inspector on the live canvas takes effect immediately — no reload. *(Tristate incl. numeric payloads, the disc tilt class and the rewire are browser-tested. E138 redesign: the card is now the E134 circle disc, not the old SVG handle.)*
- [ ] **Shutter value ranges & dedicated command topics (B26)** (circle-cover, glass-cover): set `min`/`max` (e.g. 0/1 for Homematic) → an incoming position of `0.5` displays as 50 %, and setting 50 % publishes `0.5` (json and separate mode); same for `slat-min`/`slat-max` with the tilt slider; defaults 0/100 behave exactly as before (integer payloads). Set `publish-up`/`publish-stop`/`publish-down` topics → the corresponding button publishes its payload to the dedicated topic instead of `publish-command` (also wins over the json publish topic); leave them blank → single-topic behaviour unchanged. Both inspectors: Command section shows the three optional dedicated-topic fields (separate mode), Config tab shows the Value ranges section.
- [ ] **Dialogs** (material-dialog, countdown-dialog, paper-dialog): open/close triggers; template body renders.
- [ ] **Paper card (E87)** (paper-card, renamed from card-template): heading/subhead/header-image render; elevation 0–5 shadows; `${msg.payload}` body updates from the subscribed topic; a saved dashboard still using `<feezal-element-paper-card-template topic="…">` renders identically (deprecated alias maps `topic` → `subscribe`; console warning appears).
- [ ] **Paper dialog parity (E86)** (paper-dialog, paper-dialog-view): both are attribute-identical twins of the material dialogs (unit-asserted) — verify the paper *look* (2px corners, paper shadow, flat uppercase text buttons) in light + dark themes; paper-dialog-view embeds the selected view live (MQTT bindings work inside); B24 sizing + B25 header behave like the material pair.
- [ ] **Dialog sizing (B24)** (material-dialog, material-dialog-view): with little body content set `min-height` (e.g. `300px`) → the open dialog is at least that tall instead of collapsing to ~50px; `height` forces an exact panel height; empty values keep auto sizing. Check the Layout section rows in both custom inspectors.
- [ ] **Dialog header parity (B25)** (material-dialog, material-dialog-view): both dialogs show the same header — title left, ✕ top-right — by default (material-dialog now has the ✕ too); ✕ closes; unchecking `show-close` (with no title) removes the bar; `hide-header` removes the bar entirely even with a title; changing these while the dialog is open updates it live.
- [ ] **Repeater**: array payload → N children; key→attribute mapping; child MQTT wiring.
- [ ] **Chart / map / camera / energy-flow**: render their heavier viz; check performance + no leaks.
- [ ] **Auto-discovery consumers** (light, climate, cover, switch, sensor-like): dropping onto a discovered device wires topics/payloads correctly (see §9).
- [ ] **Light sub-integer brightness range** (circle-light): with `brightness-min=0` / `brightness-max=1` (Homematic LEVEL) an incoming `0.5` shows 50 % on the ring, and releasing the ring at e.g. 49 % publishes `0.49` — not just 0/1. Integer ranges (0–100, 0–254 json mode) still publish whole numbers. *(pctToRaw scaling is unit-tested; this is the end-to-end check.)*
- [ ] **Button state feedback (E79)** (material-button + paper-button, same contract): set `subscribe` to a state topic → payload `1`/`0` (or custom payload-active/-inactive) highlights/unhighlights the button; other payloads leave it unchanged; with `--feezal-button-active-color` set, all five material variants and the paper button render the state; `disabled` greys the button and blocks publishing (UI guard only — the topic stays writable); E50 condition rows can toggle `disabled` from MQTT. *(Payload mapping + disabled guard are browser-tested for both.)*
- [ ] **Contact single-mode only (E78)** (circle-contact): a saved view still carrying the removed `contacts` JSON attribute renders the normal single-contact disc (attribute ignored, no dot grid, no crash); open/closed/tilted mapping and availability badge unchanged. Room overviews = multiple contact elements (composed component / repeater).
- [ ] **QR content assistant (E76)** (basic-qrcode): the inspector shows a Type picker — pick *WiFi*, fill SSID/password/security → the canvas QR updates live and the *Generated value* preview shows the `WIFI:S:…;T:…;P:…;;` string; special characters (`; , " : \`) scan correctly; re-selecting the element round-trips the value back into the fields; an unknown value opens on *Text / raw* unchanged; switching Type alone never clobbers the value. Password field is masked with a reveal toggle. *(Generate/parse round-trips are unit-tested.)*
- [ ] **Click-through (E82 family)** (basic-template + basic-image: boolean, default off; basic-icon: `on|off` select, **default on** — its long-standing decorate-a-button behaviour): stack the element over a button (U33 stacking), enable click-through → in the **viewer** clicks reach the button underneath (whole-element passthrough); icon with `click-through: off` catches clicks instead and the setting survives save/reload; in the **editor** all three stay selectable/draggable regardless.
- [ ] **Template JSON coercion (N35)** (basic-template): subscribe to a topic carrying a JSON object payload → a bare `${msg.payload}` renders compact JSON (`{"temperature":21.5,"hum":40}`), not `[object Object]`; nested interpolation (`${msg.payload.sensors[0]}`) renders that object as JSON too; an array of objects renders as JSON (no `[object Object],[object Object]`); string/number/boolean payloads render exactly as before and property access (`${msg.payload.prop}`) is unaffected; `${JSON.stringify(msg.payload, null, 2)}` remains the pretty-print route. Other elements subscribed to the same topic are unaffected (the shared message is never mutated). *(Coercion incl. nesting, arrays, primitives, cycles and no-mutation is unit-tested.)*
- [ ] **Light Homematic dimmer mode (E77)** (circle-light): set *On/off source: brightness* in the Config tab (Topics tab then hides the unused State inputs) against a dimmer with only LEVEL topics — incoming `0.5` shows on + 50 %; centre tap turns off (publishes `0`) and back on (restores the previous %); dragging the ring **from off** to 30 % turns the light on; with *On* = `1.005` the toggle-on publishes `1.005` and the centre shows the device-echoed level (OLD_LEVEL restore). Zigbee lamps in the default *topic* mode behave exactly as before (off retains brightness). *(Derivation/toggle/drag semantics are unit-tested; this is the real-device check.)*
- [ ] **Schedule editor (E52)** (material-schedule, see docs/schedule-format.md): retained schedule JSON on the subscribe topic renders as blocks on the Mon–Sun grid; **drag on empty space** paints a block (snapped to `step` minutes), **dragging block edges** resizes without overlapping neighbours, **tap** selects (toolbar: Delete; `type="number"`: value input clamped to min/max); edits show “● unsaved” — **Save** publishes the whole JSON retained, **Revert** restores; publish a different schedule externally *while dirty* → “changed remotely” hint, draft intact; the **now chip** (“now: 21.5 → 22:00”) and the dashed now-line track the client clock; touch: paint/resize with a finger. Verify a she/Node-RED consumer reads the published JSON (wall-clock contract). *(Format round-trip, clamping, dirty/save/revert/remote-hint, effectiveNow are unit-tested.)*
- [ ] **SVG bindings (E51)** (basic-svg): upload an SVG with `id`d shapes via the Asset Manager, set `src` → renders inline scaled to the box; in the custom inspector add binding rows — `#lamp` + map `on→#ffb300` recolors live, a `rotate` range turns a needle with the payload, `format` fills a `<text>`; an unmatched payload **reverts the shape to its original**; a row with a publish topic makes the shape clickable (cursor + focus ring, publishes payload; not in the editor); editor canvas shows the “N bindings” badge; an SVG containing `<script>`/`onclick` is defanged. Note: static exports opened from `file://` can't fetch the asset (browser restriction) — hosted exports work. *(Mapping semantics, sanitize, revert, click regions are unit-tested.)*
- [ ] **Script (E49)** (system-script, see docs/script-element.md): editor shows a code chip with the `name`; the inspector's Monaco editor offers `fzl.` completions and the expand (⛶) overlay works; write `fzl.sub('a', p => fzl.pub('b', p))`, bind a display element to `b` → deploy + **reload the viewer** → publishing to `a` updates the element; the script does NOT run on the editor canvas; a `throw` shows a `[name]`-prefixed console error without breaking the page; source view shows the code inside `<script type="text/feezal">` and survives a save round-trip (incl. `<` in code). *(fzl API, payload convention, scoping, error handling are unit-tested.)*
- [ ] **Time picker (E25)** (material-time-picker): retained `06:30` / `06:30:15` / `23400` (seconds) on the subscribe topic all show in the field; typing a time in the native field publishes in the configured `format` (`HH:MM` / `HH:MM:SS` / `seconds`); the clock icon opens the **wheel overlay** — drum columns snap to entries, `step=5` coarsens the minutes column, `show-seconds` adds a third drum; OK publishes, Cancel doesn't, backdrop click cancels; `publish-on-change` publishes on every drum settle; nothing publishes in the editor. *(Payload parse/format round-trips are unit-tested.)*
- [ ] **Camera click actions** (basic-camera; hard-renamed from circle-camera, see BREAKING-CHANGES.md): `click-action: popup` — tapping the feed opens a near-fullscreen top-layer popup with the same feed (✕ button, tap anywhere or Esc closes; with `type=hls`/`webrtc` + `show-controls` the native video controls stay usable and don't close it); `click-action: publish` — tapping publishes `payload` to the `publish` topic; nothing fires on the editor canvas; `click-through` lets taps reach an element beneath the feed (and disables click-action) while the camera stays selectable/draggable in the editor; with `popup-animation` the popup visibly grows out of the camera tile on open and shrinks back into it on close (~220 ms; off = instant, unchanged). *(Publish, popup open/close, click-through and the animation are e2e-tested: `test-e2e/material-camera.test.js`.)*
- [ ] **Camera E163 features** (basic-camera): **`type: mqtt-image`** — point `subscribe` at a topic carrying image payloads (Frigate `frigate/<cam>/person/snapshot`, an HA MQTT camera, an ESP32-cam): binary frames render (the connection relays them as data URLs — works via feezal-server AND direct-MQTT), raw-base64 payloads render too; **aspect-ratio** (`16/9`) letterboxes the feed, **object-fit** cover crops; **chips** (JSON) overlay live state badges — a `show: nonzero` chip (person/car count, motion) appears only while active; **event list** — `show-events` + `events-topic: frigate/events` (+ `events-camera` filter, `events-max`) lists recent detections newest-first with time/class/score/zones and per-class thumbnails from `event-thumbs`; tapping a row opens the snapshot in the popup; **live-on-demand** — a cheap `image`/`mqtt-image` tile with `popup-type: webrtc`/`hls` (+ `popup-src`) opens the live stream only in the popup; **every button is optional** — `show-fullscreen-button` (⛶ opens the popup), `show-refresh-button` (↻, image type), `popup-close-button` off hides the ✕ (Esc/tap still close); **Frigate autodiscovery** — with a Frigate on the broker the ⚡ picker / Generate offer one camera entity per Frigate camera (source badge "Frigate") which stamps feed + events + chips + thumbs + availability in one pick; cameras resolve to **basic-camera in every family** — generating a glass/metro/eink/… app still creates the camera elements instead of skipping them as a parity gap. **Frigate URL → live tiles**: with Frigate cameras among the discovered devices, the Devices list / App setup shows a *Frigate URL* row (prefilled from the last-used value, else guessed as `http://<broker-host>:5000`); with a URL set, generated cameras get `src=<url>/api/<cam>` + `type=mjpeg` + `pause-when-hidden` (live video immediately; the snapshot `subscribe` is dropped so a detection can't replace the live stream) — clear the field to keep the pure-MQTT detection-snapshot tiles; the entered URL is remembered for the next run. The **⚡ inspector picker** applies the same rewrite when stamping a Frigate camera onto a selected element: `src` is never left empty — it uses the remembered URL, else the broker-host guess, else the `http://frigate.local:5000` placeholder (edit just the hostname). *(mqtt-image incl. base64, chips visibility, event ring buffer/filter/thumbs and the button knobs are browser-tested in `test-browser/feezal-basic-camera.test.js`; the recognizer in `server/test/native-discovery-frigate.test.js`; binary detection in `server/test/bridge.test.js`.)*
- [ ] **Camera WebRTC (WHEP)** (basic-camera, `type=webrtc`): point `src` at a real WHEP endpoint — go2rtc (`http://host:1984/api/webrtc?src=cam`) or MediaMTX (`http://host:8889/cam/whep`) — → low-latency live video plays (muted by default); the popup shows the same stream without opening a second session; publishing a different WHEP URL to the subscribe topic renegotiates onto the new source; stopping the media server → the element retries every 5 s and recovers when it's back. *(Offer/POST/answer negotiation, live frames, muted default and popup stream sharing are e2e-tested with an in-page WHEP loopback; the real-server/network path is this manual check.)*
- [ ] **Camera pause-when-hidden** (basic-camera): with `pause-when-hidden` on, deploy a mjpeg/hls/webrtc (or refreshing `image`) camera into one view of a multi-view site → while that view is showing, the feed is live; switch to another view (or scroll the camera off-screen, or background the browser tab) → the feed stops (webrtc peer connection closes, mjpeg/hls/image `<img>`/`<video>` drops to the placeholder, the refresh timer halts) — verify in DevTools ▸ Network that the stream connection actually closes; return to the view / scroll it back / refocus the tab → the feed reconnects and plays again. With the toggle **off** (default) the feed stays live in hidden views as before. Editor canvas always shows the live feed regardless of the toggle. *(Feed teardown/restore + "no observer unless enabled" are browser-tested in `test-browser/feezal-basic-camera.test.js`.)*
- [ ] **Icon-bearing** (icon-button, fab, badge, chip, tile, navbar, app-layout): glyphs load (Material Icons font), not text.
- [ ] **Data table (E75)** (basic-table): retained JSON array on the subscribe topic renders as a table — columns auto-derive from the first row, or configure them via the `columns` list editor (key/label/width/align/format/editable; a per-column `class-map` added in source view survives list edits); `format` `number:1:°C` rounds + suffixes, `date`/`time`/`datetime` render epoch-seconds, epoch-ms and ISO strings via the locale; header click cycles asc → desc → payload order (numeric-aware; `sortable=false` disables), `filter` shows the search box, `max-rows` caps, header stays sticky while the body scrolls; `row-class-map` (`{"state":{"error":"error"},"load":{">90":"warn"}}`) tints whole rows and per-column class-maps colour cells — ok/warn/error/info follow the theme state colours in light + dark; **write-back**: with `editable` on, flagged columns render inputs — commit an edit, add (＋) and delete (✕) a row → each publishes the WHOLE array (non-retained by default, the `retain` checkbox opts into the MQTT retain flag, `publish` overrides the topic) and a second open viewer shows the change live; nothing publishes from the editor canvas (sample rows show there until real data arrives). *(Ingestion, columns, formatting, sort/filter/cap, class maps, editor sample/guards and the whole-array write-back contract are unit-tested.)*
- [ ] **JSON tree (E88)** (basic-json): subscribe to a topic carrying a JSON payload → it renders as a collapsible tree — objects/arrays show a chevron + key + preview (`{…} 3 keys` / `[…] 5 items`), primitives show `key: value` rows coloured per type (keys neutral, strings quoted in the accent colour, numbers/booleans in the primary colour, null/undefined muted); clicking a node toggles it, `expand-depth` (default 1) auto-expands that many levels of newly-seen nodes, long values ellipsize with the full value in the hover `title`. **Expand state survives updates**: expand or collapse a node, then let a live payload update the same topic → the tree keeps your expand/collapse choices (it must not snap shut on every message); brand-new paths in a later payload follow `expand-depth`. **Raw-string fallback**: publish a non-JSON payload (e.g. `ON`) → it renders as the raw string, never an error state; a subsequent valid JSON payload switches back to the tree. **max-nodes guard**: publish a huge payload (e.g. an object with hundreds of keys) with a low `max-nodes` (e.g. 10) → only up to the limit renders and the remaining siblings collapse into a "+N more…" hint, the tab never freezes. **Theming**: check the tree colours (keys/strings/numbers/null/guide lines) read correctly in light AND dark themes; the `--feezal-json-*` style vars retheme them. On the editor canvas a sample object shows until real data arrives; monospace font, the box scrolls (`overflow: auto`) on overflow. *(Tree rendering, expand-state survival across updates, raw-string fallback and the max-nodes guard are browser-tested.)*
- [ ] **Lottie animation (E89)** (basic-lottie): drop the element and set `src` to an uploaded animation JSON (Asset Manager) — the `src` field **autocompletes the site's `.json` assets** (focus/type to filter, arrow keys + Enter to pick; values are `/assets/<site>/…` URLs), and you can **drag a `.json` asset from the Asset Manager straight onto the canvas to auto-create a Lottie element** (same UX as dragging an image → image element; the drag ghost shows an animation glyph, and `.json` tiles show the `animation` icon). → in the **viewer** the Lottie animation plays, scaled to the element box (svg renderer), with `--feezal-lottie-background` behind it; `autoplay`/`loop` are **true/false selects** (not checkboxes) defaulting to on — turning either OFF stores an explicit `="false"` so it **survives save/deploy** (a default-true boolean *checkbox* would serialise as absent = back to on in the viewer; that's the loop bug this avoids); `speed` behaves (loop off stops at the last frame, speed 2 runs double-time). **Lazy chunk**: with dev-tools Network open, load a dashboard that has NO lottie element → the `lottie-*.js` chunk is **never** fetched; a dashboard **with** the element fetches it once, on demand. **Transport payloads**: publish `pause`/`play`/`stop` on `subscribe` → the animation pauses, resumes, and resets to the first frame; custom `payload-*` values work. **Value→segment map**: with `map` = `{"sunny":{"segment":[0,60]}, "rain":{"segment":[61,120],"loop":true,"speed":2}, "storm":{"src":"storm.json"}}`, publishing `sunny`/`rain` seeks+plays those frame ranges (rain also loops at 2×), and `storm` **swaps the animation** (old instance destroyed, `storm.json` loaded); the map is checked **before** the transport payloads (a map key equal to `play` wins over the play command); an unmatched value is ignored (no throw). **Resize**: drag-resize the element (E38) → the animation re-fits. **Editor rendering**: the animation now renders on the editor canvas too (respecting `autoplay`/`loop`; with `autoplay=false` it sits on the first frame) — an unconfigured element (no `src`) still shows the discoverable film-strip chip, and MQTT-driven playback (map / transport commands) stays viewer-only (no subscription in the editor). The lazy `lottie-*.js` chunk loads in the editor only once a lottie element has a `src`. **Broken / invalid src**: a missing file (404) or unreadable JSON shows the subtle dashed "Lottie" placeholder (no throw); a JSON that loads but is **not a Lottie animation** (no `layers` array / version keys) shows a distinct **"Not a Lottie animation"** placeholder instead of a silent empty render — the element fetches + validates the JSON itself and hands lottie-web the parsed `animationData`. Fixing the src recovers. **Disconnect**: switch views / close a popup containing it → the instance is destroyed (no leaked animation/CPU). *(Editor rendering + viewer-only MQTT, the loop/autoplay-persist fix, transport payloads, map matching + src-swap, reload-on-src-change, disconnect cleanup, the broken-src placeholder and the not-a-Lottie validation are browser-tested with a stubbed lottie factory + mocked fetch; the bundle keeps lottie-web in a separate on-demand chunk.)*
- [ ] **Splash / FOUC cover (E39)** (system-splash, pseudo-element — placeholder chip in the editor, overlay in the viewer): drop it once on the first view. **Editor**: a dashed "Splash" chip only; position/size don't matter; the overlay never renders on the canvas; attributes edit in the normal inspector; the Style panel exposes `--feezal-splash-background` + `--feezal-splash-spinner-color`. **Fast load** (broker with retained topics, good connection): the viewer opens to a solid splash in the background colour that clears within a few hundred ms of connect (retained burst settles → `settle-window` = 500 ms of quiet) — a clean flash, no spinner. **Slow load**: throttle the network → the spinner appears (default `spinner-delay` = 0, i.e. immediately) and the splash hides once messages go quiet. **No-retained-topics dashboard** (or messages that keep streaming): the splash still clears at the `timeout` (default 1 s) fallback — it never hangs. **Connection never establishes** (wrong broker): the same `timeout` counts from page load as a hard cap → the splash still clears. **Logo**: set `logo` to an uploaded image → it renders centred above the spinner (~40vw/30vh) and fades out with the overlay. **Fade**: the overlay fades (~250 ms) then leaves the DOM. **Once only**: switching views afterwards does not re-show it. **Multi-instance**: a second splash on the site no-ops and logs a `console.warn`; the first keeps the overlay. **Light/dark theme**: the background + spinner colour follow the theme. Lottie boot-animation support was **removed** from the splash — only the logo + CSS spinner remain (there is no `lottie` attribute). *(Settle-window quiet + message reset, the hard-cap fallback with no connect, spinner-after-delay, editor chip and place-once warn are browser-tested.)*
- [ ] **Font Awesome sets (N28)** (`feezal-icons-fa`): the icon picker shows **three chips** — `fa-solid`, `fa-regular`, `fa-brands`; picking writes e.g. `fa-brands:github` and the glyph renders theme-coloured at any size (varying FA viewBox widths look right); a deployed viewer page and a static export render the used FA icons **without** loading the ~2 MB full modules (tree-shaken registration — check the page source); typing `fa-brands:` in an icon field scopes the autocomplete. *(Set registration, per-style volume, SVG normalisation and multi-set tree-shaking are unit-tested.)*

---

## 7. Themes & styling

(Theme variables, distinctness and per-theme swatch screenshots are automated;
refresh baselines with `npm run test:browser -- --update` after palette changes.)

- [ ] **Theme panel tabs**: Theme · Classes — the Theme tab holds the theme selector and colour overrides, the Classes tab the class editor; the Classes tab label shows the class count (e.g. "Classes · 2"); the tab bar aligns with the view tab bar like Inspector/Site Settings.
- [ ] Switch between all built-in themes (9) → **whole editor + viewer look right** (beyond the swatch palette); picker swatches sample real colours.
- [ ] **Colour overrides** section (Theme tab): add/edit/remove override; live preview; persists; **collapsible state persists** (default open).
- [ ] **Classes** tab: create/rename/delete a class; edit its CSS props; applies live to elements carrying it.
- [ ] User `.css` themes (create/save/delete) coexist with theme packages.
- [ ] Historical-preview (`?sha=`) uses the theme/overrides/classes from that commit.

## 8. Assets

REST endpoints and the copy-on-use drag (incl. inspector refresh) are automated —
this section is about the remaining **UI**.

- [ ] **Site/Global tab bar** (same styling/alignment as the other sidebar tab bars): switching tabs resets to the category root folder; "Move to site/global" from the context menu switches to the destination tab automatically. The thumbnail/list/details view toggles and the New-folder/Upload buttons sit right-aligned INSIDE the tab bar and work from there.
- [ ] Upload via button **and** drag-drop, to Site and Global; folder UI (mkdir, move into); rename; delete; preview overlay.
- [ ] Move/copy an asset between site↔global via the UI; references in the site HTML follow.
- [ ] **Set as background (N33)**: right-click an image asset → **Set as background** shows a fly-out submenu (opens leftward when the menu sits near the right window edge) with **Current view** / **All views**; picking one stamps `background-image` (plus `cover` / `center` / `no-repeat` defaults) as inline style on the target view(s) — visible immediately on the canvas, site marked dirty, and the style inspector shows the new values if the view is selected. From the **Global** tab the asset is first copied into the site (copy-on-use, filename suffixed on collision, appears in the Site tab) and the site copy's URL is used; if the copy fails the global URL still works. Non-image assets and folders don't offer the entry. *(Longhand stamping, both scopes, copy-on-use incl. fallback and the inspector nudge are unit-tested.)*

## 9. MQTT / connection / auto-discovery

- [ ] **Cross-component discovery (E156)** — the ⚡ picker no longer requires the entity's component to equal the element's. **Switch ← light:** drop a `*-switch` (material / glass / metro / circle / eink / carbon / panel / paper), open ⚡ and confirm **lights appear alongside switches**; pick a **zigbee** lamp → `subscribe`/`publish` are its state/command topics with `ON`/`OFF`, and toggling really switches it. Pick a **Homematic dimmer** → it wires the LEVEL topics with **`payload-on: 1` / `payload-off: 0`** — *not* the `1.005` OLD_LEVEL sentinel a light card uses (a switch has no remembered level to restore); toggling drives the lamp fully on and off. **Slider ← axes:** drop a `material-slider` / `carbon-slider` / `paper-slider` — it now has a ⚡ picker at all. A dimmable colour-temp lamp appears as **two rows**, “… brightness” and “… color temp”; each wires that axis's topics and range (zigbee brightness → `max: 254`; colour temp → the mired `min`/`max`). A dimmable-only lamp shows just the brightness row. An HA **`number`** appears with its `min`/`max`/`step`. **The guardrail:** a **`sensor`** (temperature, humidity, power…) must **never** appear in a slider's picker — it is read-only — and neither must an on/off-only relay lamp (that is a switch match, not a slider one) nor a `number` without a command topic. *(The acceptance mechanism, both switch payload dialects, the per-axis rows and every settable-only exclusion are unit-tested in `test/feezal-discovery-cross-component.test.js`; the picker itself is the manual part.)*

- [ ] **Cross-component rollout to the remaining controls (E157)** — the same mechanism, now on the controls E156 skipped. **Checkboxes + chip:** drop a `material-checkbox` / `carbon-checkbox` / `paper-checkbox` / `tui-checkbox` / `material-chip`, open ⚡ → **lights appear alongside switches**, and picking one behaves exactly like the `*-switch` case (zigbee lamp → state/command topics + `ON`/`OFF`; Homematic dimmer → LEVEL topics with **`payload-on: 1` / `payload-off: 0`**, never `1.005`); ticking the box really switches the lamp. Check `paper-checkbox` specifically: it reads from **`topic`**, not `subscribe` — after stamping, `topic` holds the state topic and `subscribe` is **absent**. **Knob:** `panel-knob` now offers a dimmable lamp as **two rows** (“… brightness” / “… color temp”) like a slider, while an HA **`number`** still stamps its **`unit`** into the readout (the slider's map has no unit — the knob must keep its own). **Read-only displays:** `material-tank` and `material-progress` have a ⚡ picker at all now — a **`sensor`** wires `subscribe` + `unit`, an HA **`number`** wires `subscribe`/`min`/`max`/`unit`, and a dimmable lamp offers a “… brightness” row that shows the level. **The mirror-image guardrail:** none of these three ever gets a **`publish`** attribute — even when the `number` or the light has a command topic — so a display can never command the device; and a light with no brightness *state* topic is not offered to them at all. *(Both fragments, the `topic`-vs-`subscribe` spelling, the knob's unit retention and every read-only exclusion are unit-tested in `test/feezal-discovery-cross-component-rollout.test.js`, which also asserts each of the 8 elements really pulls the shared fragment; the pickers themselves are the manual part.)*

- [ ] **zigbee2mqtt lights in a slider's picker (B81)** — drop a `material-slider` / `carbon-slider` / `paper-slider` / `panel-knob` and open ⚡ with a **zigbee2mqtt lamp** discovered. It now appears as "*<lamp>* brightness" (and "*<lamp>* color temp" if supported) — before this fix z2m lamps were **missing entirely** while Homematic dimmers worked. Picking brightness wires `subscribe` to the lamp's base topic, `publish` to `…/set`, **`publish-json-key: brightness`** and `message-property: payload.brightness`; dragging the slider publishes **`{"brightness": 128}`** (an object, with a **numeric** value — not `"128"`) and the lamp dims. Range comes from `brightness_scale` when declared (z2m: `max` = 254), else the JSON-schema default **0–255** — *not* the slider's own 0–100. Colour temp behaves the same with `publish-json-key: color_temp` and the mired range (default 153–500). **Homematic dimmers are unchanged:** still one topic per axis, still a plain number, and **no** `publish-json-key` attribute (a LEVEL topic takes a scalar — an object there would break it). **Guardrail:** an **on/off-only** z2m lamp (`supported_color_modes: ["onoff"]`) still yields **no** slider row.

- [ ] **Setpoint and blind-position sliders (E158)** — with a Homematic **thermostat/TRV** and a Homematic **blind** discovered, both now appear in a slider's / knob's ⚡ picker. **Setpoint:** the thermostat shows "*<name>* setpoint"; picking it wires the SET_TEMPERATURE topics plus `min`/`max`/`step` from the device (Homematic: 4.5 / 30.5 / 0.5) and `message-property: payload.val`. Dragging changes the target temperature; the slider must **not** end up subscribed to the *actual* temperature. An HA **`water_heater`** works the same way. **Position:** the blind shows "*<name>* position"; picking it wires the LEVEL topics with **`min: 0` / `max: 1`** (Homematic reports 0…1 — the range is taken from the device, not assumed 0–100), and dragging moves the blind. An HA/zigbee2mqtt cover instead wires `position_topic` → `set_position_topic` with `min`/`max` 0–100 and `message-property: payload.position`. Both publish a **plain number** — neither gets a `publish-json-key`. **Guardrails:** a climate entity with **no settable setpoint**, an **open/close-only** blind (no position topics), and a cover that **reports** position but cannot be positioned all yield **no** slider row. *(Both items' mapping, ranges, dialect switching and every exclusion are unit-tested in `test/feezal-discovery-settable-axes.test.js`; the pickers and the real devices are the manual part.)*

- [ ] **⚡ picker actually stamps (B86)** — select an element, open the ⚡ discovery picker and pick a device: its attributes appear **immediately** (topic, unit, label, availability), and reopening the picker shows that device as **selected** rather than blank. Try both id dialects: an **ESPHome** sensor (id contains `/`, e.g. `sensor/<node>/<object>`) and a **Homematic** device (id contains spaces and colons). Nothing should appear in the console — a warning there means the pick did not resolve. *(Automated in `test-browser/discovery-picker-selection.test.js`, which renders through real Lit + `sl-select` and binds to the exported separator; a unit test of `stampDiscovery` alone cannot catch this, and did not.)*

- [ ] **Thermostat readouts on value/gauge cards (B85)** — drop a `*-value` or `*-gauge` (circle/glass/metro/eink/panel/tui) and open ⚡ with a **Homematic/HmIP thermostat** discovered: it appears as **separate rows** — "… temperature", "… valve" (TRVs) and "… humidity" (wall thermostats like HmIP-WTH/STHD; plain eTRVs have none, and must show no humidity row). Picking **temperature** wires the ACTUAL temperature — *not* the setpoint — with `°C`; **humidity** wires `%`; **valve** wires the LEVEL/VALVE topic with the device's own range (HmIP LEVEL is **0…1**, VALVE is 0…100 — check `min`/`max` match the device, not a hardcoded 0–100). **Guardrail:** none of these may ever stamp a `publish`/command topic — a readout must not be able to drive the boiler. Also check a **climate card** now shows humidity where the device reports it. *(Fragment behaviour, wiring and exclusions are unit-tested; the recognizer's humidity emission is fixture-tested in `server/test/native-discovery.test.js`.)*

- [ ] **Splash adds no scroll (B82)** — place a `system-splash` on a view with enough content to scroll, deploy, and open the viewer: there must be **no second/outer scrollbar** and **no blank strip** below the content. Resize the splash on the canvas — the page must stay unaffected (its height used to leak into the document). Check the installed **iOS PWA** and a normal Safari tab, where the URL bar made it worse. The splash itself must still cover boot normally.

- [ ] **Gradient detection sees every authoring form (B83)** — set a view's gradient via **Styles → Background → Gradient** (which writes longhands) and confirm the viewer pins it on scroll. Repeat with a gradient reached through a **CSS variable**. In both cases `feezal-site` must carry the `gradient-bg` attribute and the view's own computed `background-image` must be `none` — if the attribute is missing, detection missed the form and nothing else will work. Also re-test on **iOS**, which is the platform B62/B83 were reported from and the one still unverified.

- [ ] Connection Settings: set broker URI, credentials, protocol version, TLS certs (N8) — connect succeeds; status shown.
- [ ] **Server connection indicator** (Connection tab, top): green dot + "connected" + broker URI while the server↔broker bridge is up; red dot + "not connected" + the broker's error message (e.g. `unable to get local issuer certificate`, `ECONNREFUSED`) while it is down; updates within a few seconds without a reload; credentials never appear in the shown URI. *(Status tracking, the redaction and the route are unit-tested.)*
- [ ] **CA upload takes effect immediately**: with an `mqtts://`/`wss://` broker signed by a private CA and no CA uploaded → indicator shows the TLS error; upload the CA (Connection → TLS) → the bridge reconnects and the indicator turns green **without a server restart** (same for removing a cert). *(The cert-change reconnect is unit-tested.)*
- [ ] Topic autocomplete **dropdown** in the inspector populates from live traffic.
- [ ] **Auto-discovery** (HA / zigbee2mqtt): discovered devices appear; dropping one wires a suitable element; device grouping / element hints.
- [ ] **Discovery picker attribute labels (U56):** a **multi-attribute zigbee2mqtt sensor** (temperature/humidity/pressure sharing one `state_topic`) shows **one ⚡ picker entry per attribute**, each with the attribute appended (`zigbee2mqtt/sensor_1 temperature`) so they are distinguishable; the chosen value stays readable in the **closed** select and the filter box matches on the appended attribute. **Single-attribute** devices and **native-recognizer** entities (hm/WLED, `sourceLabel`) render unchanged. *(Label extraction — value_template dot/bracket leaf, object_id/unique_id suffix, device_class, name — is unit-tested.)*
- [ ] **Friendly label normalization on stamping (U62):** stamping a discovered device (⚡ picker **or** Generate wizard) writes a **friendly `label`**, not the raw name — a Homematic channel drops its trailing `:14`/`:0` (`Rolladen Wohnzimmer:1` → **Rolladen Wohnzimmer**), underscores become spaces and each all-lowercase word is capitalized (`licht_hobbyraum` → **Licht Hobbyraum**), while acronyms/units keep their case (`Desk strip` → **Desk Strip**, but `power_kWh` → **Power kWh**, `CO2`/`WLED` untouched). A zigbee2mqtt device with **no usable name** (missing, or just the platform like "switch") falls back to the **last topic segment** run through the same normalizer (`zigbee2mqtt/licht_hobbyraum` → **Licht Hobbyraum**). Applies identically to native (Homematic/WLED), Home Assistant and z2m entities. Re-normalizing an already-friendly label is a no-op (`Wohnzimmer Lampe` unchanged). *(`friendlyName` + the stamp wiring incl. the topic fallback are unit-tested in `test/feezal-discovery-stamp.test.js`.)*
- [ ] **AI-on-the-edge meter discovery (E146)** — on an [AI-on-the-edge-device](https://github.com/jomjol/AI-on-the-edge-device) (v12.0.1+), enable **Home Assistant discovery** in its web UI. Its meter appears in feezal's ⚡ discovery picker / Generate wizard as a **value sensor** with the right **state topic**, **unit** and **device_class** (water/gas/energy), and **availability** wired from `<deviceRoot>/connection` (offline treatment works when the device drops). No feezal recognizer is involved — it rides the generic HA-discovery path (device resolves every topic itself, so the user-configurable slash-containing base topic is a non-issue). If a real device's discovery shape is mishandled, the fix belongs in the HA-discovery mapping, not a native recognizer. *(The ingest of AI-on-the-edge's `homeassistant/sensor/…/config` — component/state-topic/unit/device_class + the `connection` availability → N31 — is fixture-tested in `server/test/discovery-ai-on-the-edge.test.js`; the richer json/status/rate topics get a dedicated meter element in E147.)*
- [ ] **More HA discovery component types (E149)** — publish a retained `homeassistant/<component>/<id>/config` for each new component and confirm it appears in the ⚡ discovery picker / Generate wizard on the matching element, stamped correctly: **button** (`command_topic`+`payload_press`) → a **button** card (material/glass/eink/paper/carbon) with `publish`/`payload` wired, click publishes the payload; **scene** (`command_topic`+`payload_on`) → the same button card, publishes `payload_on`; **number** (`command_topic`/`state_topic`+`min`/`max`/`step`+`value_template`) → **panel-knob** with range + `message-property`; **text** (`command_topic`/`state_topic`) → **material-input** (`publish`/`subscribe`); **alarm_control_panel** (`state_topic`/`command_topic`/`code_arm_required`) → **circle-alarm** (state banner + action topic); **camera** (`topic`) → **basic-camera** (`subscribe` + `type=mqtt-image` — the topic carries image payloads); **image** (`url_topic`) → **basic-image** (`subscribe` → URL, `name` → `alt`). Abbreviated payloads (`pl_prs`, `t`, `url_t`) expand correctly, and a component declared inside a `device`/`cmps` bundle registers too. Still **unsupported** on purpose: `water_heater`/`lawn_mower` (E150 — need controller profiles), `siren`/`update`/`event`/`notify`/`tag`/`date`/`datetime`/`time`/`device_tracker`/`device_trigger`. *(Server registration incl. the `cmps` form is unit-tested in `server/test/discovery-e149.test.js`; the real element maps + `resolveElementTag` routing in `www/test-browser/feezal-discovery-e149.test.js`.)*
- [ ] **`water_heater` rides the climate card (E150)** — publish a retained `homeassistant/water_heater/<id>/config` (keys `temperature_command_topic`/`temperature_state_topic`/`current_temperature_topic`/`mode_command_topic`/`mode_state_topic`/`modes: ["off","eco","performance","high_demand"]`/`min_temp`/`max_temp`). Place a **climate** card (any of circle/glass/metro/eink) → the entity appears in its **⚡ discovery picker** (a climate card now lists both `climate` and `water_heater` entities); linking it stamps setpoint/actual/mode topics + `min`/`max` + the four modes + label, and the card operates as a boiler (setpoint + mode buttons). The **Generate wizard** also offers it as a climate device. `lawn_mower` stays unsupported on purpose (deferred — needs per-action command topics; see E150). *(Server registration + the alias stamp/`elementAcceptsComponent` matching are covered by `server/test/discovery-e150.test.js` + `www/test-browser/feezal-discovery-e150.test.js`.)*
- [ ] `wss://` (TLS) viewer connection against a real broker. *(Plain `ws://` direct and the `mqtt://` bridge are automated.)*

### Generate wizard — Devices + App (U58)

Reached from the top-bar **Generate** button (the `auto_awesome` ✨ icon, between the copy/paste/undo cluster and the `<>` source-mode toggle; hidden in source mode).

- [ ] **Tile chooser:** clicking Generate opens a large popup with two tiles — **Devices** and **App** — both live. Clicking outside the dialog does **not** close it; **Cancel**/Esc does.
- [ ] **Devices → family + list:** the Devices tile shows a family segmented control (Circle / Glass / Metro / E-ink — only families that actually ship device cards), a filter box, and the discovered devices **grouped by source** (Homematic / zigbee2mqtt / WLED / evcc, matching the ⚡ picker's grouping). Only generatable entities appear (raw/unknown MQTT topics are filtered out). The filter narrows by label and component type live.
- [ ] **Selection:** ticking rows updates the "N selected" count and the footer **Generate N elements** button (disabled at 0). A group-header checkbox selects/deselects all **eligible** rows in that group. Rows whose function has **no element in the chosen family** are greyed, un-tickable, and annotated "no &lt;family&gt; version"; switching family re-evaluates every row live (a row eligible in Circle may be a gap in E-ink).
- [ ] **Range + drag select (U68):** a plain click is always a plain toggle. **Shift+click** applies the clicked row's toggle **result** to the whole range from the previous click to it — clicking a row ON then Shift+clicking another **checks** the span; clicking a row OFF then Shift+clicking **clears** the span (across group boundaries). **Shift+click with no prior click** extends from the clicked row **upward** until the first row already in that state (a select-up-to-the-boundary fill). **Press and drag** over rows paints the pressed row's action onto each crossed row (mouse; the list is selection-proof so no text is dragged). The anchor stays put so a second Shift+click moves the endpoint; greyed parity-gap rows are never selected. Same behaviour in **Devices** and the **App review** list. *(Range fill/clear, both directions, the boundary fill and drag are browser-tested in `test-browser/feezal-generate-dialog.test.js`; the helper is `src/feezal-range-select.js`.)*
- [ ] **Generate → auto-grid:** hitting Generate drops one element per selected device onto the **current view**, laid out in a left-to-right grid that wraps to the view width; each element is **pre-wired** (same attributes the ⚡ picker would stamp — topics, availability, low-battery, `discovery-id`). One undo snapshot covers the whole batch.
- [ ] **Append-only + dupe guard:** generating again (or onto a view that already holds some of those devices) **adds only the missing ones** — devices already on the view (matched by `discovery-id`) are skipped, never duplicated, and existing elements are untouched. The result screen lists them under "Already on this view".
- [ ] **Parity gap report:** if the batch included functions the family lacks, the result screen shows "No &lt;family&gt; element (N)" grouped by component — nothing is silently dropped. **Pick more** returns to the list; **Done** closes.
- [ ] **Dark mode:** the whole wizard (tiles, list, result) is legible in dark mode — filter field, its **focused** state, the checkboxes and the **Cancel** button (incl. on hover) all stay dark (no white flashes).
- [ ] **Stable popup size:** while typing in the filter, the popup does **not** grow/shrink as the list shortens — the device list area keeps a fixed height and scrolls internally.

**App mode (Phase ②):**

- [ ] **U80 — App always builds onto a NEW site (created at Generate, not before):** clicking the **App** tile first asks for a **new site name**, prefilled with the next free **siteN** (`site1`, `site2`, … — computed from `/api/sites`). **Continue** does **not** create the site — it only remembers the name and moves on to the App setup **on your current site** (which stays untouched). Go all the way through setup → rooms → review; **only when you click "Generate app"** is the new site created (POSTed with `fromSite`, inheriting the MQTT connection), the editor **switches to it**, and the app is generated + **auto-deployed** there. Verify the deferral: type a name, **Continue**, then **Cancel/close** the wizard before generating → **no new site was created** (check the site manager). A duplicate name is caught **at the name step** (Continue shows "already exists" and stays put); a `/`-containing name errors too. The result screen shows a **prominent "Open <site> in the viewer" button** (`/viewer/<site>/`, new tab) plus an Export hint. The empty scaffold view (`view1`) the new site ships with is **removed**, so **Menu is the sole first/default view** — opening the viewer with no hash lands on Menu, not an empty page. (The **Devices** tile is unchanged — it still adds elements to the current view.)
  - **Selection survives the switch:** the rooms you edited, the devices you (un)checked, and any manual room re-assignments made in the review are **preserved** across the site-switch reload — the generated app matches exactly what you picked (it is not re-detected from scratch on the new site).
  - **Result screen goes straight to progress (no family screen re-shown):** after the reload the wizard lands **directly on the last (result) dialog** — it does **not** flash the axis/family setup screen again. That screen shows a live **progress checklist** (*New site created ✓ → Discovering your devices → Generating the app → Publishing to the new site*, spinner on the active step). The **"Open in the viewer" button is disabled** (shows a spinner + *"Preparing …"*) until the **initial auto-deploy actually finishes**, then it turns into the real enabled link. Verify you cannot click through to the viewer before the deploy lands.
  - **Discovery is not instant on a fresh site:** the new site's broker connection is only established as the site loads, so discovery can lag a few seconds. In the auto-flow the App setup **polls** for discovered devices (up to ~24s) instead of reporting "0 devices" immediately — while it polls it shows *"Connecting to your broker and discovering your devices…"*, and if nothing shows up it offers a **Retry** link. Verify: on a working broker the device count fills in on its own within a few seconds (no manual retry needed); it does **not** flash "No devices discovered" while still connecting.
- [ ] **Step 1 — axis + family only (U69):** after the new-site step, the App wizard opens on just a **By room / By function** toggle and the **family** picker, plus a "**N** devices discovered" note — there is **no flat device checklist here anymore**. The primary button reads **Review rooms…** on the room axis (→ the U78 room step) or **Review devices…** on the function axis (straight to the device review); **nothing is created** if you back out or cancel.
- [ ] **U78 — room review step (room axis only):** picking **By room** and continuing lands on a **Rooms** screen listing the detected rooms *before* any devices. Each row has a **drag handle**, the **icon picker** (our standard icon field — type/pick a Material/mdi icon), an editable **name**, **↑/↓** buttons, and an **✕** to remove the room. An **Add a room…** input (Enter or the button) appends a room; the **order here is the app drawer order** — reorder by drag or ↑/↓. Continuing to **Review devices…** re-assigns every device to this edited list: a device whose room you **deleted** is re-scanned and, if its name/area matches a **remaining** room (or one you **added** — e.g. add "Küche" and a `kueche_*` device joins it), lands there; otherwise **Unassigned**. The device review then shows the buckets **in your room order**. The function axis skips this step entirely. (Renaming a room to a name that already exists is rejected.) **Stable size:** removing rooms does **not** shrink the dialog — the room list keeps a reserved minimum height, so the dialog stays its initial size as you delete (and the per-row icon picker popup is never clipped).
- [ ] **Illustrated family picker (U71):** the family control is a **gallery of thumbnails** — a bundled screenshot per family (Glass / Metro / Circle / E-ink so far), the selected one framed in the primary colour with its label bolded; a family with no image yet shows a placeholder tile + its label. The images are **self-hosted** (bundled, no external request — check the network tab: only same-origin asset URLs) and lazy-loaded. Picking a family re-evaluates the discovered-device count and the review buckets.
- [ ] **App picker excludes plain families:** the App flow's family gallery offers only the styled, app-oriented families (Glass / Metro / Circle / E-ink) — **Basic** and **Material** do **not** appear here (they remain on the **Devices** tile's family bar). If you had Basic/Material selected on the Devices tile and then start the App flow, it falls back to the first app family (Glass) — the App is never generated in a plain family.
- [ ] **Review is the selection (U69):** every discovered device starts **checked** in its bucket; **untick** the ones you don't want by clicking their **checkbox** (a bucket header checkbox toggles the whole room, showing an indeterminate state on partial). The footer count and "Generate app (N views)" reflect only checked devices; a bucket you fully uncheck creates **no view**. Buckets seeded from HA `suggested_area` / a device-group area (trusted → green **"area"** badge, shown verbatim), the multilingual room-word lexicon (**"guessed"**), or a U77 recurring-name cluster (**"detected"**); unmatched → **Unassigned** (last). Rename a bucket to an existing name → **merge**. (Row/checkbox interaction is U75; room matching is U76/U77 below.)
- [ ] **Room-review provenance badges:** in the room-review step (and the device-review buckets) every room carries a badge that says where it came from — **"detected"** (recurring-name cluster), **"guessed"** (matched from device names via the lexicon), or a green **"area"** (an explicit Home Assistant `suggested_area` / device-group area, taken verbatim). A **user-added** room (via *Add a room…*) carries **no** badge. Specifically: a device whose area is e.g. `Hobbyraum` shows a room **"Hobbyraum"** with the **"area"** badge — *not* "guessed" (even though `hobbyraum` is also a lexicon synonym for Basement, the trusted area wins verbatim and out-ranks the lexicon). *(Provenance flags + the area badge are unit/browser-tested.)*
- [ ] **＋ Create new room (U70):** in **room** mode, each device's move-to dropdown ends with **"＋ Create new room…"** → a small **dark-mode-aware** dialog opens (stacked above the wizard) to type a name; **Create** (or Enter) moves the device into the new room and makes it selectable for the others; **Cancel**/empty leaves it put. A name matching an existing room merges rather than duplicating. (Function mode has no create option — its buckets are the fixed taxonomy.)
- [ ] **Review — functions:** deterministic buckets in taxonomy order (Lights, Switches & sockets, Covers & blinds, Climate, Windows & doors, Motion & presence, Alarms, Sensors, Locks, …) — no "guessed" badges; only buckets with devices appear.
- [ ] **Generate:** one undo snapshot creates (or **reuses**) the `Menu` view with a `layout-app` (`rail: auto`, site-name title, **content max-width 960px** + 12px inset, and the shell element itself sized **100% × 100%** so it fills the view) and one **flow-layout sub-view per bucket** (`flow-justify: start` — cards fill from the top-left) — view *names* are **slugs** (`Büro` → `buero`, stable across runs; URLs stay clean) while drawer entries keep the human **labels** + lexicon icons; every device card is pre-wired exactly like the ⚡ picker. Switch the editor to the Menu view: the drawer navigates between the generated sub-views; cards wrap responsively and the content column is capped/left-aligned, giving **more than two columns of glass cards on a wide window** (the 520px→960px cap).
- [ ] **U67 — Menu is the first/default view, selected after generation:** on a site that already has hand-made views, after generating the **Menu** view is the **first** tab (so it is the site/viewer **default** — what opens with no hash), the generated sub-views follow (bucket order), and the pre-existing views are pushed to the **end**. The editor **switches to the Menu view** immediately, so closing the result dialog lands on Menu (not the view you generated from). Open the viewer fresh (no `#/…`) → it opens on **Menu**.
- [ ] **Generated views use grid layout (U90)**: after Generate, every generated **sub-view** carries **child-position = grid** and **grid-justify = center**; the gap is **0 for the glass family** (its cards already carry their own outer spacing, so a gap on top reads as a double gutter) and **10 for every other family**. The **Menu** and **System** views are deliberately NOT converted. The point of the change: generate an app for a setup that includes a Frigate camera → the camera card is larger than the surrounding sensor tiles, and the cards after it **flow into the space beside it** instead of leaving it dead as flow did. Cards should sit centered as a block when the view is wider than a whole number of columns.
- [ ] **U67 — no zigbee housekeeping cards:** a zigbee2mqtt device's diagnostic entities (`linkquality`, `last_seen`, OTA `update`, …) are **not** offered in the Devices/App lists or carded — only the device's functional entities appear (HA `entity_category: diagnostic`/`config` and `enabled_by_default: false` are filtered). A multi-function device still yields one row per *functional* entity.
- [ ] **U67 — localized room labels:** with the site locale set to German (`feezal.locale`/site `locale` = `de`), the matched room buckets read **"Wohnzimmer" / "Küche" / "Badezimmer"** (not "Living room"), and the German view slugs follow (`wohnzimmer`); an explicit HA `suggested_area` is shown **verbatim** (never translated); a locale with no dictionary (e.g. `ja`) falls back to English. Reuses A27 Phase 1's locale machinery. (Function-axis labels are not yet localized — a follow-up.)
- [ ] **Idempotency:** re-running App mode reuses the existing shell (never a second `Menu`/`layout-app`), **merges** into same-named sub-views, skips devices already carded **anywhere in the site** (`discovery-id` guard) and appends only missing drawer entries — existing views/elements/entries are never touched or cleared. The result screen reports created views, added cards and skips.
- [ ] **B94 — room-move dropdown stays in sync:** in the review, a room bucket with **two or more** devices; change the **top** device's room via its dropdown. The device that shifts up into the vacated slot must still show **its own** room in its dropdown (not the room you just moved the first device to); the moved device shows its new room. (Was a stale-DOM reuse bug — the row beneath inherited the moved row's `<select>` value.)
- [ ] **U75 — two independent layers, each with the right multi-gesture:** clicking a device row's **checkbox** toggles only that device's *inclusion*; clicking the row **body** does NOT toggle it (and clicking the checkbox does NOT select the row).
  - **Checkbox layer (inclusion) — Shift/drag multi-check restored:** on the **checkboxes**, a plain click toggles one; **Shift**-click ranges the toggle from the last checkbox you clicked; **press-and-drag** across the checkboxes paints the same on/off onto each. (This is the device-list behaviour, now working in the App review too — it had been swallowed by the row-select layer.) These gestures change only the **checkboxes**, never the row highlight.
  - **Row-select layer (highlight for bulk move) — Explorer/Finder semantics:** click a row **body** to select **only** that row (any previous selection clears); **Shift**-click selects the contiguous **range** from the anchor; **Ctrl/Cmd**-click **toggles** a single row in/out while keeping the rest. There is **no drag-to-paint** on the row layer, and a plain click never adds to an existing selection. These change only the **highlight**, never the checkboxes. In **dark mode** the selected-row highlight is a **dark blue** (not the old light `#cfe5fb`, which was unreadable on the dark dialog).
  - While ≥1 row is selected, a **bulk bar** shows "N selected" with **Move to room…** (existing rooms + ＋ Create new room), **Check**, **Uncheck**, **Clear**. Bulk-move drops every selected device into the target room and clears the selection; "＋ Create new room" moves the whole selection into the new room. Selection is ephemeral — it resets when you re-enter the review and never affects the checkboxes.
- [ ] **U76 — room matching (compounds + umlauts):** a device whose name carries a room word inside a **compound** now resolves — e.g. a channel named **"Schlafzimmerfenster"** lands in **Bedroom** (German prefix compound), not Unassigned; `küche`/`kueche` fold to the same room. The guards still hold: **"bad" never fires inside "badge"**, and an `ipcamera`-style name is not dragged into Bedroom by the ambiguous `camera` stem.
- [ ] **Guest toilet / guest room are their OWN rooms:** a device named **"gästetoilette"** (or ASCII **"gaestetoilette"**, or **"gästewc"**) is suggested as a distinct **Guest toilet** (de **Gäste-WC**) room — **not** swallowed into generic Bathroom (the specific word out-ranks `toilette` under the longest-match rule). A device named **"gästezimmer"** → **Guest room**. Both umlaut (`gäste…`) and ASCII (`gaeste…`) spellings work. A plain `toilette`/`wc` device still lands in **Bathroom**.
- [ ] **U77 — detected zones (recurring names):** devices the lexicon can't place but that **share a custom token across ≥2 devices** (a nickname / person / pet / wing the user used as a zone, in Homematic AND zigbee names) are pre-grouped into a bucket named after that token, badged **"detected"** (distinct from "guessed") — instead of all landing in Unassigned. A token on a single device, or a device/function/brand word (sensor, zigbee, light, …), never forms a zone. Precedence: an explicit `suggested_area` and a lexicon room both win over a detected zone. The detected bucket is a **suggestion** — rename, merge (U75 bulk-move), or dismiss (uncheck) it like any other.
- [ ] **U74 — family-matched site theme:** generating a fresh app sets the **site theme** to match the chosen family so the result looks like its U71 screenshot — **Glass → midnight-blue** *and every generated sub-view gets the default gradient* (`linear-gradient(180deg, #0284c7 0%, #1e293b 100%)`) behind the frosted cards; **Metro → metro**; **Circle → gruvbox-light**; other families keep the default. The theme is applied whenever the site is still on the **default** theme (so it works even on a reused shell) and **persists** — it goes through the themes sidebar so it survives deploy + reload (not just an in-editor className); it never overrides a theme you have already picked. The glass sub-view gradient is a one-time stamp.
- [ ] **U79 — hidden "System" view:** a generated app also contains a view named **System** that is **not** in the drawer menu, holding a **splash** and a **connection-status** element. In the editor it shows both placeholder chips; it never appears as a drawer entry and is never the active view; a re-run doesn't duplicate it. In the **viewer**, the splash still covers boot and the connection-status banner still appears on disconnect **even though its view is never shown** — both hoist themselves to `<body>` (the connection-status element gained this portal; check it works from the hidden System view AND when placed on a normal view). Remove the System view (or its elements) to opt out. The gradient **renders immediately** and the view's **Background** inspector (Style tab) shows it as a gradient with the two stops **without needing a re-edit** — it's written as `background-image` (the property the editor reads/writes), and the editor parses a gradient even when the browser drops the default `180deg` on round-trip.

*(The stamp/resolve/grid core + room lexicon/buckets/slugs + localized room labels in `feezal-discovery-stamp.js` are unit-tested; the dialog's generate/dupe-guard/parity-skip orchestration, the diagnostic-entity filter, the U72 card ordering, the U74 family theme + glass gradient, view ordering and the App scaffold incl. idempotent re-run, area-beats-lexicon and review editing are browser-tested in `test-browser/feezal-generate-dialog.test.js`.)*

### Autodiscovery grace period (Editor Settings → Autodiscovery)

Server-side ghost filter, configured from **Editor Settings → Autodiscovery** and persisted in `<dataDir>/editor.json` (`discoveryGraceEnabled` / `discoveryGraceDays`).

- [ ] **Defaults:** on a fresh data dir the section shows **Hide stale devices = ON** and **Grace period = 30 days**.
- [ ] **Effect (Homematic climate only):** a heating/climate device whose newest datapoint `ts` is older than the grace period is **hidden** from the ⚡ discovery picker and the Generate list; a fresh message on it makes it reappear. Event-driven devices (contacts, lights, covers) are **never** hidden regardless of age. An already-placed element re-syncs by `discovery-id` even if the device is currently stale.
- [ ] **Toggle off:** turning the switch off hides the days field and shows all devices regardless of age (takes effect on the next discovery fetch, no restart).
- [ ] **Change days:** lowering the grace (e.g. 7) hides devices between 7 and 30 days old; raising it brings them back. The value survives a page reload and a server restart (persisted server-side).
- [ ] Without a server data dir (serverless/static preview) the controls still render and fall back to the defaults; nothing crashes.

*(The read-time climate staleness filter, `{enabled, days}` mapping and event-driven exemption are unit-tested in `server/test/native-discovery.test.js`.)*

## 10. Site management, versioning, export

(Site manager UI — create/rename/duplicate/delete — is automated, as is the
export: A16 layout — `<sitename>/{index.html, assets/}`, referenced-only,
no `global/` folder, rewritten references — and opening the bundle from
`file://` with live MQTT and a loading image.)

- [ ] **Export dialog / bundle size breakdown (U34):** Deploy menu → Export opens a dialog; after a spinner ("first run builds…") it shows total minified / ~gzip kB and a sorted bar list (element packages, `lit`, `feezal core`, theme, `icons (tree-shaken)`), top 3 highlighted, per-row kB / ~gz / %. **Download ZIP** downloads `<site>.zip` and closes the dialog. Re-opening is instant (shared bundle cache). With an `mqtt://` connection the old "Cannot export" error dialog still appears instead. *(Report bucketing/normalisation, the `/bundle-report` route sharing the export cache, and the dialog render/error states are automated.)*
- [ ] A site named like an old reserved word (`themes`, `elements`) is allowed (post-A14).
- [ ] **Default site topics:** a new site opens with Subscribe Topic `feezal/<sitename>/set` and Publish Topic `feezal/<sitename>` prefilled (Site tab). **Rename** the site → both topics follow the new name; change a topic first (e.g. `home/stat`) and rename → the customised topic stays, the untouched one still follows. **Duplicate** retargets default topics to the clone's name. Element-level `subscribe`/`publish` attributes on canvas elements are never rewritten. *(Default scaffold, per-attribute rename/clone retargeting and the element-attribute exclusion are unit-tested.)*
- [ ] **New site inherits the current site's MQTT connection:** with the current site connected to a broker (broker/port/protocol, credentials, TLS on, a CA/client cert uploaded), create a **New** site → open its Connection settings: the **broker, port, protocol, username/password, TLS options and the cert files are already there** (same as the site you were on), so the new site connects out of the box. Only the connection is inherited — the new site's **views start empty** and its theme/other config are the fresh defaults; a **Duplicate** still copies everything as before. *(The `POST /api/sites` connection-config seeding + TLS-cert file copy from `fromSite`, and the no-`fromSite` case inheriting nothing, are tested in `server/test/api.test.js`.)*
- [ ] **Git history** panel: timeline, per-commit preview (`?sha=` banner + prev/next), restore (non-destructive), discard (archived branch), bookmarks (if present).
- [ ] **Certs are never committed** to a site's git repo (in `.gitignore`).

## 11. AI assistant (full) — see also §2

(Panel visibility gating, model listing, prompt → streamed reply → proposal
card → Accept-applies are automated against a stub provider.)

- [ ] Panel docks right, resizable (width persists); **open/closed state persists**.
- [ ] Source mode (Monaco diff/splice) apply path.
- [ ] Conversations: new, history timeline, delete; file-context chips (＋ / drag-drop); token estimate.
- [ ] Larger multiline input auto-grows; activity animation while busy; agent toggle chip; tool-call limit + num_ctx settings honoured.
- [ ] Editor-internal classes are never echoed back into proposals.

## 12. Viewer

- [ ] No editor chrome; elements interactive — taps/clicks publish.
- [ ] Theme applied; user `.css` theme `<link>` present when active.
- [ ] Mobile/touch: taps, swipe nav; app-layout/responsive behave at small widths.
- [ ] Reconnect on broker drop with a **direct `ws://` connection**. *(Bridge-mode reconnect is automated.)*
- [ ] **Gradient view background stays put (B62)** — give a view a **Gradient** background (Styles → Background → Gradient) and enough content to scroll. In the **viewer** on **iOS Safari** (normal tab AND the installed Home-Screen/standalone PWA, which is where it was reported): scroll to the bottom and rubber-band past both ends — the gradient must **stay fixed to the viewport**, never scroll away and never repeat/tile with a visible seam; the iOS status-bar inset and the overscroll bounce still show the view's colours (not white). **Check Chrome/Windows too — it was wrongly believed correct there for two attempts, and it was not.** Then set the same view to a **solid colour** → unchanged behaviour (the pre-B62 path). Check the view **both directly and embedded in a layout-app drawer entry** — those are two separate rendering paths and each needed its own fix. In the **editor** the checkerboard still fills the area outside a fixed-size view (by design — **U61**, which is the same overflow seen from the editor side). *(Automated: `test-e2e/b62-sticky-background.test.js` samples the RENDERED PIXELS at a fixed viewport point, scrolls, and samples again — pinned means byte-identical. Deliberately not a CSS-text assertion: attempt #2 asserted the stylesheet contained `position: fixed` and passed while the bug was fully present.)*

- [ ] **Known NOT fixed by B62, do not conflate:** (a) **glass cards leave rectangular artifacts** after scrolling up on iOS — that is `backdrop-filter`, isolated by testing the same gradient with non-glass cards (artifacts gone); (b) a **second, outer scrollbar** in desktop Chrome — never reproduced in E2E (`document.scrollsBy: 0` in both configurations). Both need their own repro; neither is a reason to touch the background code again.

### Viewer presence & Clients panel (N24) — see docs/presence.md

*(ID generation/persistence, retained status payload, per-client command
routing, rename incl. old-status clear, collision toast, `presence="off"`,
hub disconnect-clear and bridge retain-forwarding are automated.)*

- [ ] Open a viewer with a site **Publish Topic** set → corner toast "Connected as \"viewer-xxxx\"", dismissible. Without a Publish Topic no presence (even with a Subscribe Topic).
- [ ] Broker shows retained `<publish>/clients/<id>/status` JSON (view, connectedSince, lastChange, connection, userAgent); switching views updates `view` in the payload.
- [ ] With a Publish Topic but **no Subscribe Topic**: viewer announces itself (row appears) but the per-client actions in the Clients panel are disabled and the viewer takes no commands (announce-only). *(Announce-only subscription behaviour is unit-tested.)*
- [ ] Editor → **Site Settings → Clients** tab lists the viewer live; a second viewer (other browser/device) appears as a second row.
- [ ] Editor **dark mode**: the Clients tab paints a dark panel background (no white area behind the client cards); text, card heads and inputs stay readable.
- [ ] **Switch view** select shows the client's current view and follows it live (viewer-side navigation updates the dropdown); picking another entry changes the view on exactly that viewer (other viewers unaffected); **Reload** reloads it; **Set theme** restyles the viewer (until its next reload — the deployed theme is baked into the site HTML).
- [ ] Viewer reconnect (toggle the network briefly / restart the broker) → the client row survives or reappears; a stale broker LWT clearing the status is self-healed by the viewer republishing it. *(Self-heal republish is unit-tested.)*
- [ ] Reload the **editor** while a viewer is online → its row reappears immediately (the hub asks the broker to replay retained state on subscribe, even for statuses first retained after the server started). *(Automated in the hub full-chain tests.)*
- [ ] Reload the editor, then open **Site Settings → Clients** → the row appears without further steps (the panel wires itself on load, before the site DOM exists); changing the site Publish Topic rewires the panel live. The console traces this as `[feezal-clients] subscribing <topic>`. *(Late-site wiring is automated.)*
- [ ] A **static export** behaves like the served viewer: first load shows the "Connected as …" toast, the site appears in the Clients panel, and per-client commands (view/theme/reload/rename) reach it. *(The presence import in the export entry is automated.)*
- [ ] **Rename** to `hallway-panel` → viewer toast, row re-appears under the new id, old status topic cleared on the broker; rename survives a viewer reload (`localStorage`).
- [ ] Close the viewer tab → row disappears (direct: broker LWT; bridge: server clear). Kill the browser process (ungraceful) with a direct-MQTT connection → LWT still clears the status.
- [ ] Site-wide control topics (`<subscribe>/view`, …) still steer **all** viewers.
- [ ] Duplicate id (copy `localStorage` value to a second browser) → "already online" warning toast, both keep working.
- [ ] Site Settings → Viewer → **Viewer presence** off → redeploy: no status published, no toast, Clients panel empty.
- [ ] **All four broker protocols** (`mqtt://`, `mqtts://`, `ws://`, `wss://`, mosquitto): the Clients panel lists viewers and survives a **viewer page reload** (row stays/reappears within seconds). Watch the server console: no `mqtt-bridge: connection closed` after a presence clear — with a ws(s) broker, an empty retained publish used to kill the bridge connection (mqtt.js sends the empty payload as an empty WebSocket frame; mosquitto drops the client with a protocol error). *(Empty-frame guard, hub cache JSON parsing and panel string tolerance are unit-tested; the mosquitto interaction was verified against a real broker.)*

## 13. Auth / deployment (if used)

- [ ] Editor auth gate in the browser; viewer public vs protected as configured.
- [ ] Reverse-proxy setup (nginx) + proxy auth path (Authentik/Authelia) if documented.

---

## Regression sign-off

- [ ] `npm run test:all` (root — all four suites; on Windows run via WSL) green. Needs chromium (`npx playwright install chromium`) and a built `www/dist`; see `www/vitest.browser.config.mjs` for WSL notes.
- [ ] `cd www && npm run build` clean.
- [ ] No new console errors/warnings during the manual pass.
- [ ] All `[!]` items filed as issues / roadmap bugs.

---

## Automation candidates (shrink this file further)

Ordered by value-per-effort; each would delete or shrink checklist items above.

1. **Agent-mode AI E2E** — extend the stub provider with OpenAI tool-call responses; assert the tool loop (activity stream, topic-search tool round-trip, final proposal). Removes most of §2's agent block.
2. **Remaining N6 inspectors** (component) — same pattern as navbar/layout-app for light, climate, cover, alarm-panel, humidifier, media-player, vacuum, repeater, energy-flow, layout-flex, layout-responsive. Removes the §4 N6 item.
3. **E2E: multi-step undo/redo + cut** — deepens §3's remaining interaction items.
4. **E2E: resize via handles + snapping** — drag a resize handle, assert size; drag near another element and assert the snap position. Removes the §1/§3 resize items.
5. **Component: dialogs + repeater** — open/close triggers and array-payload child rendering. Removes the §6 element-specific notes.
6. **`wss://` TLS harness** — self-signed cert on the ws listener + cert upload via the API; automates the §0/§9 TLS items.
7. **E2E: git history panel** — deploy twice, open the history sidebar, preview `?sha=`, restore. Removes the §10 history item.
