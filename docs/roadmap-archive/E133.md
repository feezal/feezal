# E133 — Palette category rename: "Material" → "Circle", "Simple" → "Material" ✅ done

*Archived roadmap item — Element Ecosystem. Open items: [../ROADMAP.md](../ROADMAP.md) · Index: [README.md](README.md)*

**✅ Implemented (07/2026)** — as scoped (display strings only, nothing serialized, nothing breakable in saved sites):

- `palette.category` swept across **39 packages** (15 device cards → **Circle**, 24 MD3 widgets → **Material**), each patch-bumped; verified bumped set ≡ swept set.
- Palette `ORDER` is now `Components, Basic, Layout, System, Circle, Glass, Metro, Material, Carbon, Paper, Panel, TUI` (the suggested placement — Circle where the device cards sat).
- Collapse-state localStorage keys self-heal on first run (accepted harmless staleness, as designed).
- Docs: TESTING.md §6 heading + B42 filter row (`circle` / `material` as search terms); **element-spec.md §3.1 was still carrying the pre-07/2026 `'Device'` guidance** — its category table, the §3.1 note, the device-card checklist and the publishing checklist now say `'Circle'` and document the full fixed order. The AI prompt needed no change (its hardcoded text concerns TAG prefixes, which are untouched; the catalogue categories flow from the descriptors).
- Landed in the same release wave as **E130** (outlet → "Switch" palette name), per the coordinate-the-renames decision; E132's tag renames remain separate (blocked on the alias mechanism).

**Naming tension, accepted deliberately:** the Circle-category cards keep their `feezal-element-material-*` tags (tag prefix ≠ category label — already true before for the Simple category). The full prefix cleanup is E113/E115/A23 territory.

Original item:

The 07/2026 category split left a naming mismatch: the **device cards** (light, climate, cover, contact, wled, …) sit in a category called *Material*, while the actual **MD3 widgets** (button, checkbox, gauge, slider, …) sit in *Simple*. Rename both so the labels say what they mean:

| Category today | Becomes | Content | Why the name |
|---|---|---|---|
| `Material` (~15 elements) | **`Circle`** | device cards | the circular-slider design language of light/climate/cover (see E134) |
| `Simple` (~24 elements) | **`Material`** | MD3 widgets | they literally are Material Design 3 controls |

**Cheap by construction — but wide:** the palette category is a **display string only** (never serialized into dashboards, no tag/package involvement — unlike E130/E132's tag renames, nothing can break in saved sites). The touch points: `palette.category` in ~39 packages, the palette's fixed `ORDER` array, the collapse-state localStorage keys (harmless staleness), docs (TESTING.md §6, element-spec.md, AI prompt check), B42's filter TESTING row.

**Relates:** **E113** (taxonomy — this bakes "category = style family" one step further), **E134** (the Circle name's design commitment), E130 ✅/E132 (the concurrent renames), U45 (picker rework — builds on these category names), B42 ✅ (category filter).
