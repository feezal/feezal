# E132 — Generalize the boolean-sensor family: *-occupancy → *-sensor, numeric *-sensor → *-number ✅ done *(tag renames pre-staged — blocked on the alias mechanism as designed)*

*Archived roadmap item — Element Ecosystem. Open items: [../ROADMAP.md](../ROADMAP.md) · Index: [README.md](README.md)*

**✅ Implemented (07/2026)** — everything except the actual tag swap, exactly as the item scoped it ("this item can pre-stage everything except the actual tag swap"):

- **Palette pre-stage:** the boolean cards show **"Sensor"** in all three families; `glass-sensor`/`metro-sensor` (numeric) show **"Number"**. Tags/packages unchanged — the tag renames execute once the load-time tag-alias mechanism exists (E115/E130's shared wall; that mechanism is the remaining follow-up and lives with E115).
- **Shared vocabulary, defined once** (`@feezal/feezal-element/feezal-sensor-types.js`, the E117 pattern): `SENSOR_TYPES` (motion/presence/radar/zone + water-leak/smoke/gas/co/vibration/tamper/generic with per-type default icons, state texts and the `alarm` flag), `SENSOR_TYPE_OPTIONS`, the hazard-aware `SENSOR_DEVICE_CLASS_MAP` (`moisture→water-leak`, `carbon_monoxide→co`, …, `_default: generic`) consumed by all three cards' discovery maps, the **E124 `batteryLowAttributes` trio + `battery-low-threshold`**, and `batteryLowFromValue()` (booleans/compare-payload/percentage-vs-threshold).
- **Cards:** all three offer the full type list with type-default icons/texts (empty text attrs = type default); **alarm classes render their active state in the error colour** (material hazard view, glass `--_state-color`, metro tile background); glass gained `icon-active`/`icon-clear` for metro parity; material renders hazard classes as icon+text (the four motion types keep their SVG art; E134 turns the hazard view into the circle state disc). All three carry the battery contract with a warning badge — a weak battery never blacks out the state.
- **Discovery, both ecosystems:** z2m via the extended shared map; Homematic via **E131's** generalized recognizer (motion confirmed; water/smoke best-knowledge, verify-gated). **E124 core machinery landed for the sensor wave:** server-side `battery_low_normalized` (z2m sibling lookup at read time — boolean `battery_low` sibling preferred, percentage `battery` sibling as the fallback without `payloadLow`; **bracket-form `value_json["x"]` templates now parse** in both the server `templateToProperty` and the client `valueTemplateToPath`), client `_applyDiscovery` auto-stamps the trio **guarded on the element declaring the attribute** (contacts/climate join with E124 proper).

Versions: feezal-element 3.0.5, material-motion 3.0.3, glass-occupancy 3.0.8, metro-occupancy 3.0.3, glass-sensor 3.0.8, metro-sensor 3.0.2. Tests: server (sibling producer incl. bracket templates + fallback + guards), client applier (stamp + declaration guard), browser (type table/map/battery on all three cards, alarm colouring). TESTING.md row added.

**Remaining for later items:** the actual tag swaps (blocked on E115's alias map), E124 proper (contacts + climate + the hm contact recognizer's LOW_BAT fix + moving LOWBAT out of availability), and real-hardware verification of the HmIP/water/smoke channel types (E131's caveat).

Original item (abridged): water/fire/motion sensors all behave the same way — one boolean state, an icon that flips, a text per state, battery hardware. Rename plan (metro-sensor→metro-number, glass-sensor→glass-number, material-motion→material-sensor, glass-occupancy→glass-sensor, metro-occupancy→metro-sensor) blocked on the tag-alias mechanism; generalized type list with per-type defaults and alarm-coloured active states; discovery valueMap extension for both ecosystems; battery (E124) as part of the contract.

**Relates:** **E124** (battery indicator — its core machinery landed here for the sensor wave), **E131 ✅** (motion recognizer — implemented against the generalized type list), E130 ✅ (the sibling rename blocked on the same tag-alias mechanism), **E115** (family switching — natural home of the alias map), E114 (parity — landed family-wide), E113 (taxonomy — "sensor means boolean, number means numeric").
