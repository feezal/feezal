# Keep element and theme packages isolated in www/node_modules
# so the element discovery scanner (server/src/build/elements.js) can find them
public-hoist-pattern[]=!@feezal/feezal-element-*
public-hoist-pattern[]=!@feezal/feezal-theme-*
